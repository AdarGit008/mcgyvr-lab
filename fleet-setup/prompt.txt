You are running an autonomous fleet-stamping run in the mcgyvr measurements worktree.
cwd: /home/adaramir/pi_agent/projects/mcgyvr-measurements

GOAL: create three fleets (a-solo, b-small, b-big) on dev and stamp them to live
via `mcgyvr fleet lock`.

READ FIRST, and follow it in order: fleet-setup/run-plan.md (the authoritative,
owner-decided plan). Drafts are beside it: fleet-setup/fleet.yaml and
fleet-setup/policy.yaml — placeholders marked `<>` are to be filled as you
measure; every room/window value is a pin-to-dev-run, not a guess.

Execute the run-plan tasks in order:
1. Build the Lidenburg docker image on srv2 (fork at /home/adaramir/llama.cpp-lidenburg,
   `.devops/cuda.Dockerfile` + liburing-dev; tag llamacpp-lidenburg:e85e4d9).
2. Sweep srv1 Qwen3.6-35B max-context (width 1, `-c 32768` then `65536`, stop at the RAM ceiling).
3. Validate srv2 Lidenburg 35B at 256k via llama-server (not cli).
4. Validate the four b-fleet combinations (3B+7B, deepseek, 80B, 35b_b).
5. Validate the four switch moves (b-small <-> b-big, 2 rigs each).
6. Compute the rig-/unt- identity digests, finalize fleet.yaml/policy.yaml,
   assemble evidence.json, run `.venv/bin/mcgyvr fleet lock`, and iterate on
   every refusal until it writes records/fleet/.
7. Report.

GUARDRAILS (non-negotiable):
- dev profile only. Both rigs are idle (card at 1 MiB) — do not leave anything
  running; tear down after every run.
- srv1 is lock-prone: stay at or below `--n-cpu-moe 30`, watch MemAvailable/swap,
  never let it page (see flexibility-2026-09-09 Q7).
- Every number must come from a run you actually did, or a cited prior-art file
  under records/measurements/ (the run-plan lists them). Harvest prior art
  rather than re-measuring. Never invent a number.
- SSH via ~/.ssh/config (hosts srv1, srv2). mcgyvr via `.venv/bin/mcgyvr`.
- Record all outcomes under records/measurements/fleet-setup-<date>/.

At the end, write fleet-setup/REPORT.md containing: the final fleet.yaml and
policy.yaml, the evidence.json, the `mcgyvr fleet lock` output, a
per-combination and per-move table of pinned numbers with evidence paths, and an
explicit list of anything that could not be pinned and why.

Be thorough and methodical. This is a long run; keep going until the lock writes
or you hit a real blocker. If you hit a blocker you cannot resolve, stop and
report it precisely (do not invent a workaround that skips a required check).
