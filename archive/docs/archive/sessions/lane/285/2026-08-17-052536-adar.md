---
record: session/1
lane: lane/285
agent: adar
started: 2026-08-17T05:25:36Z
---

# Session — lane/285 — 2026-08-17

## Did
Landed #285 as **ADR-0033**. ADR-0026 decided that three fields change from a name to content and ADR-0027 shipped the record shape; nothing wrote the content, and ten of the 27 declared fields had no writer anywhere in the repository. This wrote four of them.

THE NUMBER THAT MADE THE PROMPT HALF URGENT. Measured on `bench-py`'s 257 contracts: `bundle_sha256` — the field on disk — is **one value across `stock`, `norule`, `noscaffold` and `planonly`**, because every one of those levers edits the USER message and it hashes the system half. `prompt_sha256` separates all four. So the resume refusal and the D6 guard that read it could not tell a mislabelled cell from a correct one, which is how eight directories were mislabelled before a reader noticed. The digest is over both halves of every task's render, keyed by task id — not the first task's, which would be the curated-subset defect at a smaller scale. 257 contracts render in 0.1s, once per run, against hours of dispatch.

THE BAR IS ASKED OF THE CHECKERS, NOT DERIVED FROM THE CONFIG. `gate_rungs` is five names and both arms write the same five. `bar_sha256` hashes what the checkers RESOLVE to — `ruff check --show-settings`' `linter.rules.enabled` (250 rules for the staged `bench-py` bar on ruff 0.16.1) and `eslint --print-config` for the file the arm writes — plus every resolving tool's version, because ADR-0025 makes the checker version part of the instrument. Expanding a select into concrete rules is ruff's resolution, it moves between releases, and a second implementation here would drift from the one that scores. Only `linter.rules.enabled` is taken: the rest of that output carries `linter.project_root`, an absolute path, and a digest that moves with the checkout describes the machine. PER LANGUAGE — one digest over both arms would restate `gate_rungs`' defect with more hex. And the workspace is staged by the CALLER, because the bench's bar is not `make lint`'s: it is whatever `score.stage_dir` puts in a workspace, and resolving the repository's own settings would digest a bar no candidate is scored against.

THE MODEL DIGEST IS THE OVER-SENSITIVE ONE, AND SAYS SO. `model_sha256` is the manifest digest `/api/tags` already returns and `detect.py` throws away — sha256 of a five-layer manifest, so it moves when the template, system or licence layer changes and the weights do not. Over-sensitive is the SAFE direction for a comparability guard: it refuses a contrast that would have been sound and never permits one that is not. The unsafe direction cannot be closed from a dispatch — `tensors` carries shape and dtype, not weight values, so a fine-tune has identical shapes, and #189 was a fine-tune contrast. `vocabulary_sha256` and `merges_sha256` are the model's own GGUF content where the manifest digest is ollama's addressing of it, so a disagreement on one with agreement on the others is a re-tag rather than a different tokenizer. `template_sha256` sits in the SERVER group: `template` is ollama's rendering on top of the GGUF, and the same weights under two templates are two instruments. `verbose: true` is encoded in the probe rather than in a comment — without it the tokenizer arrays come back `null` rather than absent, 0 against 151,936, and a probe that omitted it would record "unobtainable" one flag away from the answer.

TWO CORRECTIONS TO THE ISSUE, both places where it compressed a decision into something wrong to build. **`prompt_sha256` does not enter `KEY`** — the acceptance box says it should, D6 says "keyed WITHIN a condition", and the difference is the bench: the ablation changes the render on purpose, so `stock` and `norule` differ by construction and a global key entry would refuse every contrast the bench exists to draw. The mechanism D6 asks for is `require_comparable`'s per-condition loop and it was already written; only the writer was missing. **There is no fresh run on the second runner** — `tools/bundle/measure.py`'s `record_run` calls `refuse_to_measure` as its first statement and both its arms were retired by #240, so it refuses every call. Wiring digests there would be unreachable code justified by a checkbox, and ADR-0020 retired those sets permanently rather than temporarily.

A DEFECT, AND THE SECOND OF ITS SHAPE IN TWO DAYS. `unfingerprinted` and `drift` took the key as a DEFAULT ARGUMENT — bound when the function is defined — so `tag()` read the key frozen at import and ADR-0027 D8's stated property, that a `verified` record demotes on its own when the key widens, could not be exercised without reimporting the module. It held in practice because admitting a field means editing the literal, and it was untestable and one refactor from false. Both resolve at call time now, and the demotion is proven by a test that actually widens the key. The first instance was `product._open_cli` writing a round entry whose digest and file map described two different trees (#291).

WHAT `null` NOW CARRIES. D2 says an unobtainable field is `null` WITH A REASON, and the reason cannot live in the field without being the sentinel string D2 forbids — so it lives in one sibling block, `identity_refusals`, keyed by field name. `GROUPS` stays the 27 names it declares rather than gaining ten `*_reason` twins. On resume, a digest ABSENT from an older directory is adopted forward (`serving_build`'s argument: refusing on a key the directory could not have carried is spurious), and a digest that was `null` and is now answered is DRIFT and refuses — that directory holds rows measured under weights nobody recorded, and appending rows measured under weights somebody did would let the manifest describe half the denominator.

`PENDING_REASON` is complete and held to the tree by test: every field pending on #276's rule has a writer, every field pending on #286 has none, and a name added to `GROUPS` cannot arrive unexplained.

The model group is written against the API shape #276's survey verified and has NOT been exercised against a live endpoint — there is no ollama reachable from here. The refusal paths are tested; the success path is tested against stubs carrying the survey's own key names. First rig contact confirms or corrects it. 24 new tests; ruff, ruff format, mypy strict and the full suite green.

## Left open
next: open the #285 PR; then #287 (the two runners' resume drift checks, now that identity has the fields) and #286 (the observed probe set) — the rest of the pre-r2 batch
