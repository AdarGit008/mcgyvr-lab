---
record: session/1
lane: lane/167
agent: adar
started: 2026-08-07T01:30:00Z
---

# Session — lane/167 — 2026-08-07

## Did
#167 answered, and the answer is neither of the two readings it was opened to
separate. CLM-0004's Python task set was still in AdarGit008/local-ai and is now
vendored (records/evidence/local-ai-2026-08-02/instrument/) pinned to 6d8c11d6 —
the commit the 2026-07-28 run was made at — with the drift check that makes it
the original rather than a later version: HEAD differs only by ruff quote
normalisation and one docstring, and both versions serialise TASKS identically.

Three arms on qwen2.5-coder:3b Q4_K_M at srv1, 180 dispatches, 0 lost.
Serving stack ruled out: the vendored harness re-run byte-unchanged against
Ollama reproduces the effect (35/50/55/65%) and CLM-0004's never-passing set
exactly. Language ruled out: the same twenty tasks ported to mcgyvr contracts
through the rig are flat, +1 task at every rung, p=1.00. The variable is the
harness — render_user_message already ends every message demanding the whole
file as one fenced block, which is the device the gain runs through. Positive
control, on the unported contracts under the unported harness: that one sentence
alone takes c0 from 7/20 at 427.4 completion tokens to 11/20 at 121.5, matching
the entire 1972-byte bundle and beating it on tokens.

CLM-0017 recorded; CLM-0012's scoping sentence discharged in place and its
attribution of the flat token curve to the task set superseded. prompts/python.md
moved to a new BundleStanding.MEASURED_REDUNDANT and gained the provenance marker
it never had — neither existing value could say "real effect, already supplied by
the harness". measure.py takes --language; JS/TS stays the default and is
untouched. The sweep's 105 replies were pinned by tools/replies/pin.py, which is
#184's mechanism firing on its first real sweep.

## Left open
next: PR open. If merged, #167 closes.

Not done here and worth an issue: if the bundle's whole measured value is one
sentence mcgyvr already sends, both shipped bundles are ~1.9 KB of prompt per
request for no measured return — #168 already owns the JS/TS half of that
question and now has an answer to inherit. Also unresolved: the residual effect
once output discipline is present is +1 task at p=1.00, consistent with zero and
with one to two tasks; n=20 with 13 condition-insensitive tasks cannot resolve
it, and no arm was re-rolled or replicated on srv2.

Flake seen, unrelated to this lane and left alone:
tests/test_capacity.py::test_the_capacitys_own_record_agrees_with_what_was_observed
failed once under full-suite load and passed on every isolated run. It asserts
usage.peak == 3 and waited_seconds > 0 on a six-jobs-through-three-slots race
(#185's Capacity).
