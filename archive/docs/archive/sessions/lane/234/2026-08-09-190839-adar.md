---
record: session/1
lane: 234
agent: adar
started: 2026-08-09
---

## Did

Rewrote **45 open issue bodies** so each states the current plan without its
comment thread, wrote the convention that was missing into ADR-0001, and
corrected three factual errors the rewrite found in the doctrine it was
transcribing.

**The sweep is defined against the live graph, not the comment list.** #234
scoped itself to "issues carrying an amendment comment" — 21 of them. Reading all
73 open bodies against ADR-0017, ADR-0018, the audit and the dependency graph
found 45 stale and 28 clean. The 24 additions are stale for reasons a comment
would never have recorded: #227 asserting it blocks #225/#224 when the graph has
`#113 <- 217,227`; five epics (#3, #13, #19, #40, #45) listing closed children as
outstanding work — #45 listed nine; #132's sole deliverable handing off to #123
and #110, both closed. Owner picked that scope over the literal 21 and over a
wider reading that would have folded pre-restructure comments in too.

**Three inherited errors, found by transcribing the doctrine into the bodies.**
All three were in ADR-0017's own amendment, and two had been copied onward into
ADR-0018 and the audit:

- **The Python floor figure was a different arm's.** `tools/bundle/python/tasks/`
  was recorded at **35/50/55/65%** on the 3B. That is CLM-0017's **arm B** —
  CLM-0004's instrument run unedited on its own *unported* contracts, whose tasks
  live in the vendored `context_tasks.py`. This repository's task set is **arm A**:
  `run.json` carries `"language": "python"`, which `tools/bundle/measure.py:223`
  binds to that root, and it reads **65/70/70/70%**.
- **"Gains *and* regressions visible" holds for the JS/TS arm only.** It moves
  (45/55/50/45). Arm A is flat — 13/20 then 14/20 three times, a null at p = 1.00.
  The Python root is in band by *level*, not shown to be in band by *response*,
  which is narrower than the amendment claimed and changes what #225 widens.
- **"12 tasks per rung" understates `d1`**, which is 20; `d2` and `d3` are 12.

Corrected in ADR-0017, ADR-0018 and the audit rather than only in the bodies —
otherwise 45 issues cite three documents that say the opposite, which is the
defect this issue was filed to end. P1–P3 and Q1–Q4 are untouched. A fourth,
smaller one went the same way: the test-witness sweep's "four tests opt out when
a tool is absent" counts table rows; `test_bundle_ladder.py:544` is the marker's
definition and it is applied to five, so the figure is nine.

**The convention, as an ADR-0001 amendment.** *An amendment that changes what an
issue **is** edits the body; the comment records why it changed.* Boundary 10
already makes the issue tree the scope of record and every issue footer already
points there, so that is where the next author meets it. It carries the corollary
the sweep forced — **dependency prose in a body is a claim about the graph, and is
checked against it** — and the mechanical guard below.

**`tools/issues/body.py`.** `gh issue edit --body-file` blanks a body and exits 0
on an empty file; it has cost a body twice. #235 deliberately kept this out of
`wire.py` and left it here. Same three properties: refuse the empty file before
`gh` is called, save the prior text to `<file>.prev`, and read the live body back
to prove the edit landed. Line endings are normalised on that comparison and
nowhere else — GitHub stores CRLF, so a byte-exact check would fail every edit and
teach the reader to ignore the guard.

**Provenance for the 26 bodies that had none.** 18 of the 45 already carry a
2026-08-09 amendment comment; the other 26 changed with nothing recording why.
Under the convention just written that is half a record, so each got a short
comment quoting the sentence removed and giving what is true. Every amendment
comment was retained — comment counts on all 18 are unchanged.

**Also:** #190 retitled — *(sequenced behind the pilot and #184)* named two closed
issues; the gate is #221. It was the only stale title in the tree.

## Verified

- `body.py verify` over all 45, read back from GitHub after the writes: 45/45.
- The empty-file refusal exercised live, and a no-op re-run reporting `unchanged`.
- Every code citation the rewrite introduced was opened and read; the coherence
  pass rejected the ones that did not hold. It caught a claim consequence
  CLM-0013 had explicitly withdrawn being quoted as live.
- `make check` green: 1155 passed, ruff and mypy clean.

## Left open

`tools/deps/wire.py show` lists a blocker whatever its state, so once this issue
closes it will report 48 issues as blocked by a closed #234. The lifecycle
handles the block itself; the display does not say so. Same family as the defects
#235 was built for, and small — but it is #235's tool and nobody asked for it.

next: the trunk head is #229 (the bar), unblocked; #159 is the other structurally unblocked issue
