---
record: session/1
lane: lane/189
agent: adar
started: 2026-08-07T01:30:00Z
---

# Session — lane/189 — 2026-08-07

## Did
Ran the whole pilot, claim to verdict, in one session. Built the training set
from the corpus (`tools/finetune/build_dataset.py`: verified passes only,
prompts rebuilt through the shipped assembly and held against each run's
pinned `bundle_sha256`, dedup + per-task cap → 658/80 over 20 tasks — and the
census that surfaced the corpus is 20 problems, 100% TypeScript, filed as
#197). Trained on rig_b (`tools/finetune/train_qlora.py`, 32 min), exported
merged-16bit → f16 GGUF → q4_K_M via llama.cpp, and measured the full
{base,tuned}×{q4,f16} matrix through the front door: EvalPlus 0.3.1 greedy
against the ollama-bundled llama-server on the rigs.

**Verdict: miss.** Tuned-q4 is +1.9pp HumanEval+ over base-q4 against the
pre-registered +3pp bar; the tune does not enter the table. The matrix and
raw outputs are `records/measurements/finetune-pilot-2026-08-07/` — including
the accidental CPU-backend first pass, kept because it shows the same
artifacts' delta at −0.7pp: backend numerics alone move a greedy delta by
more than half the admission threshold.

Operational finds along the way: the ollama-bundled llama-server needs
`GGML_BACKEND_PATH` pointed at its cuda `.so` or it silently runs CPU with
`-ngl` ignored; the old ollama-backend HumanEval+ number for this model
(80.5%) does not reproduce through llama-server on the same weights (78.0%).

## Left open
next: decide what consumes the backend-sensitivity finding — a caveat on
MIN_QUALITY_GAIN where the table admits models (#162's routing-matrix design
is the natural home), and whether #190's distillation waits for #197's
problem-diverse corpus, which this pilot's null-ish result argues it should.
