---
record: session/1
lane: lane/216
agent: adar
started: 2026-08-08T04:30:00Z
---

# Session — lane/216 — 2026-08-08

## Did

Made the output cap a parameter, then spent it: the 269-problem sweep re-run at
2048 with nothing truncated, which is the uncensored distribution #216 needs and
which no re-reading of a capped run can produce.

**The rig change.** `--max-output-tokens`, one value threaded through both the
dispatch path and `record_run`, defaulting to the inherited 768 so every
existing run directory keeps its identity. Two invariants, both
mutation-checked: the cap a run *records* is the cap the worker was *sent*, and
a cap change refuses a resume the way a temperature change does. The first
matters because a manifest that misreports its own cap is #212's failure one
level down — that lane spent itself on refusal rates that turned out to
describe the instrument.

**The measurement.** 807 rows, 0 truncated, 0 refused, 0 lost. Max reply 1370
tokens against a 2048 cap. Only the cap differs from
`pool-sweep-14b-2026-08-07` — bundle, tier, temperatures, draws and all 269
task digests identical, checked rather than assumed.

**What #216 asked for:**

| task type | n | median | p95 | p99 | max |
|---|---:|---:|---:|---:|---:|
| `function_implementation` | 564 | 418 | 803 | **1151** | 1370 |
| `bug_fix` | 243 | 345 | 739 | **805** | 933 |

The per-type branch is better justified than the issue argued. #216 reasoned
from *available inputs* — `target_content` exists for `bug_fix` and is
identically absent for `function_implementation` — and that holds. But the
tails also differ in **shape**: the largest `bug_fix` reply ever seen (933) is
below `function_implementation`'s p99, so one flat cap over-provisions the
easier type by ~43%. The old 768 sat below `function_implementation`'s p95,
which is exactly the 6.4% overrun measured.

#216's correlations, refit on uncensored data, barely moved (+0.688 → +0.714
and so on). Its premise was sound; what the run adds is the **absolute**
percentiles.

## Two results that matter more than the formula inputs

**The cap was costing half a point, not the difficulty gap.** Of the 47 cells
truncated under 768, **4 now pass and 43 still fail**. The truncated replies
were wrong answers that also ran long. #212 inferred this by discarding
truncated rows and said the gap moved 18.9pp → 17.5pp; this measures the same
thing directly and agrees.

**The churn is larger than the effect, and that is the durable finding.** Net
+11 passes decomposes into 49 newly passing and 38 newly failing, only 4 of
them previously truncated. Greedy — the arm that is supposed to be
reproducible — is byte-identical on only **227 of 269** replies across the two
runs; 14 of the 42 differences are truncated replies legitimately continuing,
leaving **~28 cells, 11% of the untruncated greedy ones, differing at
temperature 0 on identical weights, prompt and host.**

**Differing text is not a differing verdict, and I first wrote this up as if it
were.** Collapsed to one outcome per problem, with cap-explicable flips removed,
the same two runs give greedy discordance of **1 problem in 269** (+0.4% drift)
against **24 in 269** (+3.7%) for the any-of-3 measure. So 11% of greedy
*replies* differ and only ~1% of greedy *verdicts* do — most divergence lands on
a problem that was going to fail either way. Greedy is much quieter than the
byte-identity rate suggests. Ranked: greedy re-run ±0.7pp, sampled ±3.6pp,
backend change 2.6pp. The record carries the corrected table; the first draft's
"~2pp for greedy" was the conflated figure.

**#212's verdict survives the cap's removal.** The 80 stay about half as
passable as the first 189 (ratio 0.50 → 0.56), now with **zero refusals on both
sides**, so nothing in that comparison is a property of the instrument any more.

## The run took an outage, and that exposed a defect

srv2 became unreachable for **51 consecutive tasks** (`p164-truncated-product`
… `p214-debounce-levels`) — 152 draws, all identical 120s transport timeouts —
then answered `/api/tags` in 0.14s. The rig recorded each as a row carrying
`dispatch_error` rather than raising, which is the documented choice, and the
summary said `152 draws lost`.

But `done_keys` counts **any** row as a filled cell, so re-running the identical
command would have printed `resuming: 807 draws already recorded` and
dispatched nothing. The hole would have been permanent in a directory whose
`run.json` claims 269 tasks — exit 0, expected line count, and only a scrolled
summary line to tell anyone. Filed as **#217**, with the design fork stated
(prune-on-resume vs last-row-wins vs an explicit flag) rather than picked here.

Recovered by hand: no candidate files existed for failed draws, so the 152 rows
moved to `dispatch-errors-invocation-1.jsonl` and `results.jsonl` was rewritten
with the 655 clean ones; the resume then correctly reported `resuming: 655` and
refilled the gap. Both invocations are in `run.json`.

## Also carried here

`e2af86e9` rescues the cap-provenance correction that **missed PR #215's
squash by six minutes** (committed 04:20:42Z, merged 04:14:39Z). #212 is closed,
so lane/212 is spent under DIV-01 and could not carry it. It belongs here
anyway — the provenance of 768 is what this lane is about. It is a deliberate
REC-01 mutation on a landed record, precedent `67c92db` (#118's citation
repair, one of the cases #171 argues the rule should not flag alike). Only the
measurement README was carried; lane/212's session record is untouched.

## Left open

- **A formula is not fitted.** This lane produced the distribution #216 needs
  and stopped there. Fitting coefficients, choosing the margin, and deciding
  which failure the margin optimises for (#216 items 2–4) are the next lane's.
- **These percentiles are one worker.** `qwen2.5-coder:14b`, TypeScript, three
  draws. #216's own out-of-scope line says the cap must not need to know which
  worker will answer — so whether 1151/805 transfer to the 7B or to Python is
  **unmeasured**, and a formula fitted only on this run would be fitting one
  model. That is the gap to close before item 2 is credible.
- **The rig still ignores `contract.limits`** (#216 item 4). `--max-output-tokens`
  makes the cap choosable, not contract-derived; nothing here changes which
  number a sweep reaches for by default.
- The corpus is 12,177 replies after re-pinning, still 398 refusals — this run
  added 807 replies and not one refusal.

- **The noise floor turned into its own issue.** #219 filed: #189's +3pp bar was
  tested on HumanEval+ at n=164, whose paired minimum detectable effect is
  ~+4.8pp, so its +1.9pp cannot separate "no effect" from "an effect that would
  have passed". #219 carries the run plan (null calibration, paired interleaved
  comparison on held-out pool problems in both languages, backend control) and
  pre-registers IN / OUT / **UNDECIDED** — the third box #189 lacked, which is
  why an unresolvable result was filed as a negative one and #190 inherited it
  as settled.

next: PR review for #216's measurement half. **#217** (dispatch errors block
their own retry) is unclaimed and independent. **#219** is the fine-tune
question, taken up in a fresh session. Fitting #216's formula wants the
cross-worker run named above first.
