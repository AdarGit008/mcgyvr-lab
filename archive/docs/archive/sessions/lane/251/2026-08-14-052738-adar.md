---
record: session/1
lane: lane/251
agent: adar
started: 2026-08-14T05:27:38Z
---

# Session — lane/251 — 2026-08-14

## Did
Owner pushed back on #256's framing: 'language separation is not the true difference maker'. They were right, and checking it changed the shape of the work.

Recomputed CLM-0011's 2,387 units across BOTH axes instead of just language. Spread across models is 15.0pp; across languages 7.0pp (worst model) and 9.0pp (best). The model axis is roughly twice the language axis. ESTIMATE_RESERVE = 0.32 is deepseek-coder-v2's pooled figure — and that model IS in data/capability-table.json, so it is a legitimate worst case, not a phantom — but it charges a qwen2.5-coder worker 32% where it needs 23% on JS and 15% on Python. That is bigger than the language gap #256 was filed for, and in the same direction.

This is the audit's own lens 3 turned on itself: 'pooled' was the right diagnosis, 'language' was a label standing in for the property. Recorded as a correction in the audit doc rather than quietly rewritten.

Also checked, and it is a real gap: of ADR-0026's three mandated digests, only the BAR had an owner (#262, filed by me hours earlier). The MODEL fingerprint is named in exactly one open issue — #251, which is scoped out of fixing anything. The CONDITION digest has no owner either; #231's check 2 needs it and cannot tell its own control from its baseline today because bundle_sha256 hashes only the system prompt.

Filed #265 to decide run identity ONCE rather than three lanes negotiating it in three PRs. The concrete reason, not tidiness: report.COMPARABLE and report.BOUND_MATCH are single tuples, so three lanes each editing them is three chances to name four fields and permit the fifth silently — verbatim the defect ADR-0026 warns about. #265 also carries the question that most wants settling once: what happens to the 123 existing manifests, which cannot acquire a model digest retroactively.

#256 re-scoped behind #265, original framing retained under 'The original framing, retained for the record' rather than deleted. #262 and #231 each told which half of the contract they own and what is unaffected.

Method note for the next reader: the model-vs-language comparison took one script over data already vendored. Nobody had run it because no tool cuts by model, which is lens 4 — what is cheap to vary decides what is found.

## Left open
next: #265 is the decision that unblocks #256 and shapes #262 — it needs an owner before either of those can be answered
