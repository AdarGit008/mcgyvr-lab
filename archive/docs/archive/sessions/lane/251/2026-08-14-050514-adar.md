---
record: session/1
lane: lane/251
agent: adar
started: 2026-08-14T05:05:14Z
---

# Session — lane/251 — 2026-08-14

## Did
Second pass on #251, at the owner's direction: file every homeless finding, review ALL open issues for drift, fix what is wrong.

FILED. Four new issues where nothing owned the ground: #261 (a crashed linter scores as a clean pass — in BOTH adapters, and the JS comment claiming parity with Python is true, which is the finding), #262 (the two bench arms declare one bar and apply four different ones: 328 vs 66 rules, prettier unconfigured, no tsc, and the 120-vs-30 timeout), #263 (statistics, at three explicitly different confidence levels), #264 (dead exported surface incl. the sandbox fallback). Six comments where an issue already owned it: #231, #118, #225, #66, #16, #207.

Deliberate on #263: only the ablation pooling is stated as confirmed — verified by execution, counts() tallies 8 draws per task mixing 1 at T=0.0 with 7 at T=0.7. The 'flips the sign of a published contrast' half is NOT reproduced and the issue says so. The redundancy interval is carried forward unverified. The MDE disagreement narrowed usefully: both tools import the SAME detectable_delta, so it is a psi question, not a method question.

DRIFT, and it had propagated. tools/problems/admit.py:104 was copied into five issues and lands on a blank line; the fact is now existing_task_roots() at 126, and #222 still named EXISTING_TASK_ROOTS which does not exist at all. Same shape: measure.py:202 (3 issues), pin.py:54 (2), test_bundle_ladder.py:544 plus all five application sites, and #17's three MAX_OUTPUT_TOKENS citations off by ~48 lines. Every quoted STRING was verbatim-correct — only the numbers rotted, which is why nobody noticed. Eight bodies fixed through tools/issues/body.py and verified by read-back. All 115 line citations now resolve.

Dependency drift left as COMMENTS not rewrites, following this tree's amendment convention: #225 still claims to block #113 (closed 2026-08-13, landed without it — so the harness was commissioned on twelve tasks a rung, which is the condition #224 says it cannot satisfy); #243's exit pass is now unblocked.

False positives worth not re-chasing: #71's server-common.cpp and ollama#5348/#7900 are upstream; #118's paths are correctly described as 'pinned but not copied'; #146's test_pager.py is a hypothetical; #152's CLM-00xx is a placeholder; #222/#224 were my own regex reading across a line.

The good news, which matters as much: the claim figures are RIGHT. CLM-0012's 45/55/50/45, CLM-0017's arm A 65/70/70/70 against arm B 35/50/55/65, and the ~+20pp output-shape effect all check out against the claim records, and #234's arm-A/arm-B correction propagated consistently across every issue quoting it. #231 already carries a dated amendment correcting the one real misquote. The register is in better shape than the code it describes.

## Left open
next: decide #256 (per-language reserve or one conservative 0.36) and #261 (what the gate does with an inconclusive rung) — both are shipped-behaviour decisions the audit could find but not make
