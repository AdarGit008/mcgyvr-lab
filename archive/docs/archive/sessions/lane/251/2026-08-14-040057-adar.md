---
record: session/1
lane: lane/251
agent: adar
started: 2026-08-14T04:00:57Z
---

# Session — lane/251 — 2026-08-14

## Did
Worked #251: the ADR-0026 four-lens sweep. Every inventory item confirmed, narrowed or refuted by execution — no silent drops — then swept for the classes and gave each a check with a positive control.

The headline is in src/, not the rigs: ESTIMATE_RESERVE = 0.32 is CLM-0011's worst-vocabulary p05 POOLED over language, in a claim whose own statement says the band is language-dependent. Recomputed from the vendored units: JS/TS -35.1%, Python -28.9%. The shipped constant under-reserves JavaScript by ~4pp of prompt budget on every prompt. Filed #256 rather than changed — splitting it needs the target's language at check_prompt_fits time, which the gate does not take, and one conservative 0.36 for both is the other defensible answer.

Two of the ADR's figures reproduce to the digit: 328 ruff rules against 66 eslint, and py format credited 23 while firing 155 (6.7x, both null-gate arms independently). Four narrow: GATE_RUNGS has TWO literal matches not one; three of CLM-0013..0016 do name a language (the no-language set is eight claims, a different set); the three adapter tuples are real but runner.py:91 is a timeout, the tuple is in gate/runner.py; the eslint severity-2 filter is inert today because all 66 rules are severity error. One refutes as stated: tsc is not in the declared bar and ADR-0006 makes its absence designed — but the TS arm is scored with no type checking and no manifest says so.

The class sweep is what the issue was for. A sameness-comment sweep gives 426 candidates (84 naming a target) and is unenforceable — most is honest architectural prose. The enforceable property underneath is one constant, two definitions: 17 duplicated module-level constants, 4 already disagreeing. The inventory named two of them.

Dead end worth recording: I first concluded the full finding lists were unrecoverable because results.jsonl has no findings key. Wrong — measure.py:616 joins them into fail_output on every failing row, which is how the 23-vs-155 recomputation was possible without a re-run. Lens 1 was working there.

Also could not read: there is NO norule run committed. ADR-0026's central evidence row and the stock-to-norule effect sizes rest on runs that are not in the repository. The bundle_sha256 half is confirmed in code (it hashes the system prompt only).

Writing the checks found what reading did not: target_symbol (a recorded field nobody listed, correctly acquitted as construction input), seven more duplications including PROBE_TIMEOUT_S at 1.0 vs 2.0, and require_rungs promising coverage of every declared rung while its canary covers lint and format — neither of which is a declared rung. Filed #258.

Filed #256 #257 #258 #259; commented on #207 rather than closing it. mypy strict, ruff and the full suite are green; two checks verified by mutating the real tree and restoring it.

## Left open
next: get PR #260 reviewed and merged, then decide #256 — a per-language reserve or one conservative 0.36 — because it is the only shipped number this audit moved
