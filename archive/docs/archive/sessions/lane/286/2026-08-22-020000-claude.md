---
record: session/10
lane: lane/286
agent: claude
started: 2026-08-21T19:15:00Z
ended: 2026-08-21T20:10:00Z
---

# Session — lane/286 — 2026-08-21 (tenth)

**#326, identity, is done (`e3172cff`).** Every row names the card, the
driver, the compute capability, the engine and its build; vLLM rows name
their weights and serving-config pins; a refused load keeps its trail.

## What changed

| where | what |
|---|---|
| `contract.py` | `hardware(host)` (one `nvidia-smi` line, null + command on failure), `HARDWARE_UNANSWERABLE` (bandwidth, with the reason), `RefusedError.attempts` |
| `backends/ollama.py` | `build(host)`; per-attempt `seconds`; the refusal carries the trail |
| `backends/vllm.py` | `build(host)`; a digest past `DIGEST_TIMEOUT_S` is an `error` naming it |
| `calibrate.py` | `identify`/`IDENTITY` merged at `emit`; `_expected_weights` from `srv-full.json`, passed at both `vllm.claim` sites; `_serving_pins` on launch/ramp/sleep rows; `model_sha256` on ollama ramp rows; `LAUNCH_CHECKS_DISPOSITION`, `LAUNCH_WEIGHTS_DISPOSITION`; load row `attempts`/`attempt_outcomes`/`rescued_by_retry`/`attempt_seconds`; `engine: ollama` on the ollama fast/load rows |
| `run.py` | the refusal block carries `attempts` whole |
| tests | 10 by the DoD's names; `_ollama_rig` per-attempt `children`; the `claimed`/`ollama_attempt` fixtures restore the shared contract module (they had been leaving `ssh`/`first_int` patched, which surfaced once `emit` started reading the host) |
| README D6 table | two appended lines |

## Shown to reject

14/14 mutations. Gate 1836 → 1846. No rig time.

## Left open

- #327 ambient, then #328, #329; #330 alongside. ADR-0037 still Proposed;
  #318 must not carry `Closes #286`.
- The identity block's hardware group is not in `identity.GROUPS` — an
  ADR-0027 amendment, out of scope here.
- Both rigs have never answered `contract.hardware`; the first run after this
  lane either fills the block or records the command it failed on.

next: #327 — ambient (the ramp reads only memory.used; no temperature, clock, power, load; order fixed ascending)
