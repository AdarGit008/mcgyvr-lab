---
lane: 286
date: 2026-08-25
session: 32
---

# Session — lane/286 — 2026-08-25 (thirty-second)

**#358 is ruled: admitted. The KEY widening now has the record the DEC-9
precedent gave the last one — an owner comment on the issue, a dated
amendment block in ADR-0027, and a DEC line in the plan — and P1.2's box is
ticked on the lane that will merge it.** No rig time.

## What the owner was shown first

Two checks over the whole lane, run as multi-agent verification with every
non-PASS finding attacked by two refuters (61 + 45 agents):

1. **#286's seven acceptance boxes** all hold at `1bb3ebed`. Box 5 holds only
   as restated on 2026-08-25 (the original wording fails: `KEY` gained
   `serving_resolved_sha256` under #358). Box 7's figure in the issue body —
   "1994 passed, 7 xfailed, 1 skipped" — does not reproduce: the tree gives
   **1999 passed, 13 xfailed, 0 skipped**, and no session record carries the
   1994 figure. Box 1's "breadth on a fresh run" has no committed artefact:
   `git ls-files | grep -c observed.json` is 0.
2. **The merge inventory**: 52 code files, 12 evidence directories, 39
   issues. Nine evidence directories reproduce their headline numbers from
   raw files; three are README-only (serving-surface's ramp tables,
   coresidency-feasibility, sole-client-counters). Every 2026-08-19
   throughput column is voided by #356; every constant was re-derived on
   2026-08-24 and none moved. The config-sweep README's 518.2 / 5.02x is
   read against `perf-interactivity`, not `baseline` (530.1 / 4.91x in the
   file). The engine-sweep's "serving default for r2" feeds no code and is
   the owner's, still open.

Both reports are in the owner's hands; their content is what the next lane
records should cite when a figure above is quoted.

## The ruling, and what landed with it

- **#358 — admitted.** Owner comment on the issue, 2026-08-25.
- `docs/decisions/0027-…md`: `## Amendment — 2026-08-25 (#358, DEC-14)`,
  mirroring the DEC-9 block — what the field is, why it enters by
  qualification rather than by #276's perturbation (the resolved-config pair
  refuted `serving_build`'s own justification), and what it costs (only the
  serving harness writes it; breadth/bundle rows refuse until a writer
  exists; `allow_unfingerprinted=True` is D3's waiver).
- `docs/plans/302/README.md`: DEC-14 in §3; P1.2 ticked with the DEC-14
  addendum, the shape P1.1 has for DEC-9.

## What blocks the PR

Under the owner's 2026-08-21 rule, #286 closes when its GitHub sub-issue
tree is complete: **7 of 9**, with #322 and #329 open. #329 is done on the
branch (6/6, xfail off at `06050027`) and waits on the owner's close. #322's
part 1 — the run-intent gate as code — is not on the lane; its body leaves
"whether part 1 also blocks #286's close" for the owner to append. The PR
body cannot carry `Closes #286` until that line is written.

Not filed, and owed: a `serving_resolved_sha256` writer for the scored
runners (breadth/bundle), so a fresh bench row is not unfingerprinted by
construction.

## Gates

`make check` green at commit time: lint, typecheck, and 1999 passed, 13 xfailed (352 s).

## Ruling, later the same session

**#322 — deferred.** Part 1 does not block #286's close (owner, 2026-08-25;
appended to the issue body). #322 is removed from #286's GitHub sub-issue
tree; #330 stays. The tree is now #329 short of complete.

## Left open

The #329 close and the writer issue above.

next: owner closes #329; then push and open the real PR lane/286 → main with Closes #286, #358, #329
