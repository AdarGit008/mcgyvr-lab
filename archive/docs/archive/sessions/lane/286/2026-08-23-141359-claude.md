---
lane: 286
date: 2026-08-23
session: 24
---

# Session — lane/286 — 2026-08-23 (twenty-fourth)

**#352 is closed the way the owner chose — the promise narrowed, not the check
widened — and the first live reading of the fix corrected the issue on three
counts and produced a new one.** One commit, one issue closed, one issue filed.

## The ruling

The run contract's second post-state clause read **"no container of ours"** and
the reading beneath it was `docker ps`, which lists **running** containers only.
Two ways out were put to the owner: widen the check to `docker ps -a`, or narrow
the clause to "running" with its reason stated. **The owner chose to narrow.**

The reason, written into `docs/run-contract-2026-08-22.md` §4 as a dated
correction: a stopped container holds nothing this rule protects — no card, no
port, no process, and no claim on the next cell, which removes it before it
launches. Requiring its absence would make the contract's cheapest guarantee
turn on a state that costs nothing to leave behind.

**The original sentence is left byte-identical.** A pointer sits beside it and
the correction carries the change, which is ADR-0039's form. The one departure
from that form is the pointer itself: an ADR's superseded figure is harmless
because the *code* is operative, whereas §4's four clauses in check order are a
list an operator acts on directly, so a reader who stops at the list must not be
left acting on the superseded one.

## What landed

`50e69c3e`, then the corrections below.

- **The record takes `-a`** (`vllm.py:475`), so an exited container appears with
  its status and the `docker inspect` that follows recovers the argv it died on.
- **The gate does not** (`vllm.py:570`). `released` decides whether anything of
  ours still holds the card, and a stopped container holds none of it. **The
  record sees more than the gate acts on, and that asymmetry is the answer
  rather than a leftover.**
- **The other two `docker ps` reads keep their running-only form, and one of
  them matters.** `launched_width` reads `--max-num-seqs` off a container's own
  argv, and `docker ps -a` lists newest first — a sweep that added `-a`
  everywhere would answer with the width of the run that **failed**. That is a
  mutant in the sweep below, and it is killed.
- **A failed cell's container is removed at once, because its reason is read
  first.** `_launch_log` (`vllm.py:1109`) reads the engine's last 40 lines on
  the failure path and carries them, scrubbed, into the refusal. Keeping the
  container until somebody looks makes every launch depend on a human having
  been there, and a campaign runs unattended for hours.
- **The pip rig lost the reason the same way and nobody had noticed.** The
  refusal named two places to look; the container rig destroys its one with
  `docker rm -f`, and the pip rig redirects `> /tmp/vllm-serving.log`, which
  **truncates** the previous cell's rather than appending to it. Both launchers
  are read now.

Seven checks in `tests/test_serving.py`; seven mutants, one edit each, **all
seven killed**. `make check`: 1,945 passed, 14 xfailed. `make lint` clean.

## What the live reading changed

The fix was run against both rigs the moment it existed, which is the only
reason the next three paragraphs exist. Every one of them corrects something
this session had already written down.

**srv1 was in the same state.** It holds `vllm-nemotron-4b Exited (1) 13 days
ago`. #352 named srv2 alone, and srv2 was simply where somebody happened to
look. A defect attributed to one machine was on both.

**Four containers, one of them ours.** srv2's listing is `mcgyvr-vllm`,
`vllm-7b-coder`, `vllm-nemotron-30b`, `vllm-nemotron-4b`. `_start` gives its
container exactly one name, `mcgyvr-vllm`; the other three were built from the
same image by work that is not this project's. The filter is `ancestor=`, by
**image**, and the count derived from it is called `own_containers_remaining`.
That is #352's own shape one level down — a field's name claiming more than the
reading beneath it tests — and it is filed as **#355** rather than fixed here,
because deciding what "ours" means changes what `released` refuses on.

**The evidence this issue was filed on no longer exists.** srv2's `mcgyvr-vllm`
now reads `Exited (0) About an hour ago`: the refit campaign's last cell
replaced it, and its `docker inspect` gives back that cell's whole argument
list, `--kv-cache-memory-bytes 1610612736` included. **The `Exited (1)` #352
documented was destroyed by the mechanism #352 describes, one cell later.** The
case for capturing the log at failure time was made by the machine rather than
by us, on the very defect that was being closed.

Both rigs read `own_containers_remaining: 0`, `released: True`, card at 1 MiB
with four and one stopped containers respectively — the asymmetry, verified
live rather than only in a fixture.

## Kept

- **A fixture is only a check if the fake host discriminates.** The canary here
  puts the pre-fix question to the same host and shows the answer differs;
  asserting that `readings` contains the string `-a` would have confirmed the
  thermometer was installed and nothing more.
- **A claim about scrubbing was overstated and corrected before it shipped.** A
  first comment said the tail was scrubbed because "the argv is where a
  `-e HF_TOKEN=` lives". It is not: `contract.scrub` catches credential URLs,
  home-directory prefixes and published token shapes, and an arbitrary
  `KEY=value` an operator invented is redacted by nothing in this tree. The
  comment, the contract and the check all now say the weaker true thing.

## Handoff

Tree clean, nothing unpushed. Both rigs idle at 1 MiB, no resident models, no
container of ours running — and four stopped ones on srv2 and one on srv1,
which under the clause now in force is a state the record holds rather than a
state the contract forbids.

Tracker: #352 closed, #355 filed. #286's own seven DoD boxes were already
ticked; this session moves none of them, because #352 was found beside the
lane rather than inside it. The lane's remaining named work is unchanged and
all still open: #329's rig arm, #322's register, #335 and #336.

## Left open

- **#355**, filed this session and not started: `own_containers_remaining`
  counts containers of an *image*, not containers of ours. Both directions are
  wrong and one of them is a gate.
- **#353**, unstarted, and the reason it is next: `observed.py` already captures
  vLLM's whole `/metrics` at open and at close, so counter differencing would
  measure foreign traffic directly **if** one of the 122 series is a request
  counter. That is not asserted yet. ollama serves no `/metrics` at all — 404 on
  both rigs, measured 2026-08-23.
- **FLOW-03 was a live blocker on this lane and is now a check.** Session 23's
  record put its `next:` line after an appended `##` section, where
  `extractNext` cannot see it. `baseline` had been failing on the push-triggered
  workflow since `224dfd74` while passing on the PR-triggered one, which is a
  detached HEAD and skips every FLOW check. **Four CI rows are not one verdict**
  — two of them are not asking the FLOW questions at all.

next: #329 — the cross-rig claim's rig arm, unblocked and the rigs are free

## Session 24, continued — #353, the third term, measured on one engine

**#353 is answered, and the two engines answer differently.** The route the
issue ranked first works on vLLM and does not exist on ollama, and both halves
are measurements rather than inferences. Evidence:
`records/evidence/2026-08-23-sole-client-counters/README.md`.

**vLLM: `vllm:request_success_total`, summed over `finished_reason`.** Five
`/v1/completions` moved it by exactly five. Nothing else moved it at all —
`/health`, `/metrics`, `/ping` and `/v1/models` by zero each, three kinds of
failed request by zero each, and **two full `observed.capture()` passes by zero
each** while moving `http_requests_total` by exactly seven each. So **the
reading does not perturb the counter it reads**, and the arithmetic needs no
correction term for the harness's own traffic. That last control is the one
that makes the route usable rather than merely plausible.

A request that failed at the API layer is not counted, and that is **correct
for this question rather than a shortfall**: a request that never reached the
engine never entered a batch, so it cannot have changed the batch shape a
re-run has to reproduce. `http_requests_total` catches those and is kept beside
it as a constant for the refusal to cite.

**ollama: no counter, and the engine says so itself.** ollama answers 404 on
`/metrics`; its `llama-server` child answers **501** naming the flag that would
enable it — `Start it with --metrics` — and the child's own `/props` reports
`endpoint_metrics: false`. ollama spawns that child and does not pass the flag.
The refusal records that, which is a fact about reach, like `concurrency`'s was.

**`/slots` `id_task` was the other candidate and is refuted.** It is monotonic,
so it looked like a counter. Its increment per request is neither 1 nor stable
— **5.00, 7.00, 7.00, 5.67 measured on one server, one model, one request
shape**. llama.cpp assigns task ids to internal work too, and how much internal
work a request causes depends on cache state. It can say the server did
something; it cannot say how many requests that was, and a count is what the
subtraction needs.

**What is recorded, and what deliberately is not.** The field is
`server_completions_in_window` — named for what it holds, never for the verdict.
It carries the delta, both edges, the counter name and the window. **The
subtraction is not done here**: foreign traffic is that value minus the rows
this run dispatched, the dispatched-row count is a property of the runner, and
passing it would touch both runner call sites, which are SURFACE. It is in
`run.json` beside the file, so a reader has both terms and the note spells out
the arithmetic. Zero difference is measured sole-clientness.

Three states, not two — a number is *measured*, a refusal on an engine with no
counter is *looked in a way that cannot see*, a refusal at the open capture is
*not looked yet*. **No boolean anywhere in the field**, which a check asserts on
both engines in both captures.

Eight checks in `tests/test_bench_observed.py`; **eight mutants, all killed** —
including one that found a real gap: a window whose near edge was never read
resolved through no check at all, and now refuses rather than reading zero.

`make check`: 1,958 passed, 14 xfailed. Both rigs released back to 1 MiB after
the campaign, verified.

## Session 24, continued — #355, and the defect that was not the name

**#355 is closed, and the name was the smallest part of it.** The issue was
filed this session off #352's live reading; fixing it turned up two things the
issue did not say and one that had teeth.

**Both arms were lying, not one.** `--filter ancestor=` selects by image and
`pgrep -cf '[v]llm serve|…'` selects by process pattern. Each matches anything
of this engine on the host, ours or a stranger's, and **both were called
`own_`** — on both backends. That scope is right for an exclusion gate (E8's
finding, unchanged: anything of this engine that is up holds the card the next
entry would be measured on) and it is not ownership. Renamed to
`engine_processes_remaining` / `engine_containers_remaining`, with
`our_containers_remaining` beside them for the one container name `_start`
assigns. ollama's is renamed rather than narrowed and the reason is a fact
about that engine: it spawns the child, picks its port at load time and gives
it no name we set, so **nothing on the host distinguishes a child we caused
from one we did not.**

**The defect with teeth: release stopped strangers.** The image-filtered list
went to `xargs -r docker stop`. On srv2 that list is four containers, one of
them ours — so a release would have stopped three servers belonging to somebody
else. A cell never repairs a machine it found wrong, and killing another user's
server is further from repair than anything that clause was written about.
Release now stops exactly `mcgyvr-vllm`; what is up and not ours shuts the gate
instead, which is the right answer to finding a machine in a state we did not
create.

**E8's coincidence is live, load-bearing, and was about to be inverted.**
`--filter ancestor=<tag>` matches by resolved image **ID**. Measured on both
rigs: `:latest` and `:v0.26.0` are both `ffb2d59b1c05`, and the two filters
return byte-identical sets. E8 wrote that down on 2026-08-19 and pinned the tag
to fix a different failure. Pull a newer `:latest` and every container of it
goes invisible while `released` keeps reporting True — **E8's own failure,
arriving from the other direction, through the fix for it.** The readings match
the repository now, which does not depend on two ids agreeing.

**The limit, stated rather than left as a gap.** A vLLM from an image with
another name — a local build, a fork, a mirror — is not matched by a repository
string and would still hold the card. `card_used_mib` is where it shows, and it
is deliberately not part of `released`.

Six checks; **seven mutants, all killed**. Verified live on both rigs after the
change: `engine_containers_remaining: 0`, `our_containers_remaining: 0`,
`released: true`, card 1 MiB, with srv1's one and srv2's four stopped
containers in the record **and their image tags beside them** — which is
exactly what the pinned filter would have hidden.

`make check`: 1,965 passed, 14 xfailed. Two dated corrections now sit under §4
of the run contract; the second points back at the first, which filed this.
