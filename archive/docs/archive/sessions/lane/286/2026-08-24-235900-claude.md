---
lane: 286
date: 2026-08-24
session: 30
---

# Session — lane/286 — 2026-08-24 (thirtieth)

**#357's two rig boxes, in ~40 minutes of rig time: the declared column is
275 flags by digest, and all 26 lost reasons are now the engine's own
sentences — every one a refusal again, none a transient.** Session 28 built
the columns without a rig and left two things owed; this session is those
two things and nothing else. The owner's instruction: run to the commit
autonomously, close #357 on their approval — so the issue is open at the end
of this record.

## What the rigs were asked

Both idle at 1 MiB, no containers, before launch (srv1 had shown 1,114 MiB
with no container of ours an hour earlier; it had cleared).

1. `knobs.py declared srv2 records/evidence/2026-08-24-knob-surface` — one
   container start, no model. `declared-vllm-ffb2d59b1c05.json` + the raw
   text. **275 flags, 250 with a printed default, 31 choice sets.** The issue
   said 274; `--help` is the 275th. A device is required: session 28 measured
   `Failed to infer device type` on this workstation without one.
2. The 26 `refused_reason_lost` cells, same ids, same flags, `levels [1]`,
   `cap 1`, under the fixed `sweep.py` (`LOG_LINES = 2000`, `image_digest`
   on every launch). Matrices and records in
   `records/evidence/2026-08-24-knob-surface/reruns/`: srv1 1.5B (12), srv1
   7B (4), srv2 1.5B (10); both rigs concurrent, each the sole client of its
   own server. Both released to 1 MiB afterwards.

## What the re-runs said

All 26 refused again, and every one carries a reason:

- **srv1's four capability gates**, which the sweep README asserted and the
  table could not cite: bf16 (`compute capability of at least 8.0 ... has
  7.5`), fp8 KV in three spellings (`not supported by the Triton attention
  backend on ... compute capability 7.5`), `FLASH_ATTN` and `FLASHINFER`
  (`compute capability not supported`).
- **The 7B on srv1 is OOM loading weights** at 0.85 / 0.90 / 0.95, eager or
  not — `Tried to allocate 518.00 MiB ... 151.88 MiB is free`. The "hand
  re-run nobody recorded" is now a record.
- **Every `--max-num-seqs ≥ 320` refusal on either rig is
  `torch.OutOfMemoryError`**, with the shortfall in MiB. The ceiling the
  sweep found is a memory ceiling.
- **`--linear-backend exllama / machete / cutlass`**: `Failed to find a
  kernel that can implement the WNA16 linear layer` with the per-kernel
  reasons (the extractor now keeps the continuation lines of a reason that
  ends in a colon); **`torch`**: no such kernel for mixed-precision layers.
  Identical on both rigs — AWQ-kernel facts, not card facts.

## What changed in the tool

- `build <out> <evidence>...` reads several directories; a record's `_file`
  is `<dir>/<file>`. A later record with the same cell id is that cell's
  re-run: the earlier row keeps its recorded state and gains `rerun`, the
  later gains `rerun_of`, and each (rig, model) block reports
  `lost_reasons_outstanding` — **zero everywhere now**, pinned by
  `test_a_lost_reason_is_recorded_as_lost`, with a synthetic test for the
  two-way link.
- Untried is alias-aware: `--no-async-scheduling` is argparse's alias for
  `--async-scheduling`, so 21 tried assignments are 20 tried knobs and
  **255 of 275 are untried**, by name in `surface.json`.
- The first count read 258 because of that alias; it was wrong for the
  minutes between the capture and the fix and never committed.

## The counts, after

166 accepted-column rows (140 + 26). srv1 1.5B: accepted 41, refused 2+12,
lost 12 (all re-run), harness_defect 3. srv1 7B: refused 0+4, lost 4. srv2
1.5B: accepted 51, refused 2+10, lost 10, harness_defect 3. srv2 7B and
Qwen3-4B: 6 accepted each. 148 contrasts, unchanged — a `levels [1]` refusal
adds no contrast.

## Verification

`ruff check .`, `ruff format --check .`, `mypy` (139 files) clean; full
pytest green with the one pre-existing xfail. The drift test rebuilds the
surface from both evidence directories. Rebased onto session 29's two
commits (`c69e62c7`, `2adc7db2`) before the push; no overlap in files.

## Left open

- **#357: all five boxes are on the lane.** Closing it is the owner's call
  and has not been made at the time of this record.
- The declared column is one capture on srv2. Same digest on both rigs and
  `--help=all` is a property of the image, not the card; still, one capture.
- The 255 untried knobs are a menu, not a result. Which of them a run turns
  is #322's header's job.
- `sweep.py` still has no tests of its own.
- **#358** — resolved config into `identity.KEY`; every re-run record carries
  `image_digest`, and every surface row carries the flag dict.
- ollama's declared surface is an environment surface and nothing reads it.

next: #358 — the resolved serving config enters identity.KEY (after the owner closes #357)
