---
lane: 286
date: 2026-08-24
session: 28
---

# Session — lane/286 — 2026-08-24 (twenty-eighth)

**#357, without a rig: the accepted and effective columns are built from the
sweep already on disk, and the build shows that 26 of the sweep's 36
refusals have no reason on record.** The owner's constraint this session was
no rig access, so the declared column is owed and the tool that captures it
is in place. What the two free columns turned up outranks the column that
was not free.

## What provoked it

#357's five boxes. The 2026-08-24 sweep measured 140 cells and its README
narrates them; nothing computed which of its refusals carried the engine's
reason, which were the harness's own defect, and which knobs move a number
in which regime. The issue's own sentence about the shell-quoting defect —
"three untested speculative cells were recorded as refused" — was a
paragraph, not a state a reader could filter on.

## What landed

`tools/bench/serving/knobs.py` (in `NOT_A_FIGURE`: it states launch outcomes
and throughput ratios, no pass rate), `tests/test_knobs.py` (11 tests), and
`records/evidence/2026-08-24-knob-surface/` — `surface.json` (record
`knob-surface/1`), `surface.md`, README. The committed surface is pinned to
the evidence directory by a drift test that rebuilds it.

**Accepted has five states and they never share a label**: `accepted`,
`refused` (the engine's reason is in the record), `refused_reason_lost` (only
the wrapper `Engine core initialization failed. See root cause above` is in
the record), `harness_defect` (the engine's parse error quotes the value it
saw, the record holds a different one), `untried` (declared, never carried —
enumerable only once column 1 lands; until then `{"unknown": true}`).

- **`refused_reason_lost` is 26 of 36.** The harness kept 25 log lines and
  vLLM's traceback under the wrapper is longer than that. The sweep README
  attributed srv1's bf16 / fp8 KV / FLASH_ATTN / FLASHINFER refusals to
  compute-capability gates and the 7B's to OOM; those are reasoning and a
  hand re-run respectively, and the table carries neither. `sweep.py` now
  keeps 2,000 lines (`LOG_LINES`) and the image digest on every refusal.
- **`harness_defect` is exact, not a list of cell names.** `engine_saw:
  "{method:"` against `record_holds: {"method": "ngram", ...}`. The rule is
  the comparison, so a future shell-split shows up without anyone naming it.
- The two `refused` rows per rig that survive are real: `--ubatch-size 2`
  (needs a DeepEP all2all backend) and `method: suffix` (needs
  `arctic-inference`).

**Effective is single-flag contrasts at every shared n**, 148 of them: two
launched cells on one (rig, model) whose flag dicts differ in exactly one
key; the parent's other flags are the regime. Read off the table, not
reasoned:

- fp8 KV on srv2 1.5B, `len 1024 seqs 256`, graphs on: 0.94 / 0.72 / 0.86 /
  1.02 / 1.04 / 1.06 at n = 1 / 8 / 32 / 64 / 128 / 256. **The sign flips
  with n.** `test_the_regime_is_visible_in_the_committed_surface` pins it.
- `--enforce-eager` on srv1: **0.90 at n=1**, 1.00 from n=4, in three
  contexts. The sweep README's "0.1%" is the maximum-aggregate reading; at a
  single stream the flag costs srv1 10%. On srv2 the same contrast is
  0.17–0.20 at seqs 16 and 0.18 → 0.74 across n = 1 → 128 at seqs 128.
- ngram-3 speculation on srv2 at seqs 16 with graphs: 0.62 at n=1, **1.19 at
  n=2, 1.13 at n=4**, 0.89 at n=16. "Loses at every concurrency" was read at
  the maximum; there is a band where it wins.

**Declared: not captured, and the reason is now measured rather than
quoted.** The image was pulled to this workstation (no GPU) and
`--entrypoint vllm ... serve --help=all` exits 1 with `Failed to infer device
type`. `knobs.py declared <rig> <dir>` is the capture — one container start,
no model load — and `build` picks the file up by digest on the next run.
`HELP_COMMAND` and `DIGEST_COMMAND` are the two commands.

## Two things the checks caught

- `test_four_lenses` refused `ENGINE` and `RECORD` as module-level names
  already used with other meanings in `observed.py` and `headers.py`;
  renamed `SERVING_ENGINE` / `SURFACE_RECORD` rather than declaring an
  unrelated-meaning duplicate.
- The first regime test asserted fp8 was *inert* at n=1 on srv2 at seqs 256.
  It is not — it costs 6%. The test was written from the sweep README's
  sentence ("does nothing at n=16") and the table corrected it. That is the
  column doing its job on its own author.

## Verification

`ruff check .` and `ruff format --check .` clean; `tests/test_knobs.py`,
`test_bench_rounds.py`, `test_four_lenses.py`, `test_serving.py` green; full
suite run before the commit. **The push row of `58517949` failed on
`make typecheck`** — eight mypy errors in `tests/test_knobs.py` that the local
check (ruff + pytest) does not run; fixed in the follow-up commit, `mypy`
clean on 139 files. Lesson for the local check script: run `mypy` too, it
is a CI gate. No rig command
was issued this session. The local `vllm/vllm-openai:v0.26.0` image (pulled
for the `--help=all` attempt) is still on this workstation; `docker rmi`
reclaims it.

## Left open

- **#357's declared box** — one `python3 tools/bench/serving/knobs.py
  declared srv2 records/evidence/2026-08-24-knob-surface` on a free rig, then
  `build` again and commit both. Under a minute of rig time.
- **The 26 lost reasons** — the same cells under the fixed `sweep.py`: 12 on
  srv1 1.5B, 10 on srv2 1.5B, 4 for the 7B on srv1. ~2 min per graphs-on
  launch. Until then srv1's compute-capability story is prose, not record.
- `sweep.py` still has no tests of its own; `knobs.py` tests the records it
  writes, not the writer.
- **#358** — the resolved config into `identity.KEY`; `surface.json` already
  carries per-cell `flags` as a dict, which is the shape #358 needs.
- ollama's declared surface is an environment surface and nothing reads it.

next: #357 declared capture on a free rig, then the 26 lost-reason re-runs; #358 after
