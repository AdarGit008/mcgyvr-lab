---
record: session/14
lane: lane/286
agent: claude
started: 2026-08-21T22:15:00Z
ended: 2026-08-21T23:40:00Z
---

# Session — lane/286 — 2026-08-21 (fourteenth)

**#330 is done (`85596b47`).** Intent has a home, the spine is closed, and the
date the shape stops being provisional is a non-zero exit rather than a
sentence. Eight of nine children are code; #322's gate is the last one, and
#329's rig arm sits behind it.

## What changed

| where | what |
|---|---|
| `tools/bench/headers.py` | new. `RECORD = "run-header/1"`, `HOME = records/headers/`, `KEYS` (15, closed: 4 required + #322's other seven candidates + `run`/`answered_by`/`retroactive`/`notes`), `REVIEW_AFTER = 10`, `problems()` (nine rules), `listing()`, `list` with `--home`/`--schema` |
| `records/headers/` | thirteen headers and a README: D7's, retroactive, plus twelve seed questions |
| `tests/test_run_headers.py` | the eight checks by the DoD's names |
| `tests/test_bench_rounds.py` | `tools/bench/headers.py` declared `NOT_A_FIGURE` |

**The rules, and what each refuses.** `record`/`id`/`declared`/`question` are
required and nothing else is — requiring more now would invent the shape before
seeing the things it has to describe, which is the mistake #322 was rewritten to
avoid. A key `KEYS` does not name is refused, so prose goes under `notes` and
free text cannot enter the aggregatable layer. `{"unknown": true, "searched":
[...]}` needs a non-empty list, **checked recursively** — a bare `"unknown"` and
a silent `{"unknown": true}` are refused at any depth, which matters because the
one real unknown on disk sits two levels down. `declared` after `run.started`
requires `retroactive: true`: a header written afterwards is worth keeping and
must not count as evidence the gate was used. The filename carries the run's
start date when there is a run and the declared date when there is not, so a
question filed today and answered by a run next month keeps the date it was
asked.

**The review point is code.** `list` counts headers carrying a `run` block at
read time — a derived count cannot disagree with the directory it describes —
and at ten with no `run-header/2` schema on disk it exits non-zero and prints
what the review must do. Question-only headers are listed and not counted, or
the review could be reached by filing questions, which is the one thing that
costs nothing.

**The thirteen.** D7's is retroactive, written from its own log and evidence:
`cost.source` points at `d7-campaign.log` rather than restating it, and its one
`{"unknown": true}` is honest — no serving rate card exists (searched
`tools/bench/rate-card.json`, which prices tasks in seconds, the campaign log,
and `tools/power/`), so "half the campaign was idle" is a fact with no number
attached. Twelve questions were living inside a dated evidence directory: one
per D6 constant, one per "Decision owed" block naming the D1–D5 decision that
closed it, and one per bucket-B/C sibling (#325–#328) that **points and never
restates** — the issue is the record of truth, and a second copy here would be
a second thing to keep true.

## Shown to reject

16 mutations on scratch copies, each caught: a header losing its question, a key
the spine does not name, the `searched` requirement dropped, the nested walk
removed, the retroactive rule dropped, question-only headers counted toward ten,
the review point moved to eleven, the schema no longer clearing the owed state,
the owed state no longer setting the exit code, the D7 run start edited, its cost
pointing at a missing file, a seed question deleted, a sibling header restating
instead of pointing, an owed block no longer naming its decision, the
id-matches-filename rule dropped, and `problems()` returning nothing.

**A guard someone else wrote found the new module unprompted.** `make check`
went red on `tests/test_bench_rounds.py::test_every_figure_tool_is_classified`
(#231 check 6): a tool under `tools/` that is neither checked nor exempted fails
by construction. `headers.py` is declared `NOT_A_FIGURE` — it reads no run
directory and no row, states no rate, and the one number it prints is a count of
files. That is ADR-0037 rule 5 working from the other side: the discovery check
noticed a sink nobody had classified, which is exactly what it is for.

Gate 1863 → **1871 passed + 5 xfailed**.

## The DoD, all ten boxes

Every box has its test, so the last box — an `xfail` for any box closed without
one — is vacuous and no marker was added. The listing box was run rather than
asserted only: `python tools/bench/headers.py list` prints thirteen lines and
ends `1 of 10 run headers before the field review`, exit 0.

## Left open

- **#322's gate is the last child**: a driver that writes or checks a header
  before a run starts, writing the record type defined here. #329's rig arm
  (~19 min, the only rig time any child asks for) files its intent header
  through that gate, so the order is #322 → #329's rig arm → the re-run.
- The four `owed` xfails from #328 and #329's one are the owner's to rule on.
- ADR-0037 still Proposed. #318 must not carry `Closes #286`. The review-only
  PRs (#318/#320/#321/#331/#332, and #333 for this child) must never merge.

next: #322 — the run-intent gate, which unblocks #329's rig arm and closes the last child
