---
record: session/1
lane: lane/286
agent: claude
started: 2026-08-19T03:40:00Z
---

# Session — lane/286 — 2026-08-19

## Did

Recovered the previous session's unwritten material, finished the calibration
campaign it had launched, and recorded three findings that existed in no file.

**Recovery.** The 2026-08-18 session ran out of context at 03:17 UTC with the vLLM
ramp matrix in flight. Its transcript (~3,300 entries) was scanned and the material
that existed only there was extracted: six owner decisions taken in conversation and
written down nowhere (both fingerprint digests wired, unknown-config-key raises,
SSH always-on, the F7 close capture, the F11 depth-0 parse, `weights_sha256` as a
real pin); the end-to-end orchestrator survey, whose result file was written to a
transient path and is gone; and the ollama ramp replication. Nothing was lost from
git — every commit was already pushed.

**Two evidence citations withdrawn.** `records/evidence/serving-surface-2026-08-18/`
names `concurrency.json` and `census.json` in its `## Files` section. Neither was
ever written — `git log --diff-filter=A` over that directory returns one commit
adding `README.md` and `routes.json` only. The citation is withdrawn in a dated
block that points at where the material actually survives; what is genuinely lost is
the raw per-endpoint capture per model.

**Finding C5 — ollama's slots are real, and the 32-token knee was paperwork.**
Derived from ramp levels already on disk, no new rig time. Wall time against mean
latency at n=2 separates concurrency from queueing where throughput cannot: srv1
(`-np 2`) has wall == mean (6.01 s), srv2 (`-np 1`) has wall within 5% of the serial
prediction (7.95 s against 8.34 s predicted). Two slots run together; one slot
serializes. The slots contend — two concurrent sequences cost 1.45x the
single-sequence time each — and nothing batches past `-np`. The 32-token rows measure
something else: 58% of a 32-token request on srv2 is CPU-side fixed cost that
overlaps across requests, which is why a one-slot server reads 2.27x and clears a
gate meant to detect batching. That is C4's mechanism rather than a separate finding.

**The token count was never checked against the workload.** 33,358 completions across
170 files under `records/measurements/` put the median reply at **194** tokens and
p90 at **475**. `RAMP_TOKENS = 128` sits near the 28th percentile of what this bench
actually generates.

**Finding C6 — the vLLM matrix, and both plateau gates are wrong.** 15 ramps, 5
widths x 3 token counts, srv1, 11432 s, every launch clean. The throughput plateau
recovers the configured width exactly at 4, 8 and 16, and 128 agrees with 512 at all
five widths. Two constants fail:

- `BATCHING_SPEEDUP = 2.0` discards a correct answer at width 4, where the plateau
  is 4 at all three token counts and the speedup is 1.23-1.49. **No threshold fixes
  it**: any gate low enough to admit that curve also admits ollama at `-np 2`
  (1.45-1.48), whose plateau is unstable across replications. Speedup measures
  compute headroom, not batching — the constant is a concept to withdraw, not a
  number to recalibrate.
- The matrix's single false width — width 8 reading 16 at 32 tokens — is the unnamed
  `0.95` plateau cutoff at `contract.py:429`, not the token count: n=8 lands at
  0.939 of peak, short by 1.1 tok/s, so the rule walks on to 16.

Also recorded: partial batches cost a full batch-time at every width (n=2 is slower
than n=1 everywhere, 0.62x-0.74x), and the KV-capacity prediction for width 16 was
tested and did not bite (capacity 16.004, read 16 at all three token counts).

**Harness fixes committed** (`calibrate.py`): the `env` a pip-installed vLLM needs on
this rig, smallest-AWQ-first model selection, and `--engines` so re-running one half
does not redo the other.

**The lane was red and nobody had run the gate.** `make check` here failed on
`test_every_figure_tool_is_classified`: `tools/bench/serving/calibrate.py` had landed
in `ccae4424` without declaring whether it states a bench figure, and three commits
were pushed on top of it without a full gate run — the campaign was occupying the
rigs and only targeted test files were run. That is the **fourth** time this check
has caught a new tool under `serving/` (after `fingerprint.py` and `pin.py`, and the
`rglob` fix that made the tree visible to it at all), and the first time the failure
survived a push. Declared as NOT_A_FIGURE: it states tokens per second and speedup
ratios, which are properties of a server rather than of a run, the same exemption
`run.py` holds beside it.

Commits this session: `458637f4`, `3bd33892`, `feddb156`. All pushed.

## Left open

**Three decisions owed on measured constants**, each stated beside its numbers in
`records/evidence/calibration-2026-08-19/README.md`:

1. **Withdraw `BATCHING_SPEEDUP`** — and decide whether the ramp becomes a
   vLLM-only instrument. Ollama's width is *readable* (`total_slots` from
   `llama-server`'s `/props`, confirmed four ways); only vLLM's `max_num_seqs` is
   unaskable. Removing the gate costs the independent cross-check that first
   validated the method.
2. **Name and set the plateau cutoff** — `0.95` is a function default and produced
   the matrix's only false width.
3. **`RAMP_TOKENS`** — 128 and 512 agree at every vLLM width, so anything >= 128 is
   stable. Against the served workload the defensible values are 194 or 475.

Plus the two from earlier phases: `MAX_INLINE_ITEMS` (512 is the one value that
behaves differently per model; the gap is 52->255) and `MIN_VRAM_FRACTION` (the
detector is right, the policy locks five of srv2's twelve models out permanently).

**Four constants still have no measurement**: `START_TIMEOUT_S` = 900,
`DIGEST_TIMEOUT_S` = 1800, `LOAD_ATTEMPTS` = 2, `RAMP_REPEATS` = 2.

**Three schema defects in `calibrate.py`'s output**, all deferred to the assertions
pass so nothing touched the harness while the matrix ran:

- the same quantity is `configured_width` on a success row and `width` on a failure
  row, and a failed *launch* is distinguishable from a failed *ramp* only by the
  absence of `tokens`
- ollama rows carry `configured_width: null` and no other field naming `-np`, so
  they cannot be scored without joining them to a document by hand
- the refused `vram_fraction` is recoverable only by regex over the prose in `why`

**Steps 2 and 5 of the owner's five-step instruction are outstanding**: assertions
and tests encoding the measured distributions, then the adversarial review. Step 2
is blocked on decision 1 — a test pinning `BATCHING_SPEEDUP` would pin a constant
just shown not to exist.

**An unfiled issue.** Promoting `serving_semantic_sha256` into `identity.GROUPS`/`KEY`
is out of #286's scope (D7 — promotion is the owner's) and per ADR-0032 lands at a
round boundary. The owner decided to lock it and chose the mechanism: lock via the
survey, which has host access, with a run declaring the survey it ran under. Putting
the field in `KEY` directly would refuse every ollama comparison permanently.

**Never run:** srv2's full survey via the docker launcher (the launcher and the
weights digest both detect `pip` vs `docker` and the digest is verified there —
`d764277f…`, 5.57 GB, 34 s — but no survey has been driven on that host), and srv2's
vLLM ramp matrix. srv2's `--max-num-seqs 16` against a measured
`kv_cache_max_concurrency` of **5.314** is a standing falsifiable prediction: a ramp
there should read about 5, not 16.

**Both rigs are idle** (1 MiB each) and nothing is running.
