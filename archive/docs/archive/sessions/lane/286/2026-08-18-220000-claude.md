---
record: session/1
lane: lane/286
agent: claude
started: 2026-08-18T13:00:00Z
---

# Session — lane/286 — 2026-08-18

## Did
P1.2 (#286) implemented: `tools/bench/observed.py` writes the `observed` block beside `run.json` from both runners' `record_run`; the four probe-set fields move from `AWAITING_PROBE_SET` to `CAPTURED_IN_OBSERVED` (not to `AWAITING_ADMISSION` — #276's rule is not their promotion path, D7 is); `GROUPS`/`KEY` untouched; nothing reads the block, pinned by a grep test and by deleting the file and resuming.

Built against live servers rather than documentation — ollama 0.32.4/0.32.5 and vLLM 0.26.0 on srv1 and srv2 — and the measurements changed the design three times. `/api/show` verbose is 7.7 MB on the 1.5B, so arrays over 512 entries are elided to `{count, sha256}` joining `run.json`'s `vocabulary_sha256`. The served window is **4096** where `/api/tags` and `/api/show` advertise 32768 (262144 for nemotron) and only `/api/ps` says so. The engines answer opposite halves of the probe set, so `engine` is on the block. Evidence in `records/evidence/serving-surface-2026-08-18/`: seven findings including the dev-mode route diff (43 vs 25 routes, 18 gated, measured), the per-model slot count, and CPU placement being decided at load and sticky.

Second deliverable: `tools/bench/serving/` — a config-driven orchestrator (`run.py`) over per-engine backends (`backends/ollama.py`, `backends/vllm.py`) that name no other backend, with the shared contract and the concurrency ramp in `contract.py`. Cross-engine exclusion lives only in `run.py`. Replaces `census.py`, which is deleted. `weights_sha256` hashes the checkpoint on the serving host because vLLM publishes no weights digest, and `expect` is enforced at claim time.

Two `/code-review` passes (high, then max) plus an adversarial pass and live runs found 25 defects between them; all fixed and pinned by tests. The most important corrections: the throughput knee alone returned **6 for two ollama rigs configured one slot apart**, so a width is now reported only when the throughput and latency plateaus agree — which recovers vLLM's 8 and correctly returns `None` for ollama; family verdicts read digest fields `capture()` never emits, so every verdict was computed from `None`; and host readings (systemd `Environment=`, `docker inspect` env, process command lines, the launch command) were written verbatim to a tracked path with no redaction.

## Left open
next: run `tools/bench/serving/run.py --config configs/srv-full.json` end to end — every backend function is live-verified individually but the orchestrator's release-then-claim ordering across two engines has only run against stubs; then re-run `/code-review`, tick the plan's §2 P1.2 box, open the PR
