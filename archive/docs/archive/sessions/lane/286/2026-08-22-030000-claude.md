---
record: session/11
lane: lane/286
agent: claude
started: 2026-08-21T19:20:00Z
ended: 2026-08-21T20:12:00Z
---

# Session — lane/286 — 2026-08-21 (eleventh)

**#327, ambient, is done (`6e775162`).** Every recorded level row names the
card it ran under and the load on both machines; the curve reads the same
in any order it was run, and the row says which order that was.

## What changed

| where | what |
|---|---|
| `contract.py` | `CARD_STATE_COMMAND`/`LEVEL_STATE_COMMAND` (one ssh: the four card fields with units stripped, then `/proc/loadavg`), `card_state`/`loadavg`/`client_loadavg`/`level_state` (null + `why` naming the command, per field), `read_level_state` (the seam, docstring prices itself in `ssh_step_seconds`), `order_levels`/`RAMP_ORDERS`; `_level(..., reader=None)` reads at the level's END (`None` reads nothing: the warm-up); `ramp(..., host=, reader=, order=, seed=)` sorts `attempts` by `n` once before any reader and returns `levels_run`/`level_order`/`level_seed`; a seedless shuffle draws its seed |
| `backends/vllm.py` | `claim` reads the card beside `gpu_used_mib`; `checks.card` |
| `calibrate.py` | `LEVEL_ROW_DISPOSITION` (12 keys, the only guard on a value inside a sink), three new `RAMP_ROW_DISPOSITION` entries, `card` in `LAUNCH_CHECKS_DISPOSITION`/`LAUNCH_ROW_DISPOSITION["checks"]`/`_claim_fields`; `_one_ramp`/`_widths`/`ramp` thread `order`/`seed`; `--level-order`, `--level-seed` |
| `run.py` | the survey's ramp names its host |
| tests | 8 by the DoD's names + `test_a_shuffle_without_a_seed_draws_one_and_writes_it` + `test_the_survey_ramp_names_the_host_its_levels_are_read_on`; `_synthetic_level` (a literal of `_level`'s shape) replaced by `_Rig`, which stubs `_one` and the reader so `_level` runs its own body and `seen` proves the read came after the level's requests |
| README | an addendum under the width-16 attribution, naming its checks |

## Shown to reject

26/26 mutations (19 designed, 7 from the review: the read between `start()` and `join()`, the host dropped at `_one_ramp`, the flags dropped at `ramp`/`_widths`/`main`, the driver load hoisted, the reader inlined, the failure prose taken for the load line, the error row losing its seed), each caught by the test written for it; the timing mutation 5/5 on repeat. Gate 1846 → 1859. No rig time for the instrument; the
sweep itself ran on the rigs' CPUs (split srv1/srv2, ~2 s per mutation).

## Left open

- #328 conflicts, then #329 cross-rig (the only child that may ask for rig
  time); #330 alongside. ADR-0037 still Proposed; #318 must not carry
  `Closes #286`.
- The ramp phase's `ollama.claim` still writes no row (#325's finding); its
  minutes stay a remainder. Not #327's.
- `throttle_reasons` is the driver's raw mask; decoding is the reader's.
- The first values are the re-run's: both rigs answered the exact line
  shape on 2026-08-21 (`50, 14.98, 300, 0x0000000000000001`), which is what
  the fixture's `CARD_LINE` is modelled on.
- `--level-order` is not part of a cell's `--resume` identity (`key()` is
  untouched); the help says so. A re-run in a new order is a fresh journal.
- Multi-card hosts: the card block is the first line `nvidia-smi` prints,
  unindexed. Both rigs are single-card; not #327's.
- The review-only PRs (#318/#320/#321/#331) still must never merge.

next: #328 — six recorded conflicts (four survive re-derivation, two dissolve, and no check reads any of them)
