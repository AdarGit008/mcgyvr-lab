---
record: session/9
lane: lane/286
agent: claude
started: 2026-08-21T18:30:00Z
ended: 2026-08-21T19:10:00Z
---

# Session — lane/286 — 2026-08-21 (ninth)

**#325, provenance, is done (`aeac6c89`).** Every row carries a clock and the
tree it ran from; every phase is a row.

## What changed

| where | what |
|---|---|
| `contract.py` | `now`/`stamp`/`seconds_between` (the one clock seam); `provenance()` + `PROVENANCE_DISPOSITION` (7 fields, all carried); `HARNESS_SURFACE` |
| `product.py` | `surface_files`/`_lines`/`digest` take a `surface` kwarg, default unchanged — the harness digest reuses the algorithm |
| `calibrate.py` | `STAMP` merged in `emit`; spans on load/launch/ramp/sleep/error/refused rows; `_claim_fields` carries `claim_started_at`/`claim_ended_at`; `LOAD_ROW_DISPOSITION` carries the attempt span; phase row in `main`; `completed` skips it |
| `run.py` | `_journal(path, stamp)` merges; `run(..., stamp)`; cell spans on all three write paths; survey phase row + `result["run"]`; `main` hashes config bytes; `completed` skips the phase row |
| `backends/ollama.py`, `backends/vllm.py` | attempt span / checks span |
| tests | 7 new (DoD names), census sees keyword-argument calls, `_survey_row` takes a real journal |

## Shown to reject

10/10 mutations on scratch copies: emit/journal stop stamping, ramp row loses
`ended_at`, phase row renamed, `completed` counts the phase row, `tree_dirty`
constant, `result["run"]` misnamed, attempt/checks lose a bound, launch span
not the claim's. Gate 1829 → 1836. No rig time.

## Finding carried forward

The ramp phase's `ollama.claim` writes no row (only the load phase's does), so
its time is attributable only as the remainder the new test names. Noted on
#325 for #326/#327.

## Left open

- #326 identity (+E3 dogfood reading `attempt`), #327 ambient, then #328, #329; #330 alongside.
- ADR-0037 still Proposed; #318 still must not carry `Closes #286`.
- The tree is already off round `r1-commissioning` (product digest moved
  before this session); the `product.py` kwarg adds nothing new to that.

next: #326 — identity on every row (hostname, pinned checkpoint, the failed load's trail)
