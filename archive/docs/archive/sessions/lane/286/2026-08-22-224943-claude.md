---
lane: 286
date: 2026-08-22
session: 19
---

# Session — lane/286 — 2026-08-22 (nineteenth)

**A verdict about a shared card was being written from a list of names.** #335
box 5 is closed: `ollama.claim` and the survey's post-ramp check now record
where every resident actually sat, so the campaign's own first `void_if` is
evaluable for the first time. No rig time; the whole session is harness code.

Session 18 (`-190347-`) proved cross-engine co-residency on both rigs and
rewrote the campaign to ask what is possible. Its phase 0 could not run,
because the thing phase 0 measures was never computed.

## The gap was one function's scope

`_placement(resident, model)` matched one row of `/api/ps` and returned. Every
other resident's `size_vram` was read in the same response and discarded. So a
co-residency cell recorded that its neighbour was **listed** and never where
the neighbour **landed** — and `coresidency_arranged`, built from an HTTP 200
and a name, said `true` either way.

The measurement that makes this concrete is session 18's, not this session's:
ollama answers `load_http=200` with **93% of the model on the CPU**, and the
spilled model still returns correct code in seconds. "It loaded" and "it fits"
are different facts and only the first was written down.

**ollama cannot report this about itself.** Its pre-flight fit check in
`server/sched.go` is guarded by `len(s.loaded) > 0` — its OWN models — so a
foreign allocation is invisible to it and llama-server auto-fits into whatever
is left. The reading has to be taken here or it is not taken.

## The second half was the one that was nearly missed

`run.py`'s post-ramp re-read exists because BL-6 found neighbours evicted mid
-curve. It compares `spec["coresident_with"]` against `backend.residents(host)`
— a list of names — and writes `coresidency_after.held`.

**A name list catches the neighbour that LEFT and nothing else.** One that
stayed and spilled is still listed, under its own name, with its full `size`
beside a `size_vram` nobody read. So `held: true` was reachable for a neighbour
sitting 93% on the CPU: the same silent nothing at the other end of the
measurement, one function away from the one #335 named.

It is a second producer and a second read, recorded beside the verdict rather
than folded into it — a second read is a second moment, and the two are named
apart for the reason the card's `before`/`after` readings are.

## Recorded, never gated — and the reason is the campaign's

Neither placement enters `ok`. A spilled neighbour is the frontier this
campaign exists to map; a claim that refused it would refuse its own question.
That is the owner's standing correction from session 16 applied where it lands:
**declared pre-state must match or refuse; everything else recorded, never
ignored.** Placement is not declared pre-state. It is the result.

**Absent is not zero and it is not one.** A backend that cannot report
placement writes `null`. `vllm.residents()` still does not exist — contract
defect 5, untouched — so a vLLM cell's placement is absent rather than a
number, and that is what bounds this box to what ollama can see.

## What landed

- `_placed(row)` split out of `_placement`, so the same arithmetic answers
  about a neighbour. `_placement` keeps its contract exactly, `{}` included.
- `_placements(resident)` over every `/api/ps` row; `placements(host)` for a
  caller holding only a host, public for the reason `residents` is.
- `claim` records `resident_placements` per attempt. One `/api/ps` response
  already read, so it costs nothing beyond the arithmetic.
- `run` records `coresidency_after.placements`, and
  `coresidency_placements_error` on the path where that read raised.
- Both sinks declare it: `LOAD_ROW_DISPOSITION` carries the field,
  `SURVEY_ROW_DISPOSITION` names `placements` as its own producer. The field
  cannot be added to a producer and dropped by a sink.
- Four named checks in `tests/test_serving.py`, listed in a dated block under
  §7 of `docs/run-contract-2026-08-22.md`, where contract defect 1 is closed.

**Mutation sweep 7 of 7 red**, including the pre-change behaviour itself
(placement for the model under test only) and the two absent-vs-empty cases.
Gate **1888 passed + 14 xfailed**, up 4. `make check` exit 0 read directly, not
through a pipe.

## A correction about my own instrument, which is the sixth instance

The first mutation sweep I ran was a bash harness that reported **all seven
mutants GREEN** — "the check does not reject" — on checks that in fact reject
every one of them. I did not take the result at face value, re-ran one mutant
by hand, saw it fail correctly, and rewrote the sweep in Python, where all
seven come back red.

That is a false negative, which is the safe direction, and it is the **sixth**
instance on this lane of a result that came from how a check was run rather
than from what it asserts — and the first where the affected instrument was the
sweep itself rather than the code under it. The lesson is the lane's existing
one, one level up: **a sweep that reports green is making a claim, and it needs
its own positive control.** The Python driver has one by construction — it
prints the failing assertion for each mutant, so a green result cannot be
mistaken for a run that selected no tests.

## The four cross-engine harness changes are filed

They had been carried as prose in this lane's records since session 18, with
feasibility proven and the work unscheduled. Each is now an issue with its
evidence verified in the tree:

- **#343** — `run.py:353-358` releases every other backend with no per-cell
  opt-out, and `run.py:365-368` *enforces* it, so a cell declaring a foreign
  neighbour has it killed two lines before its own load and is then refused for
  the state it asked for. Carries the open question of the neighbour
  declaration's shape, which #346 cannot be written before.
- **#344** — `ollama.py:486` gates on `card_idle_before_load is True`. The
  reading and the `is True` are both correct and must survive; the defect is
  that it compares the card against *empty* rather than against the declared
  pre-state (ADR-0038 rule 2).
- **#345** — contract defect 5, and **build it first**: it unblocks phase 0's
  vLLM arm alone. It is not a port of the ollama code. vLLM is one process per
  model and `/v1/models` is a served-model list, not a residency list, so the
  reading must be assembled from `gpu_compute_apps` — already captured at
  `contract.py:470` and consumed by nothing. And vLLM never spills, so
  `size_vram / size` has **no vLLM analogue**: the reporting shape is a
  decision, stated as three options in the issue, not a stub.
- **#346** — `ollama.py:361-378` is the only neighbour loader in the tree, it
  posts to the ollama base of the engine under test, and it is in the wrong
  layer: the orchestrator holds the backend table. Lists the four properties a
  rewrite can silently drop, `keep_alive: -1` among them.

Build order **#345 → #343 → #344 → #346**. The campaign header's prerequisite 3
and §7 of the run contract both name them now, so neither says "unfiled".

## The gate's own error message was the misleading half — the seventh instance

FLOW-03 failed twice on this record while it **had** a `next:` line.
`extractNext` (`tools/baseline/src/facts/git.mjs:65`) finds `## Left open`,
scans to the next `##`, and stops. The line sat under `## Next`, so it was
invisible — and the rule reports *"has an empty next:"*, which describes
neither the file nor the cause. Renamed the heading; verified by running
`extractNext` over this record and session 18's, which both return their line.

**Two things this cost, and one it taught.** Two red pushes, and the first
diagnosis ("the record has no `next:`") was wrong in a way `make check` could
never surface: **`make check` is `lint typecheck test`, and the Makefile's own
comment calling it "everything CI runs" is false** — CI runs a separate
`baseline` job. `make baseline` is the local equivalent and hung past five
minutes here, so CI is the practical authority and `gh pr checks` must be read
row by row.

It is the **seventh** instance on this lane of a result coming from *where*
something was read rather than what it says, and the first where the
misleading half was a gate's own message rather than a check of ours.

## Left open

1. **#345** — decide vLLM's placement reporting shape and build it. It is the
   only one of the four that pays off alone.
2. **#335's remaining boxes**: the ADR-0027 D2/D3 pre-state idiom, the ignore
   recorded on the contrast, refused-saturation `outcome`, label uniqueness on
   the same `label or id` fallback the loop uses, the ADR-0030 clause-1
   statement. 3 of 8 ticked.
3. #336 (K1 sampler, K2 `n_ctx_total`, K3 holder). #322 unstarted.

#286 sits at 6 of 9. #318 and #338 are review-only and must never merge.

next: #345 — decide vLLM's placement reporting shape, then build it
