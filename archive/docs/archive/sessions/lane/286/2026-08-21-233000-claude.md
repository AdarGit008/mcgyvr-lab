---
record: session/8
lane: lane/286
agent: claude
started: 2026-08-21T23:05:00Z
ended: 2026-08-22T00:20:00Z
---

# Session — lane/286 — 2026-08-21 (eighth)

**#324, the sink census, is done (`0fd5d33e`, review fixes `2a1594ea`).** ADR-0037
rule 5 now has its mechanism: every `emit()`/`record()` in `calibrate.py`
and `run.py` is enumerated by AST and must be DISPOSED or EXEMPT.

## What changed

| where | what |
|---|---|
| `tests/test_sink_conformance.py` | `_sinks()` census over both files; `SINK_DISPOSED` (6) and `SINK_EXEMPT` (16) tables; 8 new tests incl. the mutation-on-a-copy check |
| `calibrate.py` | `_load_row` + `LOAD_ROW_DISPOSITION` (15 carried / 6 dropped with reasons, of the attempt record's 21); `sleep_state` assigns `vllm.claim`'s return and merges it via `_claim_fields`, shared with `_launch_row`; `SLEEP_ROW_DISPOSITION` |
| `run.py` | `SURVEY_ROW_DISPOSITION` — `claim`, `describe`, `contract.ramp`, `residents`, each whole, with the derived paths named |

Census key: `<file>::<function>::<site>`, `#n` when a site label repeats in
one function. Deviates from the issue's sketch (which collapsed the four
ramp refusals into one key); each now carries its own reason.

## Shown to reject

Nine mutations on scratch copies, 9/9 caught: new `emit`, new `record()`,
claim discarded again in `sleep_state`, `_load_row` dropping
`model_sha256`, `ollama.claim` gaining a field, a drop without a reason,
the `declared_slots` hoist removed, `wake_call` dropped, an exempt entry
removed. `make check` green, 1818 → 1827. No rig time.

## Review (`2a1594ea`)

`/code-review high` on the commit found six things; all applied. The one
that mattered: merging the claim put `engine` on the sleep row, `key()`
reads it, and `sleep_state`'s done-probe did not carry it — **every
finished sleep cell would have been relaunched on `--resume`**, refused
ones skipped. Now a regression test writes the rows, reads them back via
`completed()`, and asserts no relaunch. The other five: `_claim_fields`
no longer owns `model`; the sleep producer set is read off the row by
identity instead of a literal in the test; the census walks the whole
tree (methods, nested defs, attribute calls) with a canary; the load
re-emission reads the row; `attempt` carries the record's own ordinal.
Second sweep 5/5. Gate 1829.

## Left open

- #324's tenth box (E3 dogfood reading `attempt`) is #326's.
- Next in the owner's order: **#325 provenance**, then #326, #327, #328, #329; #330 alongside.
- ADR-0037 still Proposed; #318 still must not carry `Closes #286`.

next: #325 — provenance (clock on every row and claim attempt; commit, config digest, run start)
