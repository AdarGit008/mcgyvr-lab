---
record: session/7
lane: lane/286
agent: claude
started: 2026-08-21T19:30:00Z
ended: 2026-08-21T22:59:00Z
---

# Session — lane/286 — 2026-08-21 (seventh)

**Five owner decisions, nine sub-issues, one ADR. #286 now has a tree under
it that blocks its close, and the mechanism session 6 proved is a decision of
record with its own enforcing tests.**

## The five decisions, in the order they were put

| # | question | ruling |
|---|---|---|
| 1 | push session 6's five unpushed commits | pushed (`7d5ec4d8..d4d6b8c1`) |
| 2 | does the `emit()` census join bucket B | **yes, before B** — the seven new instruments land behind it |
| 3 | #286's seven acceptance boxes | **tick all seven + dated note**. Two had stale wording, not unmet substance: box 5's `4e110156` predates #287's `GROUPS` change (HEAD == main at `f2d9ddb9`; `KEY` unchanged since 4e110156); box 7's test was renamed |
| 4 | how #322 is split | **every open item becomes a sub-issue of #286, blocking its close**; enforcement is the PR-body convention — no `Closes #286` until `subIssuesSummary.completed == total` (GitHub shows progress and does not block) |
| 5 | the ADR for the mechanism | **now, as a sub-issue too** |

Each answer was asked as one short question; the first attempt carried an
analogy block and the owner cut it — "no ELI5, keep it simple and concise".

## The tree

Drafted by a draft → adversarial-verify → revise workflow (24 agents, every
`file:line` re-read at HEAD, every "shows the gap" command re-run), then
read in full and de-duplicated by hand before filing.

| # | child | owns |
|---|---|---|
| #323 | ADR-0037 | the mechanism; **done this session** |
| #324 | the sink census | 19 `emit()` sites (not 10), 3 `record()` sites in `run.py`, 2 dispositions; `LOAD_`/`SLEEP_`/`SURVEY_ROW_DISPOSITION`; a new sink ships green today (shown) |
| #325 | provenance (B1, B4, B7) | clock on every row and claim attempt; commit, config digest, run start; phase rows. 8,185.3 s of 14,404 s unattributed, re-derived |
| #326 | identity (B2, B5, E3) | identity block on every row; vLLM weights pin; `DIGEST_TIMEOUT_S` measured by the pin path; the `LOAD_ATTEMPTS` dogfood test |
| #327 | ambient (B3, B6) | temperature/power/clock/load per level; **the ramp readers are order-dependent** — the same synthetic curve run descending reads `saturation_n` 24 instead of 4 |
| #328 | the six conflicts | **four survive re-derivation, two dissolve**: C4 (`endpoint_props: false` is llama-server's write-enable flag) and C5 (the 32-token knee was retired by D3). C1 is wider than filed: `top_p`/`min_p`/`repeat_penalty` differ from the pin on 17/17 slots, not 4. C3 is a sequencing gap (the yield reads the card before ollama evicts), not a contradiction |
| #329 | E2, the cross-rig claim | engine args are **identical** across the two launchers (shown by building both lines); what differs and is unrecorded on any ramp row: GTX 1660 SUPER vs RTX 3060, driver 580 vs 595, pip vLLM vs the docker image. The only child allowed rig time: ~19 min, last |
| #330 | the run-header home (#322 part 2) | record type, listing, review trigger at 10 run headers, D7's retroactive header first |
| #322 | re-parented; part 1 stays | dated split block appended to its body, nothing rewritten |

Two overlaps were removed before filing: E3's dogfood test was claimed by
#324 and #326 (now #326's); the sleep row's timing fields by both (now
#324's, #326 reads them).

## ADR-0037 (`fb4e9af2`)

Status Proposed. Five rules. Rule 2's xfail is `strict=True` with a dated
reason in one of two grammars — `owed — <question>` while the owner has not
ruled, `decided — <decision>` once they have — because #328 needs to park
four conflicts before any ruling. Rule 3 names checks as `tests/<file>::<test>`
and `tests/test_finding_is_a_check.py` resolves every such name in
`docs/decisions/` against the parsed module; the ADR is its first member. The
xfail check has no population yet; three canaries (lax, undated, bare
decorator) are the proof it can refuse. Gate 1812 → 1818.

One thing the namespace gate caught: the `Amends: none — …` commentary named
"ADR-0026" and `tools/decisions/index.py` read it as an edge. Reworded.

## Left open

- **#324–#330 are all open work**, in the owner's order: census → B (#325,
  #326, #327) → C (#328) → E2 (#329); #330 alongside. Decisions become code
  before any re-run.
- **ADR-0037 is Proposed** until the owner appends a DECIDED block.
- **PR #318** is still a DO-NOT-MERGE draft; its body must not carry
  `Closes #286` until the nine children are complete. #320/#321 never merge.
- The owner has not read the nine bodies; each is ~50–80 lines of
  verifiable DoD and may be cut.

next: #324 — the sink census (`tests/test_sink_conformance.py::test_every_sink_is_classified` and the three new dispositions)
