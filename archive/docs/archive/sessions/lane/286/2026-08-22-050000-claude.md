---
record: session/13
lane: lane/286
agent: claude
started: 2026-08-21T20:50:00Z
ended: 2026-08-21T22:10:00Z
---

# Session — lane/286 — 2026-08-21 (thirteenth)

**#329's three no-rig arms are done (`dd4f1ab9`); its rig arm is not, and is
blocked by design.** The cross-rig claim is now a function that refuses rather
than a sentence that asserts. Separately, a background review of #327 was
triaged and its one real finding fixed (`0c8cf127`).

## What changed

| where | what |
|---|---|
| `tests/test_cross_rig_claim.py` | new, three arms. `::test_the_two_launchers_hand_the_engine_the_same_arguments` builds both command lines through `vllm._start` with `launcher` forced and ssh stubbed, and holds the pip and docker branches to the same engine arguments and the same environment once `PATH` is set aside. `cross_host_contrast(journal, model, width)` — pure — returns the two hosts' speedups only when both LAUNCH rows carry the same `weights_sha256` and `identity.serving_build` and each names its card, else the field that refused; `::test_a_cross_host_contrast_refuses_when_identity_differs_or_is_missing` holds five refusals and the accepting case. `::test_the_2026_08_20_cross_rig_claim_holds_only_on_a_journal_with_identity_rows` is `xfail(strict=True)` |
| README | an append-only addendum: the journal names neither card nor configuration, what the function requires, its three checks, and the rig arm's flip. `:983-987` untouched |
| `tests/test_sink_conformance.py` | `os.getloadavg` patched to a sentinel before the shape assertions (#327's box 2 was satisfied by any constant) |

**Equal cards are not required, and that is the point.** The contrast holds the
weights and the engine build fixed and requires each side to NAME its card; the
cards differing is the hypothesis under test, so a check that demanded equal
cards could never be run on these two rigs. #329 filed the middle field as
`engine_version`; #326 landed it as `identity.serving_build`, and the code reads
what the row actually says.

## Shown to reject

11 mutations on scratch copies of the tree, each turning the arm it targets red:
a flag only docker passes, an env only docker passes, the pip `PATH` export
dropped, the model moved behind the flags; each of the three identity
requirements relaxed in turn, and the launch-row requirement removed; the
journal repointed at a complete one, where arm 3 XPASSes and `strict` fails the
suite — the flip working — and repointed at one whose hosts are reversed, where
it stays red for the direction rather than the refusal.

**One honest limit: arm 1 is a differential.** A flag dropped from BOTH branches
at once leaves it green, because what it holds is that the two launchers agree,
not that any particular flag is present. That was found by a mutation that did
not flip and is recorded rather than papered over.

The #327 fix was shown the same way: the constant body the triage demonstrated
(`return [0.5, 0.5, 0.5]`) left the suite at 115 passed before, and fails at the
sentinel assertion now.

Gate 1861 + 4 xfailed → **1863 passed + 5 xfailed**.

## The DoD, four boxes of six

- Boxes 1–4 are met: the launcher arm, the refusing contrast, the xfailed claim,
  and the README paragraph (appended at the end, which is where append-only puts
  it now that #325/#327/#328 have their own addenda; `:983-987` is byte-identical).
- **Box 5, the rig arm, is not started and cannot be**: it files its intent
  header in the register half of the #322 split before it runs, and #322/#330
  are open. About 19 minutes on the two rigs, sequential per E14 — the only rig
  time any #286 child asks for.
- **Box 6 rides box 5**: whether docker exists on srv1 is recorded in the rig
  arm's own header, during that arm's ssh.
- One deviation: #329 proposed the marker read
  `reason="2026-08-21: hardware claimed, configuration unrecorded; see #329"`.
  ADR-0037 rule 2 spells two grammars, `owed — <question>` and
  `decided — <decision>`, so the marker uses the first and carries the same
  content. Consistent with #328's four.

## The #327 review, triaged

28 agents, ten confirmed findings, delegated to a subagent that verified each
against HEAD: **1 real, 6 stale, 2 duplicate, 1 known.** The review ran against
the working tree BEFORE the seven review-driven mutants named in `6e775162`'s
message were fixed, so eight of its findings quote source that is not in the
tree — including two claiming `level_state` scans `lines` forward when it scans
`reversed(lines)`, and one calling the throttle-mask comment wrong when it was
already corrected. The `level_order`-outside-`key()` finding is a decision on
record in session 11 and in the flag's own help. The real one is fixed above.

## Left open

- **#330 and #322 are now the critical path**: #329's rig arm sits behind them,
  and so does the campaign re-run. Seven of nine children are code.
- The four `owed` questions from #328 and this one's are the owner's; each fix
  is its own commit and removes one xfail.
- ADR-0037 still Proposed. #318 must not carry `Closes #286`. The review-only
  PRs (#318/#320/#321/#331, and #332 for this child) must never merge.

next: #330 — the run-header home, which unblocks #329's rig arm and #322
