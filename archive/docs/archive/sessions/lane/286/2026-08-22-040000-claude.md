---
record: session/12
lane: lane/286
agent: claude
started: 2026-08-21T20:15:00Z
ended: 2026-08-21T20:45:00Z
---

# Session — lane/286 — 2026-08-21 (twelfth)

**#328, the six recorded conflicts, is done (`053b188a`).** Four survive
re-derivation and are now dated `owed` xfails; two dissolve and are now green
checks whose job is to refuse the re-filing. No rig time, and no commit here
touches `tools/bench/serving/`.

## What changed

| where | what |
|---|---|
| `tests/test_calibration_conflicts.py` | new. Six checks by the DoD's names, over the campaign's committed evidence. Red: **K1** 17/17 served slots ran under a sampler the semantic digest does not describe (`top_p`/`min_p`/`repeat_penalty` on all 17, `temperature` on 4, `top_k` on 1); **K2** 19/19 semantic blocks carry no name for the launched `-c` (the product holds 19/19; 6/19 launched at 8192 from srv1's `OLLAMA_NUM_PARALLEL=2`); **K3** 15/15 yields found the card held and named no holder; **K6** 36/36 ramp figures across both hosts carry no `engine_version`. Green: **K4** `endpoint_props: false` is llama.cpp's write flag, and the payload beside it is complete on 19/19; **K5** 187 disagreeing pairs of ramp figures, 0 unattributable |
| README | `## Conflicts recorded 2026-08-21 — six, re-derived at d4d6b8c1`, appended. K1–K6 (C1–C6 above are this README's own findings), each with its evidence path, its check, its question and a `- decision:` line |

**The design decision the DoD did not make.** These checks resolve their
evidence to the **newest** `records/evidence/calibration-*` directory, not to
this campaign. The 2026-08-19 files are frozen history: a check pinned to them
can never turn green, so its `xfail` would outlive its own finding and
`strict=True` would be dead weight. Pointed at the newest campaign, each `owed`
reason is a question put to the **next** run — which is what every one of them
literally asks — and the run that answers it turns XPASS and fails the suite
until the marker comes off. `campaign()` reads the root at call time so a sweep
can point it at a mutated copy.

**Two checks read wider than #328 filed them; both verdicts are unchanged.**
K5 covers both engines (227 pairs, not 27) because the vLLM matrix quotes 1.0
against 3.76 for one model at one token count and `configured_width` is what
separates them — so the declared conditions are `tokens` AND the width, which
is the general form of the lesson C5 taught in one instance. K6 covers every
figure-bearing ramp row in the campaign's journals (36) rather than the twelve
of `d7-ramp.jsonl`; a ramp row that raised carries an `error` and no speedup, is
not a figure, and is out of population.

**ADR-0037 reconciliation: nothing was owed.** The DoD asked that the
"xfail-at-filing owed state" be reconciled with rule 2 before the first xfail
lands. #323 already landed both grammars — `owed — <question>` while the owner
has not ruled, `decided — <decision>` once they have — so this file is rule 2's
first real member rather than an extension of it. `tests/test_finding_is_a_check.py`
now has a live population of four beside its canaries.

## Shown to reject

25 mutations over a copy of the evidence, each check called against the copy.
Every survivor turns green when its defect is repaired (`k1_fixed`, `k2_fixed`,
`k3_fixed`, `k6_fixed`, and all four at once) and stays red under a partial
repair: one sampler field left disagreeing on one slot, `n_ctx_total` present
but carrying the per-slot window, `holder` present and `null`, a version on one
host's rows only. Each green check goes red under four independent breakages
(K4: params gone, `total_slots` gone, params empty, no child reporting the flag
false — the last is its population guard; K5: two figures at one condition 2×
apart, every figure identical, a disagreement arriving in a journal no check
names). Two mutations prove the campaign resolution in both directions: a newer
campaign with the four defects repaired turns all six green while these files
stay as they are, and repairing *these* files while a defective newer campaign
sits beside them changes nothing. 25 of 25. The sweep script is in the session
scratchpad, not the repo.

Gate 1859 → **1861 passed + 4 xfailed**.

## The DoD, as met

Seven of the eight boxes are met as written. Three notes:

- `grep -c 'xfail(strict=True, reason="2026-'` prints **0**, not 4, and cannot
  print 4: `ruff format` explodes any decorator that does not fit in 88
  columns, and a reason short enough to fit carries no question. The property
  the box wanted holds under `grep -c 'reason="2026-'` → **4**, and the real
  guard is `tests/test_finding_is_a_check.py::test_every_xfail_in_the_suite_is_strict_and_carries_a_dated_reason`,
  which reads the AST rather than the text. `grep -c ': owed — '` → 4, so the
  close-time `0` stays meaningful.
- The block is appended at the end of the README (line 1090), not "after
  `:1012`": #325 and #327 appended their own addenda after the issue was filed,
  and append-only means the end.
- `pytest tests/test_calibration_conflicts.py` prints exactly `2 passed, 4
  xfailed`, so the file carries **no canaries** — the six checks are the six
  tests. Four of them are red today, which is their own demonstration; the two
  green ones are demonstrated by the sweep above rather than by a synthetic
  corpus.

## Left open

- **The four `owed` questions are the owner's.** K2 and K6 feed #329 directly:
  the width split (srv1 `-c 8192 -np 2` vs srv2 `-c 4096 -np 1`) and the ollama
  version split (0.32.4 vs 0.32.5) are both host-configuration differences
  sitting under the 23%/96% figure that the record attributes to hardware.
  Each fix is its own commit, referenced from its K-line, removing one xfail.
- **A background review of #327 finished during this session** (28 agents, from
  a workflow started in session 11). It reports ten confirmed findings; two of
  them are stale against the tree — both quote a `level_state` that scans
  `lines` forward, while `6e775162` scans `reversed(lines)` with a comment
  naming that exact failure mode, and `contract.py` has not changed since. One
  is real and small: `tests/test_sink_conformance.py:347` checks only the
  *shape* of `client_loadavg()`, so a body returning a constant would pass and
  #327's DoD box 2 ("the driver's `os.getloadavg()`") is untested. The rest are
  untriaged and are #327's, not #328's.
- #329 cross-rig next (the only child that may ask for rig time), #330
  alongside. ADR-0037 still Proposed; #318 must not carry `Closes #286`.
- The review-only PRs (#318/#320/#321/#331) still must never merge.

next: #329 — the cross-rig claim, `hardware, not configuration`, read off a journal that names neither (K2 and K6 are its two named conditions)
