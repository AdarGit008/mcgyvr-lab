---
record: session/3
lane: lane/286
agent: claude
started: 2026-08-19T09:42:00Z
ended: 2026-08-19T12:43:00Z
---

# Session — lane/286 — 2026-08-19 (third)

Written as a **handoff**. The next session starts at `## Start here` and its
first job is to launch the campaign.

## Did

Took the lane from "eight decisions and no code" to "the campaign is ready to
launch". Steps 0.1, 0.2, 1, 1.1 and 2 of the plan are complete; **step 3 is not
started**. Eight commits, `86b58775`..`1d618e9e`, all pushed. Baseline gate green
at **1794 passed**. Both rigs idle at **1 MiB**.

Two adversarial reviews ran against my own work and between them found **9
blockers and 25 defects**, all fixed. That is the substance of the session; the
detail is in `records/evidence/calibration-2026-08-19/`:

- `step0-gaps.md` — the scouts-and-verifier output, six blockers, decisions E1–E10
- `step0-readiness.md` — both rigs, 15/15 roster cells, findings R1–R6
- `step1-review.md` — the first review: 7 blockers, 12 defects
- `README.md` §`E1–E15, locked by the owner` — the fifteen decisions, each with
  the file it lives in

## Start here

### 0. Verify the uncommitted change is still there — **do this first**

**There is one deliberate uncommitted change in the working tree** and the run
depends on it:

```
git diff tools/bench/serving/configs/d7-campaign.json
```

It must show `min_vram_fraction: 0.85` **removed** from the `qwen2.5-coder-7b`
entry, with `expect_spill: true` and an `_uncommitted` comment in its place.

That is **DE-G**, decided by the owner: the floor gated the one entry whose whole
purpose is to spill. E2 keeps the 7B on srv1 *because* 4.7 GB plus its KV cache
does not fit 6144 MiB, and that near-miss is the measurement; it read 0.908, so a
floor at 0.85 was 5.8 points from refusing the exact data point the entry exists
for. **The owner asked that this not outlast the run**, so it lives in the working
tree, not in the history.

If the diff is empty, a checkout or rebase has restored the floor. Re-apply it
before launching — the comment in the file says how.

`launch.py` does **not** check that the tree is clean, by design here. Nothing
else will remind you.

### 1. Launch

```
.venv/bin/python tools/bench/serving/launch.py --campaign \
  --log records/evidence/calibration-2026-08-19/d7-campaign.log
```

`launch.py` refuses or launches — there is no separate verify step to skip. It
checks 17 decision markers present and 4 absent **before and after `ruff
format`**, refuses if another driver is already running (E14), then launches the
three phases detached (E4). Add `--dry-run` to see it without launching.

The phases, declared in `launch.CAMPAIGN` (E15) and run in this order:

| phase | D7 items | est. |
|---|---|---|
| sleep | 5 | ~20 min |
| survey | 1, 3, 4 | ~6–7 h |
| width matrices | 2, 6 | ~4.7 h |

**~11–12 h total, not D7's 8–9 h** — the owner accepted the revised figure.
Serial by host (E14) and chained with `;`, so a phase that refuses does not
cancel the two behind it.

### 2. If it crashes

**Relaunch the identical command.** Every phase carries `--resume`; the survey
skips entries already journalled and the matrices skip samples already on disk,
checking *before* the vLLM launch, which is the expensive part. Output:

- `d7-sleep.jsonl`, `d7-ramp.jsonl` — one fsynced row per sample
- `d7-survey.json` + `d7-survey.json.jsonl` — the document and its journal
- `d7-campaign.log` — appended, never truncated

A refusal counts as done: it is an answer about the rig, and re-running buys the
same refusal. `--retry-failed` says otherwise.

**This mechanism has never recovered a real crash.** It is unit-tested against
synthetic journals, torn lines and superseded rows; the campaign is its first
real test.

### 3. Then steps 4 and 5

- **4** — set D6's four constants from the data, and derive assertions and tests
  from the distributions. `START_TIMEOUT_S` and `LOAD_TIMEOUT_S` now get points
  from every launch (`start_seconds`) and every digest (`digest_seconds`), and
  **both** ramp repeats are kept with a `repeat_spread` summary, so the bias that
  `max`-over-repeats puts on the peak is finally quantifiable. **The owner's HITL
  gate is before this step.**
- **5** — adversarial review, session record, PR. **No PR is open for this lane.**

## What to watch during the run

Ranked by what would cost the most.

1. **The two deep-spill models are the untested case.** `gpt-oss:20b` (0.7945)
   and `qwen3-coder:30b` (0.5806) have never been ramped, because
   `MIN_VRAM_FRACTION` refused them until D4 withdrew it. Their ladder is capped
   at 12 (E13) and the per-request budget is
   `90 + n × 475 / 4` seconds. If their aggregate rate falls under **4 tok/s**,
   the top level times out and BL-4's rule refuses the **whole** ramp rather than
   truncating it — which is the correct behaviour and also a lost cell. Watch the
   first `qwen3-coder:30b` ramp; if it refuses, `RAMP_FLOOR_TOKENS_PER_S` is the
   number to revisit.
2. **srv1's sleep arm may refuse.** `--enable-sleep-mode` has only ever been
   tested on srv2. srv1 is Turing with `--enforce-eager` mandatory. A refusal
   there is correct, not a failure.
3. **Co-residency (D7 item 4) has never run.** The neighbour is pinned
   `keep_alive: -1` and residency is re-read after the ramp; if it left, the row
   becomes `ramp_failed` with `coresidency_lapsed_during_measurement`.
4. **Teardown runs at the entry point**, on the success path and the abort path,
   so both rigs should be idle afterwards. Verify it — the pinned neighbour is
   what makes this necessary.

## Left open

- **`concurrency.expect` is set on no entry** (0 of 11), so `matches_expected` is
  `None` for all 17 cells. D3's stated reason for `saturation_n` existing — the
  survey declares, the ramp measures, a mismatch is printed — is inert for this
  campaign. The width matrices carry `declared_slots` on every row now (DE-F), so
  the comparison is available there; nobody has decided whether the survey should
  declare anything.
- **`emit`/`_journal` fsync the file but not its directory**, so a brand-new
  `d7-*.jsonl` could be absent after a power loss even though its rows synced.
- **`placement.expect_spill` and `observed_2026_08_19` are recorded and evaluated
  by nothing** — a model that unexpectedly did *not* spill is not flagged.
- **The issue for `serving_semantic_sha256` is still unfiled** (carried from the
  previous session). Out of #286's scope per D7 of the original issue; lands at a
  round boundary per ADR-0032.
- **The first session record on this lane** (`2026-08-18-220000`) still has a
  stale `## Left open`.

## The shape of what changed, for a reader who wants the why

Three things drove everything else.

**A throughput plateau is not a slot limit (D1).** The output splits into
`declared_slots` — what the engine says, with a provenance — and `saturation_n`,
what the curve did, always carrying the token budget and plateau fraction that
produced it. `knee` and `batches` are retired.

**The owner's revision to E5 was the sharpest correction of the session.** I had
concluded vLLM has no observed source for its width, from the HTTP surface alone.
It is in the running server's own argv and in the container's `Config.Cmd`. So it
is observed after all, `dispatched` is only the fallback, and a disagreement
between the two returns `contradicted` and reports **neither** number — which
catches a server this run did not launch.

**Two guards refused a correct launch before they worked**, and both are recorded
because the failure mode is identical: a check that fires on the wrong thing gets
switched off. The marker list's absence test matched a docstring *explaining* a
withdrawn constant; E14's guard matched the shell that was editing the file. Both
now match structure rather than text.
