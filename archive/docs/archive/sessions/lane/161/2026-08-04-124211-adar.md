---
record: session/1
lane: lane/161
agent: adar
started: 2026-08-04T12:42:11Z
---

# Session — lane/161 — 2026-08-04

## Did
Special session: connected the two rigs (srv1, srv2) over the tailnet and made mcgyvr able to reach them.

Oriented, pruned lane/155 (merged as PR #160; its origin ref was already gone, and the fetch --prune also cleared 150/43/44). Probed both rigs before writing anything: srv1 (GTX 1660 SUPER 6GB) serves vLLM Qwen2.5-Coder-3B-AWQ on :8000 and Ollama on :11434; srv2 (RTX 3060 12GB) serves vLLM Qwen2.5-Coder-7B-AWQ with prefix caching and sleep mode, plus Ollama carrying 1.5b/3b/7b/14b, deepseek-coder-v2:16b, qwen3:30b-a3b, yi-coder:9b.

THE FIRST REAL DISPATCH THIS PROJECT HAS MADE. A hand-written config resolved three rungs, probed all three sources live (~0.2s), and runner.dispatch returned real completions from each — stop=complete, cap respected, quality_safe read correctly. Everything below the config was already host-agnostic; pool, availability, runner and capacity were written without a localhost assumption. Only the two modules that FIND things had one.

Built #161 on that. detect swept a hardcoded localhost on five ports and propose sized rungs against this machine's nvidia-smi, so a GPU-less laptop with two rigs on the tailnet took init's _nothing_to_bind path. The ports were already a table and the host was the literal, so targets_for() is the cross product. Three things the multi-host case forced that the single-host case never exposed: source names collide (two rigs, both Ollama on 11434, and sources is a mapping — one rig silently overwrites the other), so names qualify with the host when a sweep covers more than one, keyed on hosts PROBED rather than hosts that ANSWERED so a source is not renamed when a box is down; Backend.kind had to split from Backend.name because requires_backend matches what a server IS, not what its source is called; and fit evidence had to be chosen by WHERE the backend is rather than by which claim is stronger — a VRAM test describes this machine's card, so applying it to a remote rig rejects a 7B on a 12GB machine because the laptop asking has none.

A remote rig is admitted on its own model listing. That is deliberately the weaker claim and is labelled as such — vLLM lists what it has loaded, Ollama lists what has been pulled, so it establishes the rig is provisioned rather than that the model is resident — but unlike a VRAM estimate it cannot be wrong about which machine it describes. The local path is untouched: a local backend's model list is still not evidence about a card that is right here and unreadable.

Verified end to end against the real rigs, not just in tests: init on this GPU-less laptop binds four sources across two machines, pool --probe reports them live, and dispatch returns completions through the file init wrote. make check green — 1009 tests (25 new), mypy strict on 93 files, ruff clean, baseline 92% with no blockers. Eight mutations, all caught: names never qualified; a local listing treated as fit evidence; the inversion warning dropped; arbitrary placement hidden; kind collapsed into name; a remote fit citing the local card; a duplicate host read as two machines; hosts overriding an injected detection.

Dead end worth recording: my first cut admitted ANY served model as proof of fit, local or remote. A shipped test caught it — a 2GB card was suddenly proposed everything Ollama had on disk. The rule is not 'serving is proof', it is 'the local card is evidence about local sources only'.

Filed three issues, two of them found by doing the work. #161 (this lane) and #162 (the ladder is one ordered list and the evidence says routing is a matrix) were filed on request; #164 fell out of the first init run.

## Left open
next: open the PR for #161, then #164 — init binds Ollama on the CAV-01 protocol, so every rung of a default install is quality_safe=False while api: openai on the same port and model is not; it is a one-word change in a file init already writes, and it blocks the #144 sweep, which cannot run a measurement on a caveated path
