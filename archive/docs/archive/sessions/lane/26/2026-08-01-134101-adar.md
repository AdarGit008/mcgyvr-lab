---
record: session/1
lane: lane/26
agent: adar
started: 2026-08-01T13:41:01Z
---

# Session — lane/26 — 2026-08-01

## Did
Built E4 — per-task sandbox (epic #26), all five children, as new package src/mcgyvr/sandbox/. make check green (226 tests, +57), baseline 0 blockers. Both Docker paths ALSO verified end-to-end against a live daemon (CI has none): real container lifecycle + the credential invariant (host ANTHROPIC_API_KEY absent from container env), and real image build/cache/digest/reuse/evict.

#27 lifecycle — DockerSandbox: one container/task, force-teardown on success/failure/interrupt (context manager + atexit process-exit reaper keyed by container name), memory/cpu/pids bounds, sleep-infinity keepalive driven by exec. Workspace is a bind-mounted host git repo (fresh init + base commit) so the gate diffs a real change and reset() (git reset --hard + clean -fdx) wipes an attempt.

#28 stack detect — sandbox/stack.py: Python (uv/poetry/pipenv/pip, lock-derived) and Node (pnpm/yarn/bun/npm) from manifests+lockfiles; base image + install command; unpinned (no lockfile) and undetectable are explicit and name the sandbox.image override. Polyglot picks the first base and reports the rest.

#29 image+cache — sandbox/image.py: per-repo image built once; cache key = base ref + manifest CONTENTS + setup (lockfile change invalidates, unrelated source change does not); base pinned to a digest at build (REPRO-04); labels make the cache inspectable; prune() evicts oldest-first, clear() is the documented reset.

#30 tempdir — sandbox/tempdir.py: same interface, host execution, weaker mode surfaced once. Almost empty: populate/reset/teardown/credential-filter live in base.py, so both modes share one test suite.

#31 connectivity/credentials — localhost endpoints -> host.docker.internal (+ --add-host host-gateway on Linux, native on Desktop); container env built from nothing and vetted so credential_env_names(env)==frozenset() by construction. This is the red-failing SECURITY.md assertion.

Design seam: Docker is behind a runner callable, so argv/Dockerfile/cache-key logic is fully testable with no daemon. New CLI: `mcgyvr sandbox [repo] [--cache|--clear-cache]`. Config's existing sandbox.{mode,image,setup} feed open_sandbox(); no runner.py change (that is #38).

## Left open
next: E4 complete; #38 (acceptance command execution) can now wire the gate to Sandbox.run — open a lane/38, add a suite-precondition pre-flight, and use open_sandbox(config...) with sources' base_urls as endpoints. #36 (JS/TS adapter) still needs tree-sitter.
