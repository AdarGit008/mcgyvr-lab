---
record: session/1
lane: lane/47
agent: adar
started: 2026-08-01T16:39:11Z
---

# Session — lane/47 — 2026-08-01

## Did
E7 #47 deterministic index delivered — the zero-token substrate. Two modules under src/mcgyvr/orchestrator/. index.py: build_index(root, max_file_bytes=1MiB) -> Index. Enumerates non-ignored files via 'git ls-files -z --cached --others --exclude-standard' (honours .gitignore rather than re-implementing it), reads each once skipping oversized + binary (NUL sniff), holds decoded lines for text search. Index.search(term, regex, ignore_case, limit) -> Match(path,line,text). SymbolTable buckets definitions/references/exports by name for O(1) lookup. BuildStats reports elapsed_seconds, files_indexed, files_skipped_large/binary, bytes_indexed, symbol_count, per-language counts, degraded_extensions. Degrades to text-only on a no-grammar language (empty symbols, still searchable). symbols.py: extract(path, source_bytes) -> [Symbol(name,kind,path,line,detail)]. Python via stdlib ast (def/async/class -> defs with method-vs-function detail; __all__ or public top-level -> exports; Call -> references). JS/TS via tree-sitter (same _JS/_TS/_TSX grammars the gate's JS adapter uses; function/class/method/arrow-const -> defs; export named/default -> exports; call_expression -> refs). Unparseable file -> no symbols, no crash. NO model call anywhere. Exposed as 'mcgyvr index [repo] --search TERM --symbol NAME --limit N'. Verified on this repo: 176 files, 8784 symbols, 0.27s. 44 tests, make check green (342), baseline 0 blockers.

## Left open
next: Open PR for #47 to main. Next E7 child: #48 (resolve a natural-language target to candidate paths) — consumes this Index: text search + SymbolTable.definitions/references to shortlist paths for a query like 'the fetch helper'. Then #49 bounded reads, #50 decompose->contracts, #51 agent-context accelerator, #52 index cache/invalidation. Note: index is in-memory only; persistence/invalidation is #52's job.
