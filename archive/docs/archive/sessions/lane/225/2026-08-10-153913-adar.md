---
record: session/1
lane: lane/225
agent: adar
started: 2026-08-10T14:00:00Z
---

# Session — lane/225 — 2026-08-10

## Did

Phase 0 of #225: the premises checked before the campaign, and two of them
did not survive.

**The positive control's material was mis-stated in three documents, and the
route arithmetic was priced against the wrong set.** `records/claims/CLM-0017.json`
is unambiguous: the ~+20pp output-shape effect ran on *local-ai's unported
Python contracts under local-ai's own harness* — not "the twenty JS/TS
contracts" as ADR-0020, #231's body and #225's own amendment all said (the
arm-A/arm-B confusion #234 corrected, standing in three more places). Two
consequences: un-releasing `bundle-ts` never bought the exactness it was
priced at, and "reproduced across two harnesses" inverted the truth — across
harnesses the effect *nulls*, because mcgyvr's user message always carries
the rule (arm A's p = 1.00 is CLM-0017's own explanation). The n = 20 replay
was also below the m ≥ 6 wall regardless (+4 discordant, best p = 0.125).
ADR-0020 and ADR-0019 got Correction sections per #234's convention; #231 and
#225 were amended in place the same day.

**ADR-0019's "eleven of the twelve" miscounted its own table.** The live
`tools/power/report.py` reports nine of twelve structurally unresolvable and
zero of twelve rejecting; the zero is what the argument rides on. Corrected,
with the propagation into `tools/instruments.json` named.

**The owner decided the campaign's shape** (recorded as an amendment on #225,
each point argued there): n = 400 paired problems total per ADR-0019 D5 —
"per rung" satisfied by ~4 measured strata of ~100 inside one set, never by
400 per point; strata assigned from a floor-tier calibration sweep, not from
generator self-labels (#197's record: steering drifted ~2×); an equal-capacity
~400 reserve split by a pre-declared stratified hash over admitted ids, which
the bench never depends on; the positive control is a rule-ablation condition
on the generated bench, directional, with the knob landing in #113;
`function_implementation` + `bug_fix` only, mixed within stratum
(`type_annotation` deferred with #211, C12's case recorded); the arms' material
claims folded in (#126's multi-symbol subset in both languages, #17's recorded
reference-length range and `target_content` subset, #119's interior stratum,
#198's uniform renderer sections and shape-strict checkers, #162's shape
labels and retained never-passers); fresh tier names, two flat per-language
roots declared `retired: null, trainable: false` before any sweep; pause-
tolerant campaign priced in generator runs (~80 for ~800 problems ≈ 1.6× the
volume that tripped the monthly spend limit twice in one day).

**`docs/bench-sourcing-2026-08-10.md`** records acceptance item 1: the
in-repo inventory — five retired sets with n, language, 3B rates and CLM
sources as the specification; the vendored local-ai originals and the pool as
adjacent barred material — with §3 reserved for the outside-set search so the
sourcing order is auditable.

**Phase 1, same session: the outside-set search recorded, and MBPP+
measured.** `docs/bench-sourcing-2026-08-10.md` §3 adopts the 2026-08-07
adopt-nothing verdict under the bench's strictly higher bar (instrument
material must be non-public by construction — no public set can satisfy it),
cited with its date and provenance rather than re-run. MBPP+ against the 3B
through the rig sweeps' own serving path (`records/measurements/
mbpp-plus-3b-2026-08-10/`): **70.6% base / 60.6% plus** — the issue's
"plausibly between d3 and the pool" hypothesis is refuted, MBPP+ reads above
d1 at the easy end, the d3→pool collapse is a unit-of-work cliff rather than
small-function difficulty, and the gap strata must interpolate reference size
and assertion count. Contamination caveat recorded with the number, cutting
against the easy-end placement rather than for it. MBPP+'s 378 ids join
HumanEval's 164 in the campaign's decontamination blocklist. `pin.py --check`
still matches all 12,331.

**Phase 2, same session, first half: the design of record and the blind
split, committed before anything exists to read.**
`docs/bench-design-2026-08-10.md` fixes every name and mechanism: sets
`bench-ts`/`bench-py` with flat roots (`tools/bench/tasks/ts|py`), tiers
named for the arms, `b<nnn>-<slug>` ids, the reserve outside the declared
roots at `tools/bench/reserve/`, manifest-only serving, the 2048 sweep cap,
and the campaign order with the declaration ahead of the first sweep.
`tools/bench/split.py` is the split rule itself — salted per-id hash, both
arms travel with the id, stable under the pauses the #197 record guarantees
— pinned by `tests/test_bench_split.py` (salt, golden assignments, purity)
so it can never drift. `tools/bench/mbpp-entrypoints.json` puts MBPP+'s 378
entry points beside HumanEval's 164 in the decontamination blocklist. The
declared-target anti-triviality rule (replace only the target symbol's body
in the reference, helpers intact) is designed in the doc; the gate that
implements it is the next act. `make check` green, 1,212 tests.

**Phase 2, second half: the gate is built and live.** `tools/bench/admit.py`
implements the design doc's §3 order — the meta.json sidecar (labels +
per-arm `target_symbol`), declared-target anti-triviality (Python shadows
the target, TypeScript renames the mandated declaration form or refuses as
a named `target-form` failure — helpers intact either way), both front
doors screened over every declared function, the cross-split screen falling
out of screening against the whole manifest, `--pin` recording the
pre-declared split and placing the reserve outside the declaration's roots,
`--verify` holding tree/manifest/rule to each other. Smoke-tested end to
end with a real two-arm candidate: the gate first REJECTED it (4 asserts <
5 floor — the floor working), then ADMITted the fix; fixture removed.
Offline invariants in `tests/test_bench_gate.py`. `make check` green,
1,220 tests.

## Left open

Phase 3: pilot batches per steering band, then in ONE change: the first
admitted contracts + the instruments.json declaration (bench-ts/bench-py,
retired: null, trainable: false) + the measure.py tier wiring
(BENCH_TIERS, manifest-served, language from tier name) + test-pin updates
— declaration before any sweep. Then the 3B calibration sweep at cap 2048
and strata.json.

next: Phase 3 — pilot generation per band, and the declare-then-sweep
change.
