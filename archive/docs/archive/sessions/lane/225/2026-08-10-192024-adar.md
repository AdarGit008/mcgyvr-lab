---
record: session/1
lane: lane/225
agent: adar
started: 2026-08-10T17:45:00Z
---

# Session — lane/225 — 2026-08-10 (Phase 3)

## Did

Phase 3 of #225: the pilot generated, gated, pinned, declared, and measured.

**Forty paired problems exist and all forty were admitted.** Eight generator
batches (five problems each) authored `b001`–`b040` against the pinned brief
(`2026-08-10-bench-pilot-brief.md`, beside this record): four steering bands
of ten, domain × type × file_shape × shape assigned per problem, ids
allocated per batch so no two generators could collide. Each batch
self-gated to ADMIT; the joint gate run over all forty — the only level
where the cross-batch near-duplicate screen exists — passed 40/40. Several
ideas were replaced *before* writing because the generator recognised a pool
overlap (leaf-paths, keyset pagination, dice notation among them); the
near-dup screen never had to fire. Steering conformance was audited
mechanically: every reference inside its band's size bounds in both arms,
no label drift against the assignment table, scaffolds exactly on the
assigned rows, multi_symbol ≥ 25% per band.

**The pin recorded the split the rule chose: 22 bench / 18 reserve.**
`--verify` clean; per-band bench counts 6/6/4/6 (binomial noise, reported
rather than repaired, per the split rule's own doctrine). The reserve moved
to `tools/bench/reserve/`, outside every declared root.

**The ONE change landed as one commit, declaration before any sweep.**
`bench-ts`/`bench-py` in `tools/instruments.json` (`retired: null,
trainable: false`, mutually paired — the only legal live combination);
`measure.py` bench tiers (manifest-pinned, `split == "bench"` only,
language from the tier name at both inference points); serving tests
mirroring the pool's plus a `verify_manifest() == []` invariant; the
five-retired-sets pin scoped to retired sets; the builder's
refused-by-policy list now names both bench arms; the bench task trees
joined the digest-pinned lint excludes. 1225 tests green.

**The calibration sweep ran the same day, at the declared cap.** 3B via
srv1 (`protocol: openai`), greedy, one draw, `--max-output-tokens 2048`,
both arms selftested 22/22 through the rig path first. Realized rates,
bench half, per band (ts / py): g1 1/6 / 2/6 · g2 1/6 / 1/6 · g3 0/4 / 0/4
· g4 0/6 / 1/6. Failure modes checked before believing any zero (#212):
all 38 failing arms are acceptance failures — 0 cap overruns (max
completion 740 of 2048), 0 parse errors. The zeros are capability.

**`tools/bench/strata.json` opened with its first dated block.** Provisional
assignment stratum = steering band (re-binning 4–6-problem cells would fit
noise), rates quoted with n, and the calibration's one implication stated:
the ladder landed lower than steered — g1, aimed *just above* d3-class,
reads AT d3-class, and both upper bands sit at ~0. s1/s2 are interior on
both arms, so the interior requirement holds and #119 is not foreclosed;
never-passers exist in quantity for #162's cell.

## Decided

- Pilot scale 4 bands × 10 under the standing spend caution (~4 generator
  runs in #197's units, ~5% of full-campaign volume), full pilot rather
  than half-probe.
- Band definitions from measured anchors: d3-class median 34-line ts
  references / ~14 asserts at 16.7%, pool-class median 60 lines / ~18
  asserts at 0% — bands g1–g4 interpolate ts 30–45 / 45–60 / 60–80 /
  80–120 lines.
- strata.json's first block keeps stratum = steering band, explicitly
  provisional until campaign n.

## Next

Phase 4, the full campaign to ~800 admitted problems, pause-tolerant, with
one steering decision to take before the first batch: whether to add a band
easier than g1 (ts ~20–30 lines, d2-class) so one stratum reads 30–50% on
the floor tier — the pilot says size→difficulty is steeper than the
targets assumed, and without an easier band the bench's easiest stratum
sits at ~17%. Then Phase 5: #224 re-read against the strata, PR.
