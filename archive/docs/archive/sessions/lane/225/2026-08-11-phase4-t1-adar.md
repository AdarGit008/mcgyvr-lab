---
record: session/1
lane: lane/225
agent: adar
started: 2026-08-11T00:00:00Z
---

# Session — lane/225 — 2026-08-11 (Phase 4, tranche 1)

## Did

Tranche 1 of the Phase 4 campaign: the ladder re-aimed, one hundred
problems generated, gated, pinned, and calibrated.

**The steering decision, taken at the session's open (owner, via
AskUserQuestion).** Two calls: (1) band **g0** (ts 20–30 reference lines /
py 15–23 / 7–11 asserts, d2-class unit of work, aimed at 30–50% on the
floor tier) joins the ladder and **g4 stops generating** — its pilot
problems stay pinned and reported, the measured strata become g0/g1/g2/g3;
(2) the campaign runs batched with checkpoints, ~100–200 admitted per
tranche, operator go-ahead between tranches. No gate change was needed for
the new band: `admit.py` requires only a non-empty `steering_band`, and no
test pins band names or bench counts (verified before generating).

**One hundred problems exist and all one hundred were admitted.** Twenty
generator batches (five problems each) authored `b041`–`b140` against the
tranche brief (`2026-08-11-bench-campaign-t1-brief.md`, beside this
record): five band-batches of twenty — g0 twice, g1/g2/g3 once — with the
domain pool widened to thirty (ten new), no domain twice per band-batch,
quotas 6/20 bug_fix, 6/20 multi_symbol, 5/14 scaffolded fn_impl per
band-batch, table generated programmatically with the quotas asserted.
Every batch self-gated to ADMIT on its first or second pass (a few size
trims to land in-band); the joint gate over all hundred — the only level
where the cross-batch near-duplicate screen exists — passed 100/100 with
zero collisions, despite deliberate domain recurrence across band-batches.

**The pin recorded the split the rule chose: 46 bench / 54 reserve.**
Per band bench/reserve: g0 23/17, g1 7/13, g2 8/12, g3 8/12. `--verify`
clean; 1225 tests green (`make check`, exit status read directly — never
through a pipe); committed as `bb2fdb21` *before* any sweep read the new
material. The split's cell skew is reported, not repaired: g0's bench half
landed light on bug_fix (2/23 vs ~30% generated — ten of twelve g0
bug_fix went to reserve) and just under the multi_symbol quota (5/23).
Both self-correct in expectation as tranches accumulate; tranche 2's g0
rows should lean bug_fix and multi_symbol to speed the recovery.

**The calibration swept the enlarged bench the same day, at the pinned
identity.** 3B via srv1 (`protocol: openai`), greedy, one draw,
`--max-output-tokens 2048`, both arms selftested through the rig path
first, fresh dated dir `records/measurements/bench-calibration-3b-2026-08-11/`.
Realized rates, bench half, per band (ts / py): g0 **1/23 / 5/23** · g1
2/13 / 2/13 · g2 2/14 / 1/14 · g3 0/12 / 0/12 · g4 0/6 / 1/6. Failure
modes checked before believing any zero (#212): 0 of 136 arm-draws overran
the cap (max completion 740 of 2048), 0 parse errors — the zeros are
capability. `strata.json` block 2 appended (block 1 untouched); corpus
re-pinned at 12,511 replies.

## Learned

**The re-aim undershot: size→difficulty is steeper than even the pilot's
correction assumed.** g0 was steered a full class easier than g1 and aimed
at 30–50%; it reads 4.3% (ts) / 21.7% (py). The retired rulers' rates do
not transfer to matching reference size — the floor probe's d2 read 41.7%,
but the bench's shape-strict checkers and enumerated rejections price the
same file size higher. The instrument is behaving as designed; the *aim*
inherited a rate–size mapping from a laxer ruler. The language asymmetry
(py above ts at the easy end) is consistent with MBPP+ and the pool.

The interior requirement still holds on both arms (ts: g1 2/13, g2 2/14;
py: g0 5/23, g1 2/13), so #119 is not foreclosed, and never-passers are
plentiful (g3 0/12 both arms). But no stratum reads 30–50%, the range
where regressions as well as gains show clearly.

## Decided

- Tranche 1 composition 40/20/20/20 (g0 double-weighted as the
  uncalibrated band) under the standing spend caution — ~10 generator runs
  in #197's units, ~12.5% of campaign volume.
- Sweep into a fresh dated measurement dir rather than resuming the
  Phase 3 dir: same cap and identity, but the tranche boundary is part of
  what the record has to show.
- strata block 2 keeps stratum = steering band; re-binning at n ≤ 23 would
  fit noise.

## Next

The tranche-1 checkpoint question, put to the owner before tranche 2:
either steer a band easier than g0 (ts ~12–20 lines, 5–8 asserts, one
small behaviour — the third consecutive "easier than assumed" correction),
or drop the 30–50% aim and accept g0-py at ~20% as the ladder's easy
anchor. Then tranche 2 (~100–200 admitted, refill quotas leaning bug_fix
and multi_symbol in g0), and onward toward ~800; Phase 5 (#224 re-read,
PR) after the campaign completes.
