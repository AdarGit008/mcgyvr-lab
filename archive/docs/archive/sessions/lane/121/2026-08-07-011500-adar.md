---
record: session/2
lane: lane/121
agent: adar
started: 2026-08-07T01:15:00Z
---

# Session — lane/121 — 2026-08-07

## Did
The write-up the owner deferred from 2026-08-06, over a corpus that finished
overnight.

Overnight runs, all resume-safe and all complete: the srv2 campaign finished
its tenth model unattended; srv1 took repeats for llama3.2:3b and
qwen2.5-coder:7b at 0.7 and 1.0, giving three runs per cell across all four of
its models; srv2 took T=1.0 arms for 7b, yi-coder:9b and qwen2.5-coder:14b and
three runs of qwen3-coder:30b's d2 sweep; and a selectivity pass ran over all
30 srv1 sweeps.

Two overnight results changed conclusions written the evening before. The
mid-ladder T=1.0 arms (yi-coder:9b +2, qwen2.5-coder:14b +2, 7b -1) killed
"hot sampling helps the weakest models" — those are middle rungs, and the
differences are the size of the measured noise, so the finding is that **no
rule predicts temperature from the model**. And qwen3-coder:30b's d2 sweep
returned +2 of 12 in all three runs, which is the first multi-run confirmation
that breadth pays on genuinely hard tasks; the strongest rung's flat 11-of-12
remains a single run and is labelled as one everywhere it appears.

Written: a README beside each measurement (campaign and batch B), carrying
construction before result, the full tables, and the limits — including that
qwen3:30b-a3b truncated 172 of 180 draws and gpt-oss:20b 56, so both rows
measure the 768-token cap rather than a model and belong in no average.

CLM-0014 (breadth's value is a property of the rung: +3 to +8 of 20 at the
floor, +2 of 12 at d2 repeated three times, nothing at the top), CLM-0015
(temperature measured at last; only the weakest model gains, per-draw quality
always falls, T=1.3 breaks the format), CLM-0016 (breadth is bounded by its
checker: pooled gain +5.0 to +1.9 as the checker thins, wrong-but-accepted
rising with every draw, harness validated at 4751 of 4751). All three cite
vendored paths and pass tests/test_claims.py.

CLM-0013 corrected in place rather than left standing: its measurement holds,
its "#119 ships breadth defaulting to 1 citing this record" is withdrawn, and
its collapse-to-mode mechanism is marked rung-specific — 5 of 20 tasks
collapsed on the top rung, 0 of 20 on srv1's four.

The campaign and the batch that refines it share one branch and one PR.

## Left open
next: open the PR (no PR is open on lane/121 — #188 merged before the campaign
existed), and comment the outcome on #119 (breadth as per-rung policy citing
CLM-0014, temperature likewise citing CLM-0015, both read against CLM-0016)
and on #132 (its answer now bounds a shipped default, which raises its
priority). GitHub was deliberately left untouched for the owner.

Then: apply t20's repair to d1 itself — deferred only while the campaign's run
directories needed a stable tier digest, and nothing depends on that now.

Worth measuring next, in rough order of what they would settle: a repeat of
qwen3-coder-next-ud at d2, since "breadth reaches nothing at the top" rests on
one run; a raised output cap for the two reasoning models whose rows measure
the cap; and #132's sampling frame, which is the only thing that sizes how
much CLM-0016's bound costs in practice.
