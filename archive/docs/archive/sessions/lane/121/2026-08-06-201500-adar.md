---
record: session/1
lane: lane/121
agent: adar
started: 2026-08-06T20:15:00Z
---

# Session — lane/121 — 2026-08-06

## Did
Resumed the srv2 campaign from its host-restart pause (identical command, all
six finished models replayed from disk with no dispatch) and ran a refinement
batch on srv1 in an isolated worktree, so `lane_1`'s HEAD stayed at 5fb7030
and the in-flight sweeps kept recording the revision actually executing them.

Rig (`4c1ba4d`): sampled temperature became an input rather than DEC-6's
inherited constant, guarded by the identity check that already refused a
resume across workers; `d1r` holds t20 with its contract repaired, as a
VARIANT_TIERS entry the campaign driver cannot climb into; `summarise()`
dropped the "price of moving off greedy" label its own rows contradict.

Batch B (`c0948f6`), all on srv1's four models unless noted:

* **t20 is not a broken task.** Its contract does declare repeated-key
  handling unstated while `accept.mjs` asserts last-wins — a real defect — but
  repairing it changed nothing: 0 of 36 draws before and after, with t06/t07
  unchanged as controls also at 0 of 36. The floor fails t20 on ordinary bugs;
  15 of 36 crash before an assertion runs. The three never-passing tasks are
  all genuinely hard and the campaign's coverage ceilings stand as measured.
* **Temperature, never measured before this.** 0.3 / 0.7 / 1.0 / 1.3. With
  three or more runs per cell on the two smallest models the hot arm pays only
  on the weakest: 1.5b 12.2 [11-14] at k=8 against 10.0 [10-10] cold; 3b -0.8
  with fully overlapping ranges. A single run per cell had shown a gain on
  both — it was noise, and only the repeats caught it. T=1.3 is past the peak
  and fails a new way: 7 of 20 tasks lose a draw to a reply the parser
  refuses, against 0-2 lower down.
* **Breadth beats the knob.** +4 to +6 tasks of 20 from one draw to eight in
  every cell, plateauing by about the fifth; a K=16 arm added nothing from
  draw 6 to draw 16.
* **What a thin checker costs** (`tools/breadth/selectivity.py`, offline,
  re-selects from candidates already on disk). At a quarter of each
  `accept.mjs`'s assertions the entire benefit disappears — the 1.5b holds at
  6 correct from k=1 to k=8 while accepted-but-wrong rises 8 to 9 — and false
  wins climb with k, which is the mechanism rather than an argument for it. At
  three quarters most of the benefit survives with 2-3 false wins. The harness
  reproduced all 798 original verdicts at full strength before any weakened
  cell was read.

Also measured, incidentally and worth keeping: greedy is not reproducible.
Each model was dispatched greedy twice (probe anchor, sweep anchor) and 8 of
10 model-runs returned different bytes on up to 4 of 20 tasks, one flipping a
verdict. Server-side, not our sampler.

srv2 reached its last model. `qwen3-coder:30b` is the first in the campaign to
escalate — 0.95 on d1, 0.75 on d2, swept at d2 — where breadth took 9 of 12 to
11 of 12, thinly and with a long tail (draws 0, 2 and 7).

## Left open
next: the write-up, deferred by the owner to 2026-08-07 — campaign README
beside the rows, CLM-0013 corrected (its "one draw is enough" holds only where
it was measured, top rung against the easy set, and its collapse-to-mode
mechanism does not exist at the floor: 0 of 20 tasks collapsed on srv1 against
5 of 20 on the top rung), a claim record for the selectivity result, #119
answered with a default, and PR #188's body updated.

Then: apply t20's repair to d1 itself once srv2 is finished with that tier —
deferred only because changing the digest refuses a resume to every existing
run directory.

Running unattended overnight, both resume-safe, logs in the job tmp dir:
srv1 repeats for llama3.2:3b and qwen2.5-coder:7b at 0.7 and 1.0 (the two
models still on one run per cell) then a selectivity pass over every srv1
sweep; srv2, queued behind the campaign's own exit, T=1.0 for 7b / yi-coder:9b
/ 14b to test whether "hot helps only the weakest" generalises up the ladder,
then two repeats of the d2 sweep, whose +2 currently rests on a single run.
