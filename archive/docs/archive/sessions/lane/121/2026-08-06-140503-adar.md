---
record: session/1
lane: lane/121
agent: adar
started: 2026-08-06T14:05:03Z
---

# Session — lane/121 — 2026-08-06

## Did
Owner redirected the process: the top-rung-first run measured a ceiling, not breadth. Rebuilt as a two-rig campaign — campaign.py walks every model smallest-first up a difficulty ladder (d1 = bundle set; new tiers d2/d3, 12 tasks each, selftest green) to its real-logic-failure tier, then sweeps K=8 serial draws, no early exit, pass@<=1..8 from one run. srv1 campaign COMPLETE (4 models, all stop at d1; breadth pays +3..4 tasks by k=3..5; sampled draw0 runs ~1 task under greedy). srv2 INTERRUPTED for a VPS restart mid-model-7 (gpt-oss:20b sweep 45/180 rows; 6 models complete; its failures are mostly cap truncations from reasoning tokens, classified separately). All rows committed; campaign.py is resume-aware (done_keys + run.json identity), so re-running the identical srv2 command continues where it stopped.

## Left open
next: Re-run the srv2 campaign command verbatim (see the #121 pin comment) to resume at gpt-oss:20b; after qwen3:30b-a3b, qwen3-coder:30b and qwen3-coder-next finish, analyze per-(model x tier) sweet spots, write the campaign record, extend or supersede CLM-0013, update PR #188 body.
