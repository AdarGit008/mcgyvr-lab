---
record: session/1
lane: lane/184
agent: adar
started: 2026-08-06T14:02:16Z
---

# Session — lane/184 — 2026-08-06

## Did
#184 built in one pass: the bundle rig now keeps every reply body (first attempt and retry, with retry_stop_reason recorded so a capture replays), tools/replies/pin.py joins every captured reply to its row and pins the parser's verdict in records/corpora/worker-replies/golden.json, and tests/test_reply_corpus.py recomputes the whole document offline each suite run. Seeded from the 2026-08-06 breadth run: 120 replies, 119 parses, 1 refusal (incomplete-reply, cap-truncated t03 draw) kept as gold per ADR-0016. test_worker_reply.py's 'real shapes' docstring claim withdrawn; its fixtures are constructed and now say so. No model was dispatched: the rigs were under test and nothing here needed one. Scrub coverage confirmed by reading scrubcli.mjs (records/ is scoped whole) and live by the pre-push hook. #174's shape has no captured example yet; the mechanism now stands.

## Left open
next: PR open; if merged, #184 closes noting the breadth rig was already compliant and the bundle rig now is. First future sweep grows the corpus via tools/replies/pin.py.
