---
record: session/1
lane: lane/299
agent: adar
started: 2026-08-17T18:06:01Z
---

# Session — lane/299 — 2026-08-17

## Did

**Two readings of ADR-0019 corrected, both of which pushed the same way: toward treating small measured gains as worthless.** Neither is a change to a decision. Both are places where a reader took something from the record that the record declines to say.

**There is no 5pp bar, and there never was.** `5pp` appears three times in ADR-0019 and none of them is a target: the **quantum** at n = 20 (`1/20`, an arithmetic property of a 20-task set), one **row** of a five-row sizing table, and the **outcome** at n = 400 ("resolves +5 to +8pp"). D2 states fitness over a free variable — *"the bench is fit for a bar `b` iff `MDE <= b` and `d < b`"* — and D3 removes a fixed bar entirely for the cheapest lever class: *"for a Class R lever the bar is whatever the bench resolves — there is no floor of 3pp, or of any other inherited number."* Declining to fix one number is the whole of D3's argument, since a prompt line and a fine-tune cannot be judged by the same figure.

The reading entered at `docs/identity-surface-2026-08-16.md` (#276): *"About 1.3x, and still short of 5pp on the arm's own psi."* That half-sentence has since been quoted as the reason n = 400 is not worth buying. Owner's note, 2026-08-17: the number was supplied in conversation and never decided. Corrected at the source with a block quote rather than silently, so a reader who arrives from a citation of the old sentence finds out what happened to it.

**What survives is the argument next to it**, and it is untouched: 400 buys ~1.3x on the one stratum that resolves, nothing on the seven that resolve at no n, and its value turns on whether new material is more responsive than the 88% frozen corpus — #224's unmeasured number, gated by #272. That case rests on cost and an unmeasured input. It never needed a bar.

**Individually unresolved is not jointly null.** Five levers at +2pp each, every one below an MDE of 7pp, is plausibly a system that gains 10pp — which this bench resolves comfortably. Reading each non-rejection as a zero and summing the zeros is not licensed by the exact test.

D4 already guards half of it and the guard has held: *"a lever may be retired only by NULL. Never by UNDECIDED, and never by a small number"*, together with this record's own finding that **every** bundle contrast on its table is UNDECIDED. So the arithmetic error has not been committed; nothing has been retired. What was missing is the positive half, and the amendment states it: a set of individually unresolved, same-signed levers is a live hypothesis, and declining to measure the combination is a choice about cost rather than a conclusion about the system. #233 owns running it, and it is the cheap kind of question — a different contrast over material already authored, not more material.

**Two assumptions are named with it**, so the test is not mistaken for a foregone conclusion: the levers must be **same-signed** (a +2pp and a −2pp cancel) and **non-redundant** (two levers fixing the same failures stack to one). Neither is measured on anything we hold. That argues for running #233, not for assuming either answer.

**Swept for other citations.** One remains, in `records/sessions/lane/266/2026-08-14`, and it is left alone: it says twelve strata cannot show *a* 5pp effect at any n, which uses the figure as an example magnitude rather than as a bar. Session records are history here and are not rewritten.

`make check` green.

## Left open

next: #233's combination phase is now the issue this record points at and it is unowned in practice — it needs a design saying which subsets are worth running and in what order, because "every subset from two levers to the whole system" is 2^k contrasts and the same-sign screen is the cheap way to prune it; separately #299 makes no bar, so whoever sizes the r2 re-run still has to choose `b` per lever class under D3 rather than inherit one
