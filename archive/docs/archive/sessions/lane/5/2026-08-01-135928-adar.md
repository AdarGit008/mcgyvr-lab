---
record: session/1
lane: lane/5
agent: adar
started: 2026-08-01T13:59:28Z
---

# Session — lane/5 — 2026-08-01

## Did
Enabled merge protection on main rather than recording a private case. The skip precedent (the baseline's own demo repo) rests on CONTRACT.md's third rung — private repo, free plan, nothing bindable — and this repo is public, so it sits on the second rung where a required check plus strict up-to-date is free. Prevention was sitting on the table unclaimed. Ruleset is committed at .github/rulesets/main.json and was created from that exact file: required checks (baseline, test) pinned to the Actions integration id, strict up-to-date, conversation resolution, no force-push, no deletion. Approvals are 0 because GitHub forbids approving your own PR, so on a solo repo any higher number is a deadlock rather than more review. bypass_actors is empty on purpose — CONTRACT.md's Layer 0 reasons that admins can always bypass so document the valve, but that describes classic protection; rulesets grant no implicit exemption, so listing one would have created a standing valve nothing actually imposed. The valve is disabling the ruleset: admin-only, audit-logged, deliberate. Named cost: if a change breaks the workflow so a required check never reports, every PR blocks including the fix — recovery is disable, merge, re-create from the file. Verified live, GOV-01/02 now PASS on a read of enforcement.

## Left open
next: Ship #12 (config reference generated from the schema) to close E1. Still open for a decision: the naming convention's orch token has nowhere to live, since the schema gives orchestrator and verifier their own blocks rather than named tiers. Also note the two open dependabot PRs now need bringing up to date before they can merge — that is the new rule working, not a regression.
