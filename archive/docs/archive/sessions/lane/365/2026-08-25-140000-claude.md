---
lane: 365
date: 2026-08-25
session: 1
---

# Session — lane/365 — 2026-08-25 (first)

**The tables flip: real commits become the tasks, and the orchestrator's first
end-to-end loop is specified as five RED tests before a line of it exists.**
No rig time.

## Did

- Owner decisions recorded on #365: dev on the #302 plan is paused; admission
  is code+test commits from Adar's own repos (657 in `tasks_admitted`, 475
  with an issue body as spec); a multi-file commit stays one task and
  `decompose` owns the split; no API fallback; judge = output vs issue body,
  blind, at the month's review. #365 blocks every other open lane.
- The corpus lives outside every repo: `~/claude/session-mine/sessions.sqlite`
  (688 sessions, 2,814 commits with diffs; `mine.py` + `issues.py`; README
  carries the join rules and the verifier corrections).
- `tests/test_missions.py`: five tests, one per #365 item — task loader,
  live proposer, attempt, runner (no-API rule), record (no foreign verdict).
  Each loads `tools/missions/<name>.py` by path and fails by name while the
  file is absent. All five red on this commit, each for its own reason.
- `docs/plans/302/README.md`: dated amendment block recording the pause,
  quoting the owner.
- `tools/missions/` (off-SURFACE, ~2.9k lines): `tasks.py` (view row → `Task`,
  `checkout` at the parent sha, named refusals, `iter_tasks` over the DB),
  `propose.py` (`parse_proposals` → proposals or `Refusal`, never an exception;
  `LiveProposer` conforming to `decompose.Proposer`; `runner_dispatch` over a
  pool rung), `attempt.py` (`narrow_test_command`; `make_attempt` = prompt →
  dispatch → parse → `MissionSandbox` → `Gate.run` with the narrowed command as
  acceptance → `escalate.judge`; the child's test files are written after
  `ChangeSet.detect` so they never enter the gated diff), `run.py`
  (`require_local_only` → `NoApiFallback` naming every credentialed source;
  `run_task` = checkout → attach → index → decompose → `escalate` per contract →
  commit each delivery into the worktree so the next climb sees it → whole-tree
  test → record; `INTENT_LIMITS` states what the campaign cannot measure),
  `record.py` (`mission/1`: identity, intent, spec and output side by side;
  a verdict-shaped key outside `output.gate` → `VerdictNotTheGates`).
- Built by five parallel builders against the tests, then three verifier
  rounds against the `src/` contracts; 15 findings fixed (host `src/` leaking
  into the sandbox via the editable `.pth`, acceptance slot filled for
  `type_annotation`, dropped refusal reasons, `RunnerError` escaping
  `escalate`, a constant-name collision the four-lenses test refuses…).
- All five tests green; full suite 1,999 passed; ruff, bare mypy and strict
  mypy over `tools/missions` clean; `product.py --check` digest unchanged.

## Left open

The pool config (`mcgyvr.yaml`, owner's — pending the rig tests in flight) and
one end-to-end run on srv2 leaving a record under `records/missions/<sha>/`
(#365 acceptance item 2). Two properties the month's review must read with
`INTENT_LIMITS`: a single child test asserting every file's behaviour cannot
pass before the last contract, and a parent with no tests yet has no test
command.

next: owner drops `mcgyvr.yaml`; then `uv run python tools/missions/run.py --db ~/claude/session-mine/sessions.sqlite --sha <one admitted mcgyvr sha> --config mcgyvr.yaml --dry-run`, then the same without `--dry-run`
