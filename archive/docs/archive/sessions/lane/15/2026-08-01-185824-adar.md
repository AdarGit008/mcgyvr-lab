---
record: session/1
lane: lane/15
agent: adar
started: 2026-08-01T18:58:24Z
---

# Session — lane/15 — 2026-08-01

## Did
E2 #15: decomposition catalog delivered — data/task-catalog.json + src/mcgyvr/catalog.py. Nine task types carried, eight inherited ones removed with their reasons kept in the file. Entries state a guarantee, a starting family (not a rung — rung names are per-machine), and required evidence; `deterministic` is derived from the family. Made load-bearing: a contract whose type needs command-only evidence is rejected with no acceptance commands. The 'data not code branches' test caught a real defect — the contract schema had frozen the vocabulary into an enum at import; it now resolves per validation. Also force-included data/ into the wheel, which was already latent-broken for the capability table. 36 tests, suite 519 green.

## Left open
next: #50 decompose a prompt into contracts is now unblocked — E7's last child, and it was waiting on exactly this vocabulary plus #14's schema. #16 (risk floors) and #18 (authoring guide) also consume this.
