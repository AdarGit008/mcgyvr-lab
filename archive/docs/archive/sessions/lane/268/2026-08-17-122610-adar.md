---
record: session/1
lane: lane/268
agent: adar
started: 2026-08-17T09:26:10Z
---

# Session — lane/268 — 2026-08-17

## Did

**Paused on owner direction, with the evidence committed rather than dropped.** The lane opened to close #268 — extend the behavioural scan, rule on each family, and put a screen at admission that catches a re-skinned duplicate. It stops after the scan. Owner's reading, 2026-08-17: the check treats the symptom. We author the duplicates, and a 22,000-execution detector built before the emitter is fixed pays for the same defect twice — once to emit it, once to find it. The stronger form is authoring metadata: a problem that records what computation it is a statement of turns duplicate detection into a lookup at admission, and the sweep below is what a lookup would have to reproduce. The issue is paused until the authoring side (#225) is dealt with.

**The scan's own prune hid two of the six families it was built to find.** `families.py` paired on shape *equality* — same arity per declared function. The relation cross-execution actually tests is directed: a source stands in for a target when it supplies every function the target's acceptance imports, which is containment of the target's arity multiset in the source's. `b333-pace-split` declares `pace_list/1` and `pace_of/2`; `b302-stock-take` and `b277-fuel-legs` each declare one function of arity 2. Equality never compared them, so `b302 ⊂ b277 ⊂ b333` — two of the six families the issue's body reports from its own earlier sweep — was invisible to the tool that found the other four. Under containment, `b333`'s reference passes both their acceptances. `pool-ts` also joined `ARMS`: the scan declared three of the four corpora and the first acceptance item asks for both arms of both.

**Eight families on `bench-py`, and three of them are not duplicates.** The sweep is committed as `tools/bench/families.json`. Five are containment — the same computation stated twice, which is one slot's evidence in two slots. Three are something else: a *different* function that passes only because the task's own acceptance never tests the case that separates them. `b430-sort-by-len` is the clearest — all five of its assertion inputs give the same answer under sort-by-length and under plain `sorted()`, and `def sort_by_len(words): return sorted(words)` against its real `accept.py` prints `ok`. That is a false pass, not a duplicate: it inflates every rate already measured and would inflate r2. **Filed on #269 with executed counterexamples** rather than carried in a paused issue, because it is that issue's subject — nothing states what a pass here can distinguish — and because it does not decay while #268 waits.

**The classification is not mechanical, and the record says so.** Which of the eight are containment and which are weak acceptances came from reading both references and hand-building counterexamples. Three are proven by execution; five are my reading. A `differ.py` that manufactures the counterexample was designed and deliberately not built — the naive form does not work, because `unit_price(-7, 2) = -4` and `fuel_legs(-7, 2) = 0` disagree while being the clearest real duplicate in the corpus. These acceptances do not state their input domain, so "the two references disagree somewhere" classifies almost everything as weak. A probe can produce the counterexample; ruling whether that input is in-domain needs the prose. That design is in the issue for whoever resumes.

**What the sweep cannot see is committed beside what it found.** For the scan to catch a weak acceptance, the acceptance must admit a function written for an *unrelated* problem — the lowest bar there is. The 253 `bench-py` tasks with no finding are not "checked and clean"; they are "no unrelated function happened to pass". The prune is coarser than the corpus: it matches on parameter count and discards the types the contracts declare, so `b430`'s `words: list` matched `b267`'s `aliases: dict`. Tightening it to types would cut false pairings *and* would have removed the finding — the looseness is doing work until weak acceptance has its own instrument, and `families.json` records that rather than leaving it to be rediscovered.

**A per-arm family count is not a per-arm quality read, and I read it as one before checking.** `bench-ts` yields 7 families where `bench-py` yields 8, and the first reading — carried into a comment on #268 and into #295's body before it was tested — was that `b430`'s TypeScript acceptance separates sort-by-length from plain sort while its Python one does not. It does not. Plain `.sort()` passes `b430`'s `accept.mjs` exactly as `sorted(words)` passes its `accept.py`, on the same five inputs; both arms are equally weak. What differs is the **stand-in**: the ts `aliasNames` is `Object.keys(aliases).sort()`, which given an array returns its indices and fails, where python's `sorted(aliases)` takes a list as happily as a dict. The arm gap measures python's duck typing, not the material. Corrected in `families.json` rather than quietly deleted, because the failure mode — reading a detector's per-arm yield as a per-arm property of the corpus — is one a later reader can repeat.

**The drift the retracted claim was cited for stands on other evidence.** 20 of 257 python references declare nested functions against 2 of 257 on the ts arm, and ADR-0023 clause 4 found the same class independently: 104 bench and 106 reserve python checkers accepted only `ValueError` while their TypeScript twins used `assert.throws(fn, Error, ...)`, which any subclass satisfies, on contracts that never name a type. Both are two renderings of one prose authored apart, and both are #295's.

**The pool contrast is untouched by that correction.** `pool-py` and `pool-ts` return the *same* single family, and 1 family in 499 pool tasks stands against 8 in 257 bench tasks. The pool gate requires a no-op and an echo stub to fail before admission and the bench gate has no equivalent. Suggestive only — different generators, different eras.

`make check` green; 6 new tests in `tests/test_bench_families.py`, which pin the prune's contract rather than the scan's output, the full cross-execution being ~22k runs.

## Left open

next: the disposition of the 8 families and 3 weak acceptances is unruled and they are in the bench today — an hour of rulings, not a lane, and it is owed before r2 dispatches or the acceptance has to be recorded as knowing; then #268 resumes after the authoring side, with the emitter recording the fingerprint that makes this sweep a lookup
