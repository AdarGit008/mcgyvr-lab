---
record: session/2
lane: 220
agent: adar
started: 2026-08-09
---

## Did

Session 1 delivered the audit. This session shipped the restructure the audit's
C5 recommended, after the owner approved it across three review rounds.

**ADR-0018 — one bench, every lever, and the whole system measured.** Extends
ADR-0017 without disturbing P1–P3. Four decisions: every lever is an arm on one
bench and a measured null closes an arm (no lever dropped for a small gain);
effects are **comparable, not addable**, so composition is measured rather than
assumed; nothing is trusted uncommissioned and a **round** — one pinned product
revision — is the unit; the bench is parameterized by target tier so a new model
is a re-run. Records why a strict sequential chain and a new repository were both
rejected.

**The tree, rewired as native GitHub dependencies.** The blanket block is gone:
**52 of 74 open issues are now unblocked**, against 4 before. Trunk is
`#229 → #230 → (#227, #217) → #113 → (#225, #224) → #231`. Arms hang off the
gate; **#233** consumes them; consumers are #162/#16, #190, #61. Everything else
is parked — open, unqueued, no edge to the graph.

**Three issues opened.** **#231** the commissioning gate (null drift, recover
CLM-0017's known +20pp effect, pinned round, declared bounds, second tier).
**#232** the decomposition arm's missing input — `decompose()` eats a prompt plus
a repository and the bench holds contracts, so the arm had no material and no
issue defined any. **#233** the combination phase, staged A/B/C over a 2⁶ space.

**#110 closed** (all four children already done). **#111** promoted to the
trunk's epic. Scope amendments on **#113** (score via `Gate.run`, cost axis,
combination arms, single-tier mode), **#225** (widen by addition; one generation
run split by hash; its own "disposable scaffolding" language is now wrong),
**#229** (size on the responsive fraction; the bar is two things), **#230** (the
guard moves to `pin.py` — as filed it would not have prevented the contamination
it was filed for).

**#219 closed** as superseded on owner direction.

## Left open

Three review rounds found **eighteen** gaps in the proposed structure before it
was wired, which is recorded in the audit doc. Two would have let the original
failure through again. A third is worth remembering for its shape: **#225's own
text still calls its output "disposable scaffolding" when the restructure makes
it the permanent bench** — the audit's own finding, reappearing inside the
proposal the audit produced. The scope amendment says so; the issue body itself
has not been rewritten.

Not done here and deliberately: rewriting issue bodies beyond the amendment
comments. Several now carry a comment that contradicts their body text (#225 most
sharply). A body-rewrite pass is a separate job and should be its own lane.

next: work the trunk from #229, which is unblocked and needs no rig time.
