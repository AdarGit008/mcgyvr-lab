---
record: session/1
lane: 220
agent: adar
started: 2026-08-09
---

## Did

Ran the #220 audit: all 69 open issues read in full against ADR-0017's P1–P3.

- **`docs/floor-audit-2026-08-09.md`** — the deliverable. Method, a floor /
  ceiling / neither verdict for every open issue with a one-line reason, the
  four corrections to landed decisions, and 13 conflicts (C1–C13) each with
  what settles it. Counts: 30 floor, 4 ceiling, 35 neither.
- **ADR-0017 amended.** Its consequence *"Nothing in the repo can currently
  measure a small model getting better at work mcgyvr would actually hand it"*
  is false. `tools/bundle/tasks/` (20 JS/TS) and `tools/bundle/python/tasks/`
  (20 Python) are real contracts with runnable acceptance against checked-in
  references, held out by construction (`admit.py:104`), measured on
  qwen2.5-coder:3b at 45/55/50/45% (CLM-0012) and 35/50/55/65% (CLM-0017) —
  both inside the band. `measure.py:202` is explicit that the `d1` rung *is*
  the JS/TS set byte for byte, so ADR-0017's own 50.0% anchor is this
  instrument under another name. What is missing is power and coverage, not
  the instrument. The knock-on bullet claiming Python is unmapped is marked
  false. Amended visibly rather than rewritten.
- **#227 filed** — the Python arm has no tier name, so the breadth rig cannot
  serve it. `bundle.PYTHON` already exists and the rig already uses it for
  `pool-py`, so it is a tier name, not a mechanism.
- **#219** — superseded banner on the body; the run design survives, the tune
  target and eval set do not.
- **Conflicts recorded on** #16, #17, #71, #113, #119, #149, #152, #162, #190,
  #211, #217, #221, #222, #224, #225, and a summary on #220.

Corrections to landed decisions, per #220's third acceptance item: ADR-0017's
floor-instrument bullet; #190's front-door acceptance and its inheritance of
#189's empty verdict; `attempts=1` as a ceiling-first default (#152); #219
still reading as live.

## Left open

**C5 is the one thing not actioned, and it needs the owner.** Five nodes block
all 64 remaining issues, and frozen behind them are the cheapest floor raises
in the tree — #198 (whose instrument *is* the 40-task set above; one line was
worth +4 tasks in 20), #81 (zero tokens, no measurement dependency), #168
(already measured), #126, and #217 (a rig defect that silently holes every
sweep #225/#224/#226 will run). P1 does not say a floor raise must wait on a
floor *measurement* when the change is cheap and ships to every rung.
Recommendation on #220: unblock those five, keep #221/#222 blocked. The
dependency wiring was the owner's, so it stays the owner's.

Also noted and not actioned: #113's annotation in E13 still says it is blocked
on #21/#25, both landed; #50 closed on 2026-08-03, so #221's carved-out
alternative ("a reasonable way to measure whether decomposition raises the
floor, once #50 lands") now has its precondition met.

next: owner decides C5; then #225/#224 re-scoped against the instrument that
already exists, with #227 as the prerequisite for the Python arm.
