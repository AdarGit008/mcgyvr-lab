---
record: session/1
lane: lane/201
agent: adar
started: 2026-08-07T07:03:33Z
---

# Session — lane/201 — 2026-08-07

## Did
#201's sweep is done and lands as `docs/test-witness-sweep-2026-08-07.md`. All
47 test modules reviewed against the rule — telemetry is what we read, the test
is the independent validator — by test name and by every assert line, with full
reads wherever a name or an assertion suggested a report-only or a proxy
assertion. Every module carries a verdict in the coverage table, so the sweep's
reach is legible rather than implied by silence.

Seven findings — six test-side, touching eight modules; 31 clear; eight named as
exemplars or references.

F1 is the only live instance of mode 2 and is #200's test with the names
changed: test_availability.py:204 asserts `elapsed < 0.6` for six 0.2s probes,
and nothing in `Availability` records concurrency, so the stopwatch is the only
assertion that could catch a serial pool. The margin is looser than #200's, so
it has not flaked yet; the kind of assertion is the same. The fix is the barrier
#200 already built, test-side — no operator has a use for probe overlap, and
unlike #200 there is no shipped bound for the figure to mean anything against.

F2 is the largest, and mode 1 at its purest: test_orchestrator_cache.py:103 and
:122 assert only on fields of one `CacheStats` the cache filled in about itself,
so a cache that reparsed all four files and reported `reused=4` passes both. The
module's own docstring names exactly this risk — "a cache that silently does
nothing looks exactly like a cache that works" — and then the test takes the
number on faith. Two module-level seams already imported into cache.py make it
mechanical: `read_source` (:238) is called for everything except `reused`, and
`index_source` (:259) only for `rebuilt`, so both counters have a zero to prove.
That is the `Counting` probe from test_availability.py:137 one module over.

F3 and F4 are the smaller mode-1s. test_orchestrator_index.py:66 asserts
`files_indexed == len(index.files)` — one object agreeing with itself — plus a
`>= 0.0` that cannot fail, when the fixture wrote the file and knows the exact
answer; the cache suite's `cached == fresh` comparison inherits its trust from
there. `plan.spent == sum(r.estimated_tokens ...)` (read.py:57,
context.py:329) checks a sum against its own addends and never against the text
sliced.

F5 is mode 1 one tier up from the code. `ESTIMATE_RESERVE = 0.32` is documented
in CLM-0011, in preflight.py:50-56 and in its own test's name as the worst
vocabulary's p05 from `records/measurements/tokens-2026-08-03/summary.json` —
and nothing reads that file. `test_structured_and_preflight.py:187` asserts
`0.30 <= x <= 0.35`, a restatement of 0.32 with room either side, which catches
the drift its docstring names (a margin quietly rounded to 0.25) and not the one
that matters. test_claims.py:98 already holds this line for vendored evidence
and says why: without the check, it is a directory someone said came from
somewhere. F6 is a line to delete.

F7 is neither mode, and the sweep found it by being taken in by it.
`estimate_tokens`' docstring (read.py:378) still says "What the proxy's error
actually is remains #117's to measure", and CHANGELOG.md:1066 repeats it — but
#117 closed as completed on 2026-08-03, measured over 2,387 units across three
vocabularies, and the band was fed back as the reserve. **The first draft of
this sweep read that docstring, believed it, and wrote F5 up as "the error band
is unmeasured".** The baseline check caught it (DIV-02, a next: pointing at a
closed issue), and re-deriving turned a wrong finding into a better one. A
shipped comment contradicting a shipped claim is #66's class, and the fact that
it successfully misled a review explicitly hunting unmeasured proxies is the
argument for #66 being separate work.

Two answers the issue asked for and did not presuppose. **Mechanisable: only
the stopwatch shape.** A wall-clock delta reaching an assert is a two-line AST
rule with no false positives, and across 47 modules it fires exactly once —
every other timing call is a deadlock guard, a fixture or an injected clock. "Is
there an independent witness" is not syntactic: F2's failing test and
test_sandbox_image.py:176's passing one are the same shape and differ only in
whether a second assertion observes the same fact by another route. A linter
would miss F2 or flag most of test_route.py, and one with that error rate gets
muted. **Shipped telemetry: none of the seven.** #200's answer was "both"
because #185 had made the bound host-wide, giving an operator a question the
telemetry could not answer. `CacheStats`, `IndexStats`, `plan.spent`,
`PromptBuild.tokens` and `ESTIMATE_RESERVE` are already the right numbers to
ship — several of them, as F5 shows, the product of measurements this repo has
already done properly. They were simply never validated by the suite.

Also recorded, so it does not get "fixed": test_runner.py's reads of
`capacity.usage()` are a *collaborator's* record, itself validated in
test_capacity.py:198, which is a legitimate composition rather than
self-certification.

Filed separately from the modes, because it is a reporting gap rather than a
witness gap: three modules skip silently when a tool is absent
(test_python_adapter.py's three ruff skips, test_bundle_ladder.py:544,
test_structured_and_preflight.py:67). The ruff ones are the gate's lint and
format rungs, and the gate itself distinguishes "the tool was absent" from "the
check passed" as a first-class environment issue — the suite does not.

Every finding is filed, so none of them rests on the document being read: #204
(F1), #205 (F2), #206 (F3, F4, F6 together), #207 (F5). F7 went as a comment on
#66 and the silent skips as a comment on #4 — both have a subsystem home
already, and minting duplicates beside them is the contradiction DIV-02 exists
to catch.

No code changed this lane. Reviews land as a document and a record, which is
how #172's and #175's landed.

## Left open
next: PR #203 open. If merged, #201 closes. The fixes are their own work and are deliberately not in this lane — all filed: #204 (the probe stopwatch) and #205 (the cache witness) are one change each and independent; #206 carries the three counters checked against their own addends; #207 ties ESTIMATE_RESERVE to the measured p05. F7 rides #66 and the silent skips ride #4, as comments rather than new issues.
