---
record: session/1
lane: lane/197
agent: adar
started: 2026-08-07T19:51:12Z
---

# Session — lane/197 — 2026-08-07 (seventh record)

## Did
Batches 8–11 (p282–p431): fifteen generators, ~80 more domains, 105
function_implementation + 45 bug_fix. The pool stands at **419**,
re-admitted whole and pinned at every batch. `make check` green (1138).

**The 14B sweep answered the hard-tail question, and the answer is that
the tail is hard rather than broken.** Same instrument,
`qwen2.5-coder:14b` over the 269-problem pool, TS arm, 807 rows. On the
189 problems both sweeps saw: the 7B solved 53, the 14B solved 94 —
doubling the worker **rescued 48 problems the 7B never once passed**,
with 7 going the other way at three draws. 88 of 189 resisted both.
Greedy climbs 19.0% → 32.0% smoothly rather than saturating. A tail no
larger model could touch would have meant problems that are wrong; this
is the shape a discriminating instrument should have, so **no
rebalancing**. Full reading in the sweep's README.

Two secondary findings recorded there: the `bug_fix` advantage roughly
doubles at both model sizes, so the ~2× gap belongs to the task types and
not to the worker; and parse refusals rose 6 → 47, meaning the bigger
model breaks the output contract *more* often — a reply-format finding,
with the refusals pinned as corpus under ADR-0016 for whoever revisits
`worker/reply.py`.

Also noted and left open rather than chased: batches 6–7's 80 problems,
which only the 14B swept, came out harder than the pool average (20/80
against 42.4% overall). Nothing here separates "harder problem" from
"harder to state unambiguously"; a 7B pass over those 80 would settle it
cheaply.

Corpus re-pinned after the sweeps exited: **11,130 replies, 389 refusals
kept as gold**.

Verification note: one generator reported writing literal NUL bytes
through a `sed` escape and repairing them. A NUL is invisible in a text
reader and would make a contract silently unparseable, so the whole tree
was scanned independently at every batch since. Clean each time, and the
warning is now standing text in the generator brief.

## Left open
- 419/~500. Next ids **p432+**. Batches continue on this branch.
- `build_dataset.py` still predates paired arms and now has real pool
  passes on both arms to trip over. The next consumer-side task.
- type_annotation problems still deferred on anti-triviality design.
- The 80-problem difficulty question above.

next: batches to ~500, then open the PR for review.
