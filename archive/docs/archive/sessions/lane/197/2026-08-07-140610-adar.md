---
record: session/1
lane: lane/197
agent: adar
started: 2026-08-07T14:06:10Z
---

# Session — lane/197 — 2026-08-07 (fifth record)

## Did
Batch 5 (p162–p201): four generators, twenty more domains, 28
function_implementation + 12 bug_fix, all forty finished. The pool stands
at **189**, re-admitted whole and pinned. `make check` green (1138 tests).

**The first pool sweep against a served worker.** Ten problems across the
pool's range, both arms, `qwen2.5-coder:7b` on srv1: the seam dispatches,
captures and scores through the unmodified rig — no parse refusals, no
dispatch errors. Re-pinning the reply corpus afterwards put
`info_string: "python"` into `golden.json` beside `ts` for the first time.
**#197's premise — every verified pass in the corpus is TypeScript — is no
longer true**, and it stopped being true by ordinary measurement exhaust
rather than by a curation step, which is what ADR-0016 asks for. Sixty
replies pinned from the probe, eight verified passes, four per arm.

**The sweep found a defect in the seam I had landed an hour earlier.** The
first attempt recorded 157 task digests for `pool-py` against 149 for
`pool-ts`: `load_tier_tasks` served the tier *directory*, and a batch was
being generated at the time, so unadmitted candidates entered the run's
identity. That splits a tier into versions nobody chose — the d1r hazard
arriving through a different door. Pool tiers now serve exactly what
`admissions.jsonl` pins; a named-but-unadmitted task is refused with that
as the reason; the contaminated run directories were discarded rather than
kept with a footnote, and the committed rows are from the re-run.

Worth stating plainly: the seam's selftest was green and the seam was
still wrong. Only dispatching against a real worker produced the row count
that exposed it.

## Left open
- 189/~500. Next ids **p202+**.
- The probe says the pool is materially harder than d1, where this model
  family sits near its ceiling: greedy 1/10 per arm. Good for
  discrimination, a caution for corpus yield, since the corpus grows with
  passes and not with draws. Whether the difficulty distribution wants
  rebalancing is a question for a full sweep of all 189, not for a probe.
- `build_dataset.py` still predates paired arms (shared id pools both arms'
  replies under one per-task cap; prompt rebuild needs the arm's contract).
  It now has real pool replies to trip over.
- type_annotation problems still deferred on anti-triviality design.

next: batch 6 (p202+); and a full pool sweep on both arms once a rig is
free, which is what would turn the probe's yield question into a number.
