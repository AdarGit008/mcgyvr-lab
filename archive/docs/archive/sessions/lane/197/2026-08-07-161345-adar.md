---
record: session/1
lane: lane/197
agent: adar
started: 2026-08-07T16:13:45Z
---

# Session — lane/197 — 2026-08-07 (sixth record)

## Did
Batches 6 and 7 (p202–p281): eight generators, forty more domains, 56
function_implementation + 24 bug_fix. The pool stands at **269**,
re-admitted whole and pinned. `make check` green (1138 tests).

**The full sweep ran: all 189 admitted problems, both arms, greedy plus
two sampled draws, `qwen2.5-coder:7b` on srv1 (ts) and srv2 (py). 1,134
rows, zero dispatch errors.** It ran concurrently with generation, which
is only safe because pool tiers now serve the manifest rather than the
directory — the fix earning its keep the same day it landed.

Three things it settled:

- **The probe was wrong by a factor of two.** Ten problems sampled by id
  spacing read 1/10 greedy; the full 189 read 19.0% (ts) and 22.8% (py).
  A yield estimate on the probe would have misled the next fine-tune.
- **`bug_fix` passes at about twice `function_implementation`'s rate**
  (26.3 vs 13.1 ts, 33.9 vs 13.4 py). The pool is 70/30 by count, so the
  two types will feed a corpus far closer to even. Anything sampling the
  corpus by type should read the sweep, not the composition.
- **The arms are equally hard in aggregate and disagree per problem**:
  53/189 pass in each, only 34 in both, 19 in ts alone, 19 in py alone.
  **72 distinct problems produced a verified pass** — against a corpus
  whose entire verified-pass history answered 20 until today. Re-pinning
  brings it to 10,323 replies, 665 tagged `python`.

An operational gotcha, recorded in the sweep's README because it cost a
test run: `pin.py` walks every run directory, and a sweep in flight has
candidates whose rows are not flushed, so the reply corpus cannot be
pinned or checked mid-sweep and `test_reply_corpus.py` fails for the
duration. `pin.py` is right to refuse — an orphaned candidate is what an
interrupted run leaves — so the caller waits. Nothing in #184's contract
changed.

## Left open
- 269/~500. Next ids **p282+**.
- 117 of 189 produced nothing in three draws. That is a distribution
  question, not a defect — a pool a 7B always solves would not
  discriminate. Worth asking against a stronger worker before rebalancing
  anything: srv2 serves several (qwen3-coder:30b, qwen2.5-coder:14b,
  deepseek-coder-v2:16b) and none have been pointed at the pool.
- `build_dataset.py` still predates paired arms, and now has 208 verified
  pool passes across two arms to trip over. This is the next real
  consumer-side task.
- type_annotation problems still deferred on anti-triviality design.

next: batch 8 (p282+); a sweep of the 269-problem pool against a stronger
srv2 model, which is what would say whether the hard tail is too long; and
the build_dataset.py paired-arm work.
