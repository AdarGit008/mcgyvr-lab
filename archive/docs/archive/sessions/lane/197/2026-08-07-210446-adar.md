---
record: session/1
lane: lane/197
agent: adar
started: 2026-08-07T21:04:46Z
---

# Session — lane/197 — 2026-08-07 (eighth record)

## Did
Batches 12 and 13 (p432–p511): eight generators, forty more domains.
**The pool closes at 499 distinct problems, 998 arms** — 349
`function_implementation` and 150 `bug_fix`, roughly 250 domains with
none repeated, every problem in both TypeScript and Python. That is
#197's ~500 target, reached and pinned. `make check` green (1138).

**PR #209 is open** against main.

## The shape of what landed
- Sourcing was searched before building and nothing was adoptable:
  `docs/problem-pool-prior-art-2026-08-07.md`, with queries and URLs, so
  the absence claim carries provenance and an expiry.
- `tools/problems/admit.py` is the gate everything entered through:
  contract via the real loader, executed selftest, failing-first for
  `bug_fix`, anti-triviality stubs, assertion floor, HumanEval
  entry-point blocklist, lexical near-duplicate screen. Digest-pinned in
  `admissions.jsonl`; repairs supersede under a new id.
- `tools/breadth/measure.py --tier pool-ts|pool-py` is the seam, and it
  serves the manifest rather than the directory — a distinction a real
  sweep forced after a green selftest had passed over the bug.
- Two sweeps (7B both arms over 189; 14B TS over 269) and a corpus that
  now carries Python: 11,130 replies, 665 python-tagged.

## Left open, and it belongs to another lane
- `tools/finetune/build_dataset.py` predates paired arms: a shared id
  pools both arms' replies under one per-task cap, and the prompt
  rebuild needs the arm's contract. It now has real pool passes on both
  arms to trip over. This is the next consumer-side task and the
  README says so.
- `type_annotation` problems deferred pending an anti-triviality design
  for checkers that read their own source text.
- Batches 6–7's 80 problems came out harder than the pool average under
  the 14B (20/80 against 42.4%). Nothing separates "harder problem" from
  "harder to state unambiguously"; a 7B pass over those 80 would settle
  it cheaply.
- Ids p131, p141, p144–p151, p160, p161 stay retired. A gap means work
  that never landed, never work withdrawn.

next: review of PR #209. After merge, the dataset-builder lane is the
one that turns 499 problems into training data.
