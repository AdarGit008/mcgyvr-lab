---
record: session/1
lane: lane/197
agent: adar
started: 2026-08-07T11:57:23Z
---

# Session — lane/197 — 2026-08-07 (third record)

## Did
Batch 3 (p082–p121): four generators over twenty more fresh domains, 28
function_implementation + 12 bug_fix. Whole-pool re-run from this
session: **121/121 admitted and pinned**. Sixty distinct domains used
across three batches, none repeated.

The rig seam landed: `tools/breadth/measure.py` gains `pool-ts` and
`pool-py` tiers. The arm lives in the tier name, so the existing run
identity (`tier` + `tasks_sha256`) separates arms with no manifest
change; the campaign driver never climbs into them; capability is
checked per arm. Both arms selftested through the real loader. A pool
run placed under `records/measurements/` feeds the corpus through
`pin.py` unchanged — the issue's "ordinary measurement runs feed the
corpus" is now true of the pool, not just intended.

`make check` green (1137 tests — the two seam tests included, keyed to
the pinned manifest so in-flight generator files cannot fail the suite).

## Left open
- 121/~500. Next ids p122+. Batch discipline unchanged.
- build_dataset.py predates paired arms (shared id pools both arms'
  replies under one cap; prompt rebuild needs the arm's contract) —
  still assigned to the lane that first builds a dataset from pool runs.
- type_annotation problems still deferred on anti-triviality design.
- No pool sweep has been run against a served model yet; the seam is
  verified by selftest, not by a worker run.

next: batch 4 (p122+); once a rig is free, a short pool-ts probe run
against a served model to shake out the seam under real dispatch.
