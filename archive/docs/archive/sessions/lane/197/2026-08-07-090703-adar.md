---
record: session/1
lane: lane/197
agent: adar
started: 2026-08-07T09:07:03Z
---

# Session — lane/197 — 2026-08-07

## Did
Sourcing was searched before anything was generated, per the owner's
directive: three delegated web investigations (dataset hubs; synthetic
pipelines; benchmark-derived pools and contamination evidence), verdict
adopt-nothing, recorded with queries and URLs in
`docs/problem-pool-prior-art-2026-08-07.md`. The two disqualifiers are
item overlap with the front door (MultiPL-E's humaneval-ts is the eval,
item for item, 159/164) and pretraining memorization — with the sharpest
finding being that fine-tuning *revives* contamination pretraining had
diluted (arXiv 2601.06103, on Qwen2.5 bases). The caveat that survives
generation is also written down: distribution-level overlap inflates
HumanEval+ without item overlap (LBPP, +14pp), so the +3pp bar reads as
capability-plus-familiarity; #113's held-out set is the separating
instrument.

The pool lives at `tools/problems/` — named to keep out of the source
pool's namespace, which mattered: this session briefly overwrote
`tests/test_pool.py` (the #20 source-map suite) with pool tests and
`make check` passed with 382 lines of coverage silently gone. Restored
from HEAD~1; the pool's tests are `tests/test_problem_pool.py`.

Design: one problem, two arms (`tasks/{ts,py}/pNNN-<slug>/`), directory
name == contract id in both arms so the id is a globally unique join key
by construction (the d2/t01-vs-d3/t01 collision cannot recur). Admission
is `admit.py`: contract via the real loader, executed selftest,
failing-first for bug_fix, anti-triviality stubs (no-op and echo, derived
from the interface — inputs the checker's generator never saw), a
5-assertion floor, the 164 HumanEval entry points blocked under
case/underscore normalization in both casings, and a lexical Jaccard
near-dup screen at 0.55 against every existing task set and the pool
itself. Admitted problems are digest-pinned in `admissions.jsonl`;
repairs supersede under a new id (d1r's discipline); `tasks/` is
ruff-excluded like the python arm, and for the same reason. The gate was
negative-tested with a trap candidate rejected on six counts.

Batch 1: p001 hand-written exemplar plus p002–p041 from four
domain-assigned generators (20 domains, 2 problems each; 28
function_implementation + 12 bug_fix; checkers 9–19 assertions).
Admission re-run whole from this session rather than trusted from the
generators: 41/41, then pinned. `make check` green at every commit
(1135 tests, the restored 18 included).

## Left open
- The pool is 41/~500. Batches continue in this lane: next ids p042+,
  same domain-assignment discipline, fresh domains or deeper difficulty
  per batch; the near-dup screen now guards against convergence.
- No rig runs the pool yet. Seam stated in the README: a breadth tier
  that is not a campaign rung, plus bundle-style `--language`; run
  identity covers pool tasks via `tasks_sha256` with no new mechanism.
- `tools/finetune/build_dataset.py` predates paired arms (shared id
  pools both arms' replies under one per-task cap; prompt rebuild needs
  the arm's contract). Belongs to the lane that first builds a dataset
  from pool runs.
- type_annotation problems deferred pending an anti-triviality design
  for checkers that read their own source text.

next: generate batch 2 (p042+) toward ~500, or land the rig seam first
so pool problems can start producing corpus while batches continue.
