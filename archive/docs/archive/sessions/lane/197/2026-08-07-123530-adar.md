---
record: session/1
lane: lane/197
agent: adar
started: 2026-08-07T12:35:30Z
---

# Session — lane/197 — 2026-08-07 (fourth record)

## Did
Batch 4 ran into the account's monthly spend limit and all four
generators died mid-range. Twenty-eight problems were finished when they
stopped and gated normally: p122–p130, p132–p140, p142, p143, p152–p159.
The pool stands at **149**, all 149 re-admitted whole from this session
and pinned. `make check` green (1137 tests).

Half-written work was removed, not carried. p141 had a TypeScript arm
and no Python one; p144–p151 had a `reference.py` and nothing else. An
arm without its pair is not a problem this pool admits, and leaving the
fragments would have put unpinnable directories in a tree whose whole
discipline is that what is on disk is pinned.

The ids in the resulting gaps (p131, p141, p144–p151, p160, p161) stay
unused and the next batch starts at p162. A gap in the sequence should
mean work that never landed, never work that was withdrawn — a reused id
would make the manifest's history ambiguous about which is which.

The spend limit is the second interruption of this kind this lane (the
first ended one batch-2 generator). Both times the gate absorbed it: the
generators' reports were lost, their finished work was not, because
admission re-executes everything and believes no report.

## Left open
- 149/~500. Next ids **p162+**.
- Generation is spend-limited again as of this record.
- Unchanged: `build_dataset.py` predates paired arms; type_annotation
  deferred; no pool sweep has run against a served worker yet (the seam
  is verified by selftest only).

next: batch 5 (p162+) when spend allows; independently, a short pool-ts
probe against a served model would exercise the seam under real dispatch.
