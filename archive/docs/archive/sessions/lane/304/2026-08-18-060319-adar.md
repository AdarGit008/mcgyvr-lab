---
record: session/1
lane: lane/304
agent: adar
started: 2026-08-18T06:03:19Z
---

# Session — lane/304 — 2026-08-18

## Did

**The #302 plan of record has a home: docs/plans/302/.** Owner approved 2026-08-18. README.md
carries the fixed grep-able first line (`Current phase: P1` — this PR completes P0), per-phase
and per-item DoD checkboxes, the §0–§9 sections the phase issues bind to, the DEC-1…DEC-13
register (DEC-1 ratified with the sign-off), and the amendment protocol: corrections are dated
appended blocks, DEC reversals carry the owner's explicit sign-off. Beside it: `parked.md` (the
frozen not-now set) and `evidence/` (the review's synthesis + eight verification reports, every
figure beside its derivation — lane/251's rule).

**The 0035 collision is resolved and the namespace is gated.** The vocabulary record (#301) is
renumbered ADR-0036 with a dated note inside; ADR-0035 resolves uniquely to the ceiling record
(#262), which all twelve code/config/test citations already meant (verified, none changed).
ADR-0025's `Amended-by:` now names the ceiling ADR; lane/301's session record got its dated
correction. New `tests/test_decisions.py`: number uniqueness + title/filename agreement,
bidirectional amendment edges against a frozen `MISSING_BACKPOINTERS` allowlist (removing an
entry is how a repair is proved), `SAME_DAY_AMENDMENTS` with exactly the two same-day pairs
(0018→0017, 0026→0025), and canaries proving each check rejects on a synthetic corpus.

**One correction to the issue's own seed, recorded on #304.** The spec seeded eleven one-way
edges, derived from a first-line grep of `Amends:`. Three records (0020, 0021, 0032) declare
targets on continuation lines — comma-separated declaration lists, the same form as 0026's
single-line pair — so the honest read is 18 edges, 15 one-way after the 0025 repair: the four
the grep hid are 20→18, 21→17, 21→18, 32→27 (none same-day; the registry's seed stands). The
parser reads the whole wrapped field for both `Amends:` and `Amended-by:` (parenthesized
commentary never declares an edge), the allowlist holds the fifteen, and INDEX.md states the
full headers. Caught by this lane's adversarial verification pass, reproduced by direct read
before changing anything. New `tools/decisions/index.py` generates `docs/decisions/INDEX.md` (deterministic,
DO NOT EDIT marker, `--check` drift mode) — wired into `make docs` / `make docs-check` (which CI's
required test job runs) and `generated_globs`. One parser, imported by the test, so the index
and the gate cannot disagree about what a header says.

`make check` green (1637 tests); changed paths ∩ `surface_files()` = ∅ — no SURFACE byte moved,
the pin did not move.

Note on the evidence pack: its files are records of the review as conducted; where they need to
distinguish the two former 0035s they write 0035a/0035b, and their README pins the resolution.
The evidence proper is `verify/` (every figure beside its derivation); `synthesis.md` is the
review's working narrative and says so in an appended Derivation status block — a synthesis
figure without a `verify/` counterpart is a working note, never quoted onward.

## Left open

next: P1-1 (#287) — first SURFACE lane; branch only after this merges (Law 3). The 25 remaining
phase issue bodies from the report §5 go onto the tracker (rewrites replace the named bodies,
NEW ones are filed) so the spec of record lives on GitHub, not on a disk.
