---
record: session/1
lane: lane/265
agent: adar
started: 2026-08-14T06:37:27Z
---

# Session — lane/265 — 2026-08-14

## Did
Claimed lane/265 and spent the session deciding run identity rather than writing it — nothing is drafted yet, on purpose.

Owner proposed archiving every pre-fingerprint manifest, keeping only what can be reproduced under the new fingerprint. Checked it and pushed back on three grounds. (1) "Reproduces" is not decidable here today: tools/bench/reproducibility.json carries bounds: [], no null has ever been measured, and report.py refuses to qualify any delta without one. The bound is keyed on (model, tier, gate_rungs, serving_build) and there are 33 distinct combos across the 123 manifests at ~40min each, so the rule would have blocked #265 on #231 — the inverse of what #265 is for. (2) 122 of 123 manifests name the model by mutable tag; only 2 carry a blob id, so a re-run is replacement, not reproduction, and a moved tag would read as "failed to reproduce" and archive a correct record. (3) Of the 8 measurement directories the claims cite, 3 never call a model at all — tokens-2026-08-03 is CLM-0011, the 2,387 units that ARE #265's own evidence for the 15.0pp model spread, and archiving it would strand shipped ESTIMATE_RESERVE = 0.32 in gate/preflight.py.

Owner accepted label-now-delete-later with stricter tags than proposed: verified = clean, ran with a proper fingerprint; backfilled = NOT CLEAN, never read, a dormant insurance label; cant-fingerprint = never trusted, no promotion path. And no re-runs at all — rig time goes to new runs done properly rather than to repairing old ones, so CLM-0011 stays dark until a fresh measurement, and #256 waits for that rather than for a promotion.

Then verified whether ADR-0026's three fields are the right ones. They are not complete. The bench manifest records 14 facts (breadth/measure.py:862); the comparability guard reads 5 (bench/report.py:65) and ignores the task set, the output cap, the temperatures and the draw count — two cells that ran different tasks pass the guard today. "Effective context" is filed under the model in #265 but nothing sends a context size (runner.py:401-416 sends only num_predict and temperature), so the real window is a server default; same for top_p and top_k. There is no seed anywhere and the breadth sweep has a sampled arm at T=0.7, so those rows cannot reproduce at all, fingerprint or not. And two runners already write a block called identity that disagree — 14 fields in breadth, 6 in bundle (bundle/measure.py:654) — with a third five-name list in the reporter.

Decided, all owner-approved: identity is FOUR groups, not three — the model, the request, the server, the bar. The server is the missing one and it has already cost a contrast (0.32.4 vs 0.32.5 in the scaffold ablation, breadth/measure.py:806). One block, one shared module, both runners and the reporter read it, so the guard cannot fall behind what is recorded. Context size, top_p and a seed are now sent explicitly so they are recorded decisions rather than server defaults. The machine is inside identity. The bar is hashed as the RESOLVED rule list, not the config files, because a config that says "recommended" hides 328 ruff rules against 66 eslint — verified both tools can answer: ruff check --show-settings and eslint --print-config. Missing values follow one rule everywhere: a value means obtained, null plus a reason means asked and refused, an absent key means the record predates the contract — three states, no sentinel string. The tag is computed from the block, never typed. The guard compares the whole block except one contrast axis named in the call.

Also decided: a second block, observed — captured as comprehensively as obtainable including metadata, compared by nothing, written to its own file beside run.json so identity stays diffable. Comprehensive AFTER redaction, and through baseline scrub like every other record. Promotion from observed into identity is the owner's alone, and is enforced by ownership of the shared identity module rather than by a sentence in a document, because a rule that is only written down is lens 3's defect. Raw replies need no new decision — ADR-0016 already keeps them whole.

## Left open
next: draft #265's decision document: the identity block, the missing-value rule, the guard change, and the three tags — nothing is written to the repo yet
