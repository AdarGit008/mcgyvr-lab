---
record: session/1
lane: lane/175
agent: adar
started: 2026-08-06T10:58:18Z
---

# Session — lane/175 — 2026-08-06

## Did

The owner claimed the lane and locked in the decisions the survey earned,
before any PR. Four judgements were made in this session, and the split between
them — three decision records, one issue — is itself deliberate: the three
decided questions had their evidence complete and their alternatives priced;
the fourth has a solid finding and no prototyped design, so it was filed rather
than decided.

**ADR-0014 — the acceptance boundary is never mocked, and its outcome is not a
boolean.** RA.Aid's year-broken gate needed both a mock (so tests could not
see the crash) and a boolean (so the runtime could not distinguish it from a
pass); the record forbids each independently. It canonizes what
`gate/acceptance.py` and `tests/test_acceptance.py` already do — five-valued
outcome, mock-free suite driven through `TempDirSandbox` — which is exactly why
it is an ADR and not an issue: nothing currently *requires* that shape to
survive a refactor, and both plausible refactors arrive with good reasons
attached.

**ADR-0015 — a failed verifier never promotes.** The owner chose fail-closed
over open-swe's `neutral`, with the record keeping open-swe's insight in the
ledger while refusing it in the effect: *rejected* and *never ran* stay
distinguishable per ADR-0014's channel discipline, but neither promotes. This
answers #179's fifth question and the infrastructure half of its eighth; the
verdict-shape half (may the verifier blame the demonstrating instrument) stays
open on the issue, and the record says so explicitly. Rejected alternatives
written out: neutral (the degradation path — a verifier broken on Monday
produces indistinguishable-from-unverified runs all week), an
infrastructure/model fault taxonomy (fails open exactly when the failure is
novel enough to be misclassified), and deferral (four production systems made
this call; none recorded why; two encode it as magic values).

**ADR-0016 — fixtures capture what the parser reads, not what the run did.**
Fixes #184's shape in advance of its implementation, on the survey's paired
evidence: OpenHands's trajectory captures decayed in six days and were
overwritten with hand-authored mocks; aider's raw-reply corpus — 33 parse
failures kept as gold — is alive years later. The rule: raw reply text plus
minimal provenance, failures pinned as expected outcomes, the corpus asserted
whole.

**#185 filed — the capacity bound is per-process; the rigs it bounds are
host-wide and shared.** `capacity.py:111`'s semaphore, acquired at `:207`
without timeout, against shared rigs and a parallel-worktree workflow. Facts
re-verified against source before filing (zero occurrences of "process";
line numbers exact). The three annex patterns are recorded on the issue as the
field — flock, occupancy-by-query, claim-by-uniqueness-key — with the design
explicitly deferred to the fix lane, and two properties of the current module
named as worth keeping (slot-per-dispatch, which ADR-0012 leans on, and
`Usage`'s wait-time reporting).

Numbering: lane/178 (unmerged) holds ADR-0013, and ADR-0012 on this branch
forward-references it, so this lane's records start at 0014. `make lint` clean.

## Left open

next: The PR. The owner has claimed the lane and said not yet — the decisions
above were the precondition. When it opens it carries the survey, its second
read, the summary, three ADRs and the session records.

Still open from the previous session, undecided rather than forgotten:
#162 keyed on reply well-formedness (aider's evidence — median 94.2% on diff,
1,598 unparseable replies across 69 runs — makes well-formedness the routable
property, and mcgyvr discards the signal on every dispatch); the sections 1–5
second pass, with `langchain-ai/open-swe` as its first candidate; and #179's
remaining questions, now minus the fifth and half the eighth.
