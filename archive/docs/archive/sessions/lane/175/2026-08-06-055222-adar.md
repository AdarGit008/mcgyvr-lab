---
record: session/1
lane: lane/175
agent: adar
started: 2026-08-06T05:52:22Z
---

# Session — lane/175 — 2026-08-06

## Did

Ran a second, independent read of the survey and folded its corrections into
`docs/prior-art-review-2026-08-06.md`. Six readers took sections 6–10 and the
annex in parallel, each pinning its own sha before being told anything the first
pass concluded, and each given the filter and the issue list rather than the
findings. Where a first-read claim was named to a reader it was named as a claim
to test. No new repositories were added to the ten.

The pass was worth its cost for a reason narrower than "it found things": **the
first read's errors were all in one direction.** Four suites reported unrunnable,
three of which ran. One repository reported empty, whose code had moved ten days
before the pin. A cap structure reported as fully named, which loses a name on
the way to the run record. Every correction made the prior art look more capable,
which is the bias a survey working against a clock should expect to have.

Corrected: RA.Aid runs (764 passed — `uv.lock` is present and the first read
stopped one command short of it); SWE-agent runs (74 hermetic, 108 with Docker);
aider runs (468 passed, and it needs neither the network nor the display the
first read blamed); oh-my-openagent runs (12,965 pass, after `npm i -g bun`).
SWE-ReX is the one not-run that survived a second attempt. **Three of four
"cannot run" verdicts did not hold**, which is the most transferable process
finding in the document and is now stated as one.

OpenHands is re-qualified. Every observation in the disqualification is confirmed
and its inference is wrong: the agent was migrated, not deleted — `cb9138caf`,
2,568 files and 486,115 lines removed with zero insertions — and now lives at
`OpenHands/software-agent-sdk`, whose suite runs 5,696 + 375 + 462 passed with
zero failures. The disqualification heading stays, with the correction above it,
because the miss and its repair are both the record. The migration is also the
survey's strongest external evidence for its own selection rule: the largest
project in the set split its repository along exactly the caller /
below-the-contract boundary #175's amendment argued from a metadata sweep.

New, and none of it in the first pass: RA.Aid's acceptance gate has raised
`TypeError` on every invocation since 2025-01-27 and its `except Exception`
returns the same `should_break=True` a passing suite returns — verified by
running their `execute_test_command` three ways under their own locked deps —
with all six module tests green because each mocks the call that fails.
SWE-agent's four named ceilings produce three exit statuses, the step cap
reporting as `exit_cost`, and its requery loop's declared bound is not the
operative one. OpenHands captured six real completions and overwrote the replay
with hand-authored mocks six days later, which qualifies #184 rather than
supporting it: parser fixtures do not decay, trajectory fixtures do. aider routes
342 model names off a 3,128-line hand-maintained table of which 17% has a
published run behind it, and no code path reads its own leaderboard.

#179's eighth question now has two shipped answers that disagree, which is more
useful than the one it had: OpenHands and SWE-agent both fail closed (a verifier
that raises votes `HIGH`; an unparseable review scores −100), while open-swe
returns `neutral` on the ground that reviewer infrastructure failing is not the
PR failing. That is the decision, stated by people who had to make it.

The finding that lands on us: `capacity.py` bounds dispatch with a
`threading.BoundedSemaphore` acquired without a timeout, so the bound is
per-process against rigs that are host-wide and shared, and this repository's own
workflow runs lanes as parallel worktrees. Two lanes each dispatch `max_parallel`
at the same srv. The word "process" does not appear in the file, so the case is
unstated rather than merely unhandled. All three annex repositories are ahead of
us — container-use's flock is kernel-released, orca derives occupancy from a
query, multica claims by uniqueness key with no coordinator — and mcgyvr is the
only design reviewed that blocks forever.

`make lint` clean; every quoted third-party line is in a `text` fence per the
convention this lane adopted.

## Left open

next: File the issues the second read earns. The `capacity.py` cross-process
bound is the one that is about this repository and should go first; then the
acceptance-gate mocking question RA.Aid demonstrates (may `acceptance.py`'s
subprocess boundary be mocked in any test, and can a boolean carry passed /
failed / never-ran); then #184 rewritten to capture parser input rather than run
trajectory, on OpenHands's evidence; then #162 keyed on reply well-formedness,
on aider's. None were filed in this session — the survey documents them and
filing is a separate judgement the owner has not made yet.

Also open: sections 1–5 have had no independent verification. Four of six second
reads materially corrected their target, so the correct prior is that those five
contain errors of the same kind and direction. A second pass over them is the
obvious next lane, and `langchain-ai/open-swe` — read in depth here, suite clean
at 1,672 passed, 4–5/5 on the criteria — is its first candidate. It was not
promoted into the ten because renumbering a set selected under a stated rule
would make the selection unauditable.

The lane has no PR yet and nothing in it has been reviewed by its owner.
