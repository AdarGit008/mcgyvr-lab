---
record: session/1
lane: lane/32
agent: adar
started: 2026-08-01T12:21:12Z
---

# Session — lane/32 — 2026-08-01

## Did
Started E5 (#32, the deterministic acceptance gate) with its two
sandbox-independent foundations, both green under `make check` and baseline
0 blockers.

**#33 ChangeSet** — `src/mcgyvr/gate/changeset.py`. Detects the worker's whole
change once and shares it, so subprocess count is constant in the number of
changed files (the property local-ai measured at 3 spawns vs 51 for 25 files).
Key choice: rather than special-casing tracked edits, untracked additions and
deletions, I stage the working tree into a **throwaway index seeded from the
base** (`GIT_INDEX_FILE` + `read-tree` + `add -A`) and diff that against the
base — one code path for all three, touching neither the real index nor the
working tree. Added-line attribution comes from a `-U0` patch parsed
positionally against the `-z --name-status` inventory, so a quoted or
non-ASCII filename can never silently drop a file (the failure mode #33 names).
No-commit repos diff against the empty tree; binaries and deletions carry
empty added-line sets so added-line checks are inert on them automatically.
17 tests including a flat-subprocess-count assertion across 1/10/25 files and
an independent LCS reference for attribution.

**#34 Scope** — `src/mcgyvr/scope.py` (package top level, not under gate/,
because #34 requires the *same* matcher in tool resolution and output
application). One glob matcher: `*` single-segment, `**` recursive,
forbid-overrides-allow, empty-allow-permits-nothing (fail closed).
`violations()` names every offender. 17 tests.

Dead end avoided: parsing the (possibly quoted) path out of the patch header
to match hunks back to files — the positional zip against the `-z` inventory
is quoting-proof and simpler. A defensive count-mismatch guard raises rather
than misattributes if that assumption ever breaks.

## Left open
next: #35 (adapter interface + Python adapter, needs ruff shell-outs) and #37
(secret scan) are next and unblocked — both read `ChangeSet.with_additions()`
and `read_added_text()` already in place. #39 (structured-data + pre-flight)
also unblocked. Then the gate runner (#32 aggregator) orders them. Blocked
within E5: **#38** (acceptance command execution) needs the E4 sandbox
(#26/#27) to run in, and **#36** (JS/TS adapter) pulls in a tree-sitter
runtime dependency — sequence #36 last and decide the grammar-binding
approach first. Lane not yet claimed at origin; nothing pushed.
