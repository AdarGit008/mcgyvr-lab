---
record: session/1
lane: lane/361
agent: adar
started: 2026-08-25T01:47:59Z
---

# Session — lane/361 — 2026-08-25

## Did
One `uv lock --upgrade`, taken deliberately as an ADR-0032 Law 1 SURFACE mover.

`uv.lock` is in `product.SURFACE` (tools/bench/product.py:154), so each of the
three open dependabot PRs against it — #342 (ruff), #340 (mypy), #341
(types-pyyaml) — was a separate `product.digest()` move for edits to one file.
Merged separately: three movers in the r2 drain list and three rebase-and-rerun
cycles under the strict-up-to-date ruleset. Merged as one upgrade: one of each.
#275 is the precedent — a dependabot ruff bump taken as a named mover.

The resolver moved eleven packages, not three, because `--upgrade` takes every
specifier to its ceiling at once: ast-serialize 0.6.0->0.8.0, filelock
3.32.2->3.32.4, hf-xet 1.5.2->1.6.0, huggingface-hub 1.26.0->1.28.0, idna
3.18->3.19, librt 0.13.0->0.15.0, mypy 2.3.0->2.3.1, packaging 26.2->26.3,
pygments 2.20.0->2.21.0, ruff 0.16.2->0.16.4, types-pyyaml 20260724->20260815.

Ruff resolved to 0.16.4 and not the 0.16.3 #342 proposes: 0.16.4 shipped after
that PR opened, so taking the older pin would have meant a second mover within
days.

Only ruff can reach a verdict. `pyproject.toml` declares `ruff>=0.16.2` — a
floor — so `uv.lock` is the real pin, and ADR-0025's consequence is that the
checker version is part of the instrument. The rest move the digest anyway,
because the digest is over the file and not over a judgement about which changes
could matter.

The move: product.digest() 08e5038348787192 -> 02ceb856277b7e71 (first 16 of
each; the commit message carries both in full).

Gates were run on the upgraded toolchain rather than the old one: `ruff check .`
clean, `ruff format --check .` 576 files unchanged, `mypy` clean over 126 source
files, `pytest` 1697 passed / 5 skipped. So the new ruff selects the same rule
set this tree already satisfies — measured, not assumed.

## Left open

The first attempt rode a branch named `lane/uv-bump`, which failed the baseline
gate on FLOW-01 (no issue anchor) and FLOW-02 (no session record) — `lane/*` is
the anchored namespace and `dependabot/**` is the only exempt family, so a
non-dependabot change cannot ride the exemption without faking its provenance.
Issue #361 was filed and the lane re-claimed; PR #360 on the old branch is
superseded.

#340, #341 and #342 stay open until this merges, then close pointing here —
closing a dependabot PR early tells dependabot not to offer that version again,
which would lose the information while the replacement is still unmerged.

next: merge #361's PR, close #340/#341/#342 pointing at it, then P1.3 (#306, the grader into tasks_sha256) once #286 merges

## Left open
next: merge #361's PR, close #340/#341/#342 pointing at it, then P1.3 (#306, the grader into tasks_sha256) once #286 merges
