---
record: session/1
lane: lane/291
agent: adar
started: 2026-08-17T03:34:57Z
---

# Session — lane/291 — 2026-08-17

## Did
Landed #291 as **ADR-0032** — both halves of the defect, because splitting them lands the doctrine without the paths or the paths without the doctrine. #276 closed with two acceptance boxes unmet and no owning issue took either; this put both in the repository.

THE RULE WAS ONE MEMORY AWAY FROM BEING LOST. #276 decided that every identity change lands before `r2` opens, and its own acceptance box asked for that in `rounds.json` doctrine or an ADR amendment "so a fourth driver cannot route around it". Neither existed at `97173b2a`. What `product.py` said instead — "a round is closed by opening the next one, which is the only place an adopted change may land" — is true and is the misleading half: it licenses *a* change per boundary and says nothing about batching, which is exactly how lane/261 read it on 2026-08-16. The rule now lives in three places with three different jobs: `rounds.json`'s `doctrine.clauses` is the authority (data, so a clause added there is stated by the tool without touching a module), `product.py`'s docstring is the point of use, and ADR-0018 carries the amendment. **A round boundary is drained, not taken.**

WHERE THE TOOL STOPS. `_open_cli` appended unconditionally — nothing between a driver's conclusion and a closed round. It now refuses without `--adopted` (repeatable), records the batch in the round entry, and prints the doctrine and the moved-file list before appending. It does **not** verify the batch, and the ADR says why: nothing in `product.py` can know which issues are open, and every checkable proxy is wrong in the same direction — "refuse a one-file boundary" punishes a legitimate single-change round, "refuse unless every telemetry issue is closed" makes a bench tool a client of the issue tracker. A gate on a heuristic teaches drivers to work around the tool rather than the rule. What is enforceable is that the batch is NAMED, in the record, at the one moment the rule is violable.

THE PIN COVERED THE SCORER AND NOT ITS CONFIGURATION. `identity.py` files `round` and `product_sha256` under the **bar** group on the ground that "the revision they pin includes the scorer" — true of the code and false of half the bar, which lives in config: `score.lint_config` reads `pyproject.toml` at call time, `score.stage_js_toolchain` copies `eslint.config.mjs` into every workspace, and the checkers are whatever the lockfiles resolve to. A rule flipped to `warn` narrowed the bar, the digest did not move, and no round refused. Five paths joined `SURFACE` — `pyproject.toml`, `eslint.config.mjs`, `uv.lock`, `package-lock.json`, `data/task-catalog.json` — and the `identity.py` comment is corrected rather than moved.

BOTH LOCKFILES OR NEITHER. #276 named only `uv.lock`. That asymmetry is ADR-0025's own failure mode rebuilt one level down: the arms are paired ts/py, so freezing ruff inside a round while eslint floats puts a language effect inside every contrast rather than a visible refusal. The admitted cost is that a dependabot bump now closes a round — on a path that moves weekly rather than rarely. Taken deliberately: mid-campaign it is a refusal at dispatch, which is recoverable by opening a round, and the failure it prevents (a ruff patch release re-basing half a round's arms) is not. #285's `bar_sha256` is what would let the surface get finer later, and only in that direction.

THE EXTENSION GLOB. `surface_files` walked `rglob("*.py")`, so `src/mcgyvr/prompts/*.md` — the literal text a worker is sent — sat outside the digest of the code that sends it. A directory now contributes every file beneath it; the only exclusion is a path derived from files already hashed (`__pycache__/`, `*.pyc`), because otherwise the pin would depend on whether the tree had been imported. Enumeration is the filesystem's and deliberately not `git ls-files`': a file authored and dispatched before it is committed is exactly the unpinned code the refusal exists to catch. 56 files in the surface before, 63 after, and no junk in the delta.

`bundle_sha256` STAYS OUT OF THE KEY, decided rather than deferred. #276's rule admits only on a perturbation run and none has been done; corollary 1 says an untested field is recorded and not keyed. What made it look urgent is closed at the other end instead — the prompt files are now inside `product_sha256`, which IS keyed. The two pins are different and both wanted: `bundle_sha256` catches a render that differs from the files, `product_sha256` catches files that moved between two runs.

A DEFECT THE TESTS FOUND. `_open_cli` read `load_rounds()` and `digest()` through their **default arguments**, bound at definition time, while `_lines(REPO)` read the module global. Same paths in production; two different trees in one round entry under anything that redirects them — the digest described the real repository and the file map described the substituted one. Fixed by resolving both from the globals at call time, and pinned by an assertion that the entry's digest and its file map describe one tree.

CITATION CORRECTION, per the issue's fourth acceptance box. Every code citation in #291 re-verified against `97173b2a` and all held but one: ADR-0025's amendment is dated **2026-08-13** (ADR-0026), not 2026-08-16, and the "re-bases every JavaScript rate measured under it" line is in **Consequences**, not in the amendment. The claim stands; the pointer was wrong. Recorded in ADR-0032.

A SECOND DEFECT, IN THE TEST SUITE, EXPOSED BY THE FIRST. Twelve cases in `test_bench_gate_rescore.py` failed the moment the surface widened, all at `gate_rescore.py:404` — they call `rescore_dir`, which calls the **real** `require_pinned`, so the whole file was coupled to whether this tree happens to match the open round. It passed until now only because it did. That is the coupling `test_the_open_round_is_complete`'s docstring already refuses for itself ("requiring the two to agree at all times would mean opening a round per commit"), arrived at indirectly. Stubbed the pin as `test_breadth_rig.py` and `test_prompt_conditions.py` already do, and made it **autouse** rather than a parameter each case opts into: the default for a case added later must be "pinned", not "coupled to whether a campaign is open". What the pin does to a moved tree is asserted in `test_bench_rounds.py`, which is where it belongs.

`r1-commissioning` no longer matches this tree, by construction — eight paths moved into the digest, so `--check` refuses until a boundary opens. That is the expected end state: #291 is part of the batch that must land before `r2`, not the thing that decides the batch is complete. 15 new tests; ruff, ruff format, mypy strict and the full suite green.

## Left open
next: open the #291 PR; r2 stays closed — #285 (the three digests), #286 (the observed probe set) and #287 (the two runners' resume drift checks) are in the same batch and land before the boundary
