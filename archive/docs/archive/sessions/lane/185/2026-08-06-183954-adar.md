---
record: session/1
lane: lane/185
agent: adar
started: 2026-08-06T18:39:54Z
---

# Session — lane/185 — 2026-08-06

## Did
Took #185: chose flock from the issue's three candidate shapes (occupancy-by-query needs a queryable store mcgyvr lacks; uniqueness-key claims need a database and fit one-shot claims, not M-concurrent slots). A slot is now an exclusive flock on one of max_parallel files keyed by normalized base_url, replacing the in-process semaphore outright - one mechanism excludes threads and processes alike. Block-forever answer chosen and documented: the kernel-released lock, because a fixed acquire timeout would misfire on legitimate deep-queue waits; hold() gained an optional timeout for claim semantics (timeout=0 = multica's shape). Slot files are never unlinked (unlink race over-admits). Usage stays per-process observation, stated. New cross-process suite: 6 interpreters on a file barrier never exceed limit 2 and reach it (append-log replay); one-winner claim with 5 fast refusals and a host-side kernel proof (parent cannot flock the held slot); SIGKILLed holder strands nothing. Existing 22 in-process tests pass unchanged; make check 1061 green. Also isolated the suite's lock dir via autouse fixture so parallel runs cannot contend.

## Left open
next: Open the PR for #185. Noted, not solved: processes under different TMPDIRs rendezvous separately (documented, lock_dir param is the remedy); two machines at one rig are beyond a file lock and belong to #141's availability story.
