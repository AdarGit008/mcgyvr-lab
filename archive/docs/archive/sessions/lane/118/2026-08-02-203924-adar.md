---
record: session/1
lane: lane/118
agent: adar
started: 2026-08-02T20:39:24Z
---

# Session — lane/118 — 2026-08-02

## Did
Vendored the raw evidence behind the register and added the citation discipline that keeps it honest, which is #118's first bullet and its acceptance test. `records/evidence/local-ai-2026-08-02/` now carries CLM-0004's 160 runs, the five HumanEval+ completion sets, the experiment design and the premise measurement's own rows — 1.5 MB, copied at local-ai `55a084e` with a sha256 per file, plus two files pinned separately to `d201ea1`. CLM-0004 cited a single permalink and the runs behind its 45%→70% were not in this repository at all; its citations now lead with the vendored copy and keep the original permalink beside it so the copy can be checked against where it came from. Nothing new ships in the wheel — `force-include` names its two data files explicitly, so `records/` is outside it.

Landed **CLM-0005**: the premise the inherited research was sized against — small models "achieve 94%+ syntactic validity with ~0% semantic correctness", which ADR-0004 records as uncited — is now measured and false on the half that carried the argument. On identical stored completions, n=164 per corpus across five corpora, syntactic validity by `ast.parse` is 100% for qwen2.5-coder 1.5B/3B/7B and qwen3-coder-30b-a3b and 98.2% for gpt-oss-20b, while semantic correctness on the same rows is 65.2 / 80.5 / 82.9 / 88.4 / 53.7 percent. Measuring both halves on the same rows is what the claim always needed and never had. Graded `high` on one benchmark family because the three qwen figures reproduce `data/capability-table.json` (0.652 / 0.805 / 0.829) to three decimals from a separate rig, which is an independent check rather than a restatement. The record's scope note says what it does not establish: HumanEval+ is self-contained single-function synthesis, so this bounds the inherited number rather than replacing it, and the ordering that premise was used to sort is not re-derived here.

`tests/test_claims.py` pins the property ADR-0004 exists for: every citation resolves to something fixed — a file vendored here, or a URL naming a commit. A GitHub URL on a branch is neither, and it is the case worth testing because it looks like a working citation right up until it silently isn't. Negative paths are exercised directly: branch refs, tag refs and missing in-repo paths are all rejected, while sha-pinned URLs, arXiv links and vendored paths pass. The suite also recomputes every sha256 in the vendored manifest, so a copy that drifts from what was measured fails rather than quietly becoming the new truth.

Deliberately not vendored: the five other measurements taken the same day. Breadth ran at n=2, repair at n=1 on a tautological case (ruff format fixing a ruff-format failure), scope returned 0/2 in all four cells, deps found zero dependencies on four contracts, and the token band is an artefact of chat-template overhead on short prompts and silent context truncation on long ones. Their verdicts do not follow from their n, and vendoring them beside CLM-0004 and CLM-0005 would imply they do.

## Left open
next: #125 or #123. Two things this lane found but does not fix. **CLM-0004 and CLM-0005 do not validate against `tools/baseline/schema/record.claim.schema.json`** — it sets `additionalProperties: false` and both carry `_confidence_note`, as every record in the register does; the gate passes because CLAIM-01/02/04 check fields rather than running the schema, so the schema is stricter than what is enforced. `tools/baseline/` is hash-pinned (REC-06), so the fix is either a schema field for scope notes or an accepted divergence, and it is not this lane's to pick. Separately, **CLM-0005 refutes the premise that sorted the inherited implementation order** — ADR-0004 records the ordering as the thing the missing evidence damaged most, and now that the premise is measured rather than merely uncited, whether the order still holds is an open question that wants its own issue rather than a paragraph here.
