---
record: session/1
lane: lane/289
agent: adar
started: 2026-08-17T01:50:10Z
---

# Session — lane/289 — 2026-08-17

## Did
Closing record for the lane. The earlier record (2026-08-16-180751) described the state after three commits and is superseded on two counts: its `next:` names the overhead calibration and the `cells` mismatch as future work, and both landed in this lane afterwards; and its "1538 passed, +6" counted the test file at six, where it now collects twelve. Recorded here rather than edited there — a record that is rewritten to match what happened later stops being a record.

**The setup term is additive, and that is the finding rather than its size.** `run.json` carries `invocations[0].started`, so differencing consecutive passes within a session gives each pass's wall clock. Setup is 1.64-1.72 min per pass, mean 1.67, flat across passes three times apart in duration. A multiplicative overhead would have got both ends wrong; an additive one means **r2's total depends on how many passes it is cut into, not only on how many tasks it holds**. Measured on 6 of 8 passes — the last of each session has no successor — and each gap contains any idle, so 1.67 is an upper bound. The flatness across a 3x duration spread is the argument that it is setup and not idle, and the test asserts the spread rather than only the mean.

**A docstring costs a round.** Writing the ladder-side half of the `tier` cross-reference into `mcgyvr.config.Tier` moved `product_sha256` and 12 tests in `test_bench_gate_rescore.py` refused the tree. `src/mcgyvr` is inside `product.SURFACE`, and the pin does not distinguish a comment from a behaviour change — deliberately, per ADR-0018. The consequence is worth carrying forward because it accrues silently: **documentation debt inside the product surface cannot be paid off except at a round boundary.** The note is deferred to the identity range #276 already schedules before r2 opens; a test records its absence as a decision and fires if `src/mcgyvr` ever leaves the surface.

**The review earned its cost, and two of its findings were mine to have caught.** `identity.py` shipped a comment claiming a cross-reference to the file I had just reverted — it contradicted a test three files away. And the headline "2.5x too high / 18% too low / 3.0x spread" compared a wall-clock constant against summed task time; like for like it is 2.0x / 23% / 2.66x. That wrong pair had propagated into four documents before the review caught it, which is precisely the failure mode this card was built to end. The correction is stated in the record rather than overwritten, because *say which cost a figure is a figure of* is the module's own lesson.

**"The only price in circulation" was false.** ADR-0027's rejection argument prices bound combinations at "~40 minutes each" and was quotable today, while the test enforcing "a figure that stays quotable elsewhere has not been replaced" checked only `reproducibility.json`. ADR-0027 is amended and the test now covers it. The amendment strengthens that ADR's rejection: where the 33 combinations differ only in denominator, the recomputation result makes the marginal cost zero.

**What is left is not engineering.** DoD item 6 — what gets dropped if the bill exceeds the rigs — cannot be written until r2's size is known, and that waits on #276's sequencing: items 3 and 4 say r2 runs at the current 257 with the 400-task sweep decoupled, which authoring-first reverses. Raised there on 2026-08-16 for an owner amendment. The rate card is correct under either answer, which is why the lane did not block on it.

`make check` green — 1544 passed; `tests/test_bench_ratecard.py` collects 12.

## Left open
next: owner call on #276 sequencing (r2 at 257 vs waiting for the authored corpus); DoD item 6 — the coverage-drop statement — is blocked on that answer
