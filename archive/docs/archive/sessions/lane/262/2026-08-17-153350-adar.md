---
record: session/1
lane: lane/262
agent: adar
started: 2026-08-17T15:33:50Z
---

# Session — lane/262 — 2026-08-17

## Did

**The bar is recorded as content, there is one live acceptance ceiling, and three of this issue's own figures were wrong.** ADR-0035. Every number below is re-derivable from the tree: `tools/bench/ceiling.py` for the ceiling, `identity.bar_material` for the bar.

**328 does not reproduce; ruff says 250.** #262 derives the Python rule count by prefix-matching `ruff rule --all` against `[tool.ruff.lint] select` and reports that it "reproduces exactly". It does not, because a *string* prefix is not a ruff selector. `E` matches `EM`, `EXE` and `ERA`; `F` matches `FURB`, `FAST`, `FBT`, `FIX`, `FLY` and `FA`; `I` matches `ISC`, `ICN`, `INT` and `INP`; `N` matches `NPY`; `B` matches `BLE`. Sixty stable rules from ten linters this project never selects, plus six removed ones. Asked of ruff — `--show-settings`, `linter.rules.enabled` — the answer is **250** under 0.16.1 and 0.16.2 alike, so the ratio is **3.8:1** and not 5:1. The eslint half stands at 66. This is ADR-0033's own argument arriving as evidence rather than as a principle: a re-derivation of a tool's resolution drifts from the resolution that scores. The issue body is amended.

**The type check is symmetric, not a JS/TS asymmetry.** The issue reads it as the TypeScript arm alone — no `tsconfig.json` is staged. True, and incomplete: `score.lint_config` renders a `pyproject.toml` carrying `[tool.ruff]` and nothing else, so `_declares_mypy` is false and the **Python arm is not type-checked either**. Verified by calling both adapters' `locate_type_check_command` against a staged workspace. Per ADR-0006 neither is a defect, which changes what the fix has to be: a record, not a rung. `bar_material` asks the product's own adapters rather than restating the rule, and a test proves the entry is the adapter's answer by handing it a workspace that *does* declare a checker.

**A digest that could describe a workspace nothing is scored in.** `tools/breadth/measure.py:stage_bar` carried its own copy of `stage_dir`'s two configuration lines. It had not drifted; adding `prettier.config.mjs` to the scored workspace and not the digested one would have drifted it that hour. That is this issue's own defect one level in, inside the module ADR-0033 built to prevent it. There is now one seam, `score.stage_config`, and a test compares the two staged workspaces by file *contents* rather than by name — two code paths can render one filename and two rule selections, which is the failure wearing a passing test.

**The format bar that existed nowhere.** `prettier.config.mjs`, every value prettier 3.9.6's own default verbatim, staged beside `eslint.config.mjs`. #262 puts changing either rule set out of scope and this respects it: 257 of 257 `bench-ts` references format byte-identically with and without the file. The change is not to today's bar but to tomorrow's. `printWidth: 80` against `line-length = 88` is left alone and written down instead — narrowing an 8-column gap after 32,601 scored candidates re-bases every JS/TS format rate on disk for a cosmetic gain. `bar_material` records prettier's *resolved* config path, asked of prettier, so a workspace where the file failed to stage answers `unconfigured: true` rather than quietly recording defaults under a declared name.

**The ceiling, measured before it was chosen.** Three numbers in three files under two comments each asserting they matched. Two populations, and conflating them is how this gets picked badly:

| | n | max |
|---|---|---|
| references — what pool admission screens | 514 | **0.305 s** |
| candidates that **passed** — what a ceiling can wrongly reject | 8,230 | **28.718 s** |
| second-slowest pass | | 2.500 s |

The reference population settles nothing: 30 s and 120 s are 98x and 393x its cost, so any argument from "the references are fast" justifies either. The population that decides is slow-but-correct candidates, and `p242-cycle-shape-report` passed in **28.718 s under a 30 s ceiling** — 4.5% of margin, on a row already inside a published rate. Reconciled **up**: one live ceiling of 120.0 declared in `tools/bench/score.py` and *imported* by `tools/problems/admit.py`, which had carried its own `30.0` under a comment claiming it was the rig's. Cost is runaway time — 130 timeout rows in the whole campaign, none passing, so ~3.25 h spread over every run ever taken.

`tools/bundle/measure.py` keeps 30.0. Its arms are retired and `record_run` refuses every call, so its constant sets no ceiling for anything — it *describes* the 31,062 rows measured under it, 127 of them timeouts at exactly that value. Raising it would rewrite what those rows say they were measured under. `DECLARED_DUPLICATES` now carries that as the reason instead of "known to disagree, and filed".

**The [30, 120) band is empty and is not the argument.** 1,539 rows were measured at the wide ceiling and none landed in it — but 31,062 of the 32,601 were measured at 30 s and are *censored* at it, so they carry no information about the band. The tool computes the uncensored subset from the rows rather than from a manifest field, because no manifest recorded a ceiling before this issue. The test suite pins that split against a synthetic corpus, since the overstatement it prevents — reading 32,601 rows as evidence when 1,539 are — is the arithmetic nobody re-checks.

**The test asserts a property, not two equal literals.** `admit.TIMEOUT_S == score.ACCEPTANCE_TIMEOUT_S` would have passed on the day they were written and said nothing after, which is exactly the state found here. So the assertion is against the *source*: `TIMEOUT_S` must not `literal_eval`, i.e. admission holds no number of its own.

`bar_resolved` is a sibling block beside `identity_refusals`, not a `KEY` field: `bar_sha256` is the comparability key and this is the statement of what it hashed. Excluded from resume drift for the same reason — the digest covers the material exactly, and comparing both refuses a directory twice for one change. Adopted forward on absence, never overwritten.

`make check` green, 1,629 tests.

## Left open

next: re-run #295's cross-arm concordance read now that the bar is recorded — its 31.1% → 75.7% filter was approximate because `rejected_by` names only the first rung that fired (#257), and the corrected read is what decides whether the second arm is worth authoring for the remaining ~280 problems; then #231's commissioning checks, which need a run carrying `bar_resolved` before a ts/py contrast can be quoted at all
