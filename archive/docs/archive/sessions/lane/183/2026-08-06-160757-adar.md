---
record: session/1
lane: lane/183
agent: adar
started: 2026-08-06T16:07:57Z
---

# Session — lane/183 — 2026-08-06

## Did
Took #183 end to end. The design chosen from the issue's three options is a separate field: contracts gain an orchestrator-only demonstration list, evidence kinds gain baseline: pass|fail as catalog data, rule 5 checks each command-needing kind against the slot that can satisfy it, and Acceptance runs both lists with opposite baseline expectations at precondition() and identical ones at run(). One new preflight reason (demonstration-passes-at-baseline); environment faults keep their existing reasons. One catalog decision made and recorded in-file: bug_fix drops tests_pass from required evidence because its guarantee promises only the demonstration, and the bundle corpus proved the point - its seven bug-fix tasks have exactly one command, which fails at baseline by design; they now carry it in demonstration and measure.py runs the union. Worker prompts are untouched by the corpus re-slot (neither list is worker-facing), so bundle measurement comparability holds. make check green: 1076 passed.

## Left open
next: Open the PR for #183. After it lands: #146 still owns who authors the demonstrating command; the fabricated-demonstration case (a command failing at baseline for the wrong reason, e.g. pytest exit 4 on a nonexistent test file) is indistinguishable by exit code and belongs to #146's seam, noted not solved.
