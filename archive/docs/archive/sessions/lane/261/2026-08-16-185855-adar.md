---
record: session/1
lane: lane/261
agent: adar
started: 2026-08-16T18:20:00Z
---

# Session — lane/261 — 2026-08-16

## Did
#261 fixed in both adapters, and the issue's own diagnosis was too narrow. It named `except json.JSONDecodeError: return []` as the defect; measured against ruff 0.16.1, eslint 9 and prettier 3 on 2026-08-16, all four invocations the adapters make (`ruff check --output-format=json`, `ruff format --diff`, `eslint --format json`, `prettier --list-different`) answer a fatal config error with **exit 2 and an empty stdout** — not malformed output. `json.loads(stdout or "[]")` reads empty as zero diagnostics and `if not stdout.strip()` reads it as nothing to reflow, so the named exception never fires on the failure that actually happens: a fix that only populated that branch would have closed the issue while leaving every real instance in place. The test is therefore the **exit code**, checked before the output is read, via `trusted_stdout(tool, proc, expected=(0, 1))` in `gate/adapter.py`, which carries the measurement table in its docstring because the fix is only correct for as long as that table is. Two faults now, both under a new `EnvironmentFaultError` base: `ToolUnavailableError` unchanged (absent tool, recorded, verdict still reached — the keyless install `README.md` promises), and `ToolFailedError` (present and untrustworthy, carrying tool/exit code/the tool's own first stderr line). ADR-0034 records the split and its argument: an absent tool leaves a hole the operator can see, a crashed one leaves a hole shaped like a pass, which is ADR-0026 lens 3 verbatim and ADR-0027's rule one layer down. `GateResult.inconclusive` carries the rungs structurally and `accepted` now requires no findings *and* no inconclusive rung; every entry is also rendered into `environment_issues` so no existing reader loses sight of it, and `as_verdict` names `inconclusive` as `rejected_by` when nothing else rejected, so no manifest row says "did not pass, rejected by nothing". Rows in `tools/breadth/measure.py`, `tools/bench/gate_rescore.py` and `rung_report` carry the field. Scope was widened past the linter deliberately: `ruff format`, `prettier --list-different` and prettier's per-file print had the identical shape, and fixing three of the four instances would have refiled the same bug — the issue's title says linter, its framing says "a rung that cannot say whether it ran". Python's tests use no fakes at all (ADR-0014): a `pyproject.toml` ruff cannot parse, ruff really invoked, really exiting 2, plus a control proving the same file is rejected `F401` when ruff can start. The JS side follows its file's existing convention of faking eslint/prettier, and the pre-existing `test_lint_fatal_output_is_inconclusive_not_a_rejection` was rewritten — it fed the adapter a non-JSON *string*, a shape eslint does not produce, while the shape it does produce parsed cleanly and passed the change. ADR-0025 amended twice: `Amended-by` now names ADR-0026 and ADR-0034 (its Context reads the adapter as recording an environment issue on a failed invocation; it records nothing, and the JS comment claiming parity with Python was accurate — one defect implemented twice), and a new 2026-08-16 clause keeps the `_ESLINT_ERROR = 2` severity filter and moves its justification out of the constant: eslint severity is a property of the project's config, so a `warn` is this repository declaring a rule non-fatal, while ruff exposes no such dial to the adapter — symmetry is between what each config declares fatal. Zero behaviour change today; all 66 enabled eslint rules are `error`. `make lint`, `make typecheck` clean; suite green except the 12 below.

## Left open
next: **the round decision, unresolved and blocking.** The gate is inside `product.py`'s pinned surface, so this change moves the product digest and `r1-commissioning` refuses to score — `tests/test_bench_gate_rescore.py` is 12 red with `RescoreError: the product has moved off round r1-commissioning`. Verified pre-existing-clean by stashing: the failures are this change, by design, not a regression. CI runs `make test`, so the branch stays red until a round opens; `main` is unprotected so it *can* merge red, but `product.py`'s own docstring says the boundary is the only place an adopted change may land and that the new entry names what was adopted, which argues r2 opens in this lane rather than after it. Cost of closing r1: opened 2026-08-14 for #231, nothing adopted at its boundary, ~6 run directories and 26 manifests name it (the null-gate and norule-control arms of 2026-08-14, plus gate-rescores of the 08-11 ablations). Checked and worth recording: **none of those 26 manifests carries an `environment_issues` entry — which under this exact bug is not evidence the linters ran**, since a crashed linter recorded nothing. Whether r1's arms were scored under a working bar is therefore not answerable from the records, which is itself the finding. Open lanes pinning r1 are #231, #262 and #289; if any has a dispatch that must land in r1, that is the argument for deferring. Command when it is time: `uv run --no-sync python tools/bench/product.py --open r2-<id> --issue 261 --why "..."`. Also unbuilt and out of scope by ADR-0034's closing section: whether the semantic and acceptance rungs should refuse on their own environment issues — both have their own channel, neither was implicated here, and they run in the sandbox where the causes differ.
