---
record: session/1
lane: lane/109
agent: adar
started: 2026-08-01T20:49:36Z
---

# Session — lane/109 — 2026-08-01

## Did
Audited the six architecture decisions inherited from local-ai (DEC-1..DEC-6, issues #93-#99) and locked the results as ADR-0004 through ADR-0009.

Ran a 16-agent verification pass over the document's claims: 56 external claims graded, 47 conflicts surviving adversarial challenge. The finding that reshaped everything is that the premise the six decisions are sized against - small models at '94%+ syntactic validity with ~0% semantic correctness' - carries no citation anywhere and is contradicted by the figures vendored in data/capability-table.json (0.652 / 0.805 / 0.829 HumanEval+ for the 1.5B / 3B / 7B). Since every gain estimate is an increment on that floor and the implementation order is sorted by those increments, the ORDER is what the missing evidence damages most, which is why re-sizing gates the build issues rather than following them. Twenty-four of thirty-one inline citations resolve into local-ai's gitignored .pi-subagents/ tree and were never readable by anyone.

Individually: DEC-6's headline ('1.5B pass@5 = 7B pass@1 = 0.65') compares a model against itself - our own table has the 7B at 0.829 and 0.652 as the 1.5B's own single draw. DEC-1's stop set truncates a whole-file reply at its second definition and yields a syntactically valid partial file. DEC-3's base rate is not in the paper it cites, and ghostcall run on the host would validate against our four-package venv rather than the target's, so its headline property is unachievable where the issue put it. DEC-5's schema half had already landed in #14. DEC-2's 'new pruner module' duplicates the landed index.

The owner resolved eight forks. Two are worth recording because they corrected me: local tokens are not in the north star's denominator, so DEC-6 is reformulated (breadth as config policy, default 1, first-gate-pass selection) rather than rejected - and Snell et al.'s condition for test-time compute is actually MET at 0.652, which is the honest argument for allowing breadth at all. And the catch-22 - decisions need measurements, measurements need the pipeline - is broken by making the measuring system its own epic (#111) rather than a per-decision afterthought.

Re-filed the tree: epics #110 (static semantic checks - E5 closed without a home for gate work) and #111 (calibration harness, also CLM-0003's de-risk milestone), ten children, six inherited issues closed with specific corrections, six existing issues amended rather than duplicated.

## Left open
next: Work #112 - the headroom measurement for static semantic checks. It runs first because its result can retire #120 unbuilt, it needs no harness, and it costs no tokens.
