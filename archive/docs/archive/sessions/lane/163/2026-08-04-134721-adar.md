---
record: session/1
lane: lane/163
agent: adar
started: 2026-08-04T13:47:21Z
---

# Session — lane/163 — 2026-08-04

## Did

Filed #163 and parked it. The lane is claimed but deliberately not worked: the
hosts whose endpoints it would bind are being wired in another session, so
binding now would race that work, and the ladder the rungs would be ordered by is
being replaced on measured evidence.

Trimmed the issue to mcgyvr-only scope. As first filed it carried host hardware
findings and cross-repo citations; that material belongs to the repo that owns
the serving side, and this issue is the measurement contract and the binding.

## Found

**#158's window problem lands in the middle of this issue, not next to it.**
Binding a real pool is the first moment mcgyvr has an actual context window
available to record — and the sources disagree with each other about what it is
for the same model. So the capability table cannot record a bare number; it has
to record where the number came from, the way it already does for throughput.
Folded into #163's done-when rather than left for whoever picks it up.

**The blocked set is larger than it looked.** #144, #152, #121, #113 and the
quality half of #119 are all waiting on the same missing thing — a reachable
worker — rather than on each other. That makes the pool a keystone, and it is
worth stating in the issue so the ordering is not rediscovered later.

## Verified

Nothing built, nothing measured in this repo. `config.py` already expresses a
remote pool — `base_url`, wire protocol, capacity, with `openai` covering vLLM
and llama-server — so the binding is a data change, not a code change. That is
the one claim in #163 that was checked against the source rather than assumed.

## Left open
next: hold until the hosts are wired and their ladder is settled, then bind the pool from measured quality and close out the window-provenance item #158 needs; do not bind against endpoints that are still moving
