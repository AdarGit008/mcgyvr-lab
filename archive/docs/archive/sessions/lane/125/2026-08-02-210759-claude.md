---
record: session/1
lane: lane/125
agent: claude
started: 2026-08-02T20:15:00Z
---

# Session — lane/125 — 2026-08-02

## Did

Settled the ADR-0004 gate #125 puts in front of any image work on #123, and pinned the denominator the three counts will be computed over. Neither count itself is measured yet; both need a container.

**CLM-0006 — the ghostcall description is half wrong, and the wrong half is the one #123 was leaning on.** #93 passed down "`linosorice/ghostcall`, ~330 lines, zero deps, pure stdlib, pip-installable". At `56b74fc` the repository exists, is MIT, is not archived, does what #93 says, and is genuinely pip-installable (hatchling, console script, 0.1.1, release workflow). **"Zero deps, pure stdlib" is false of the package**: it declares `click>=8.0`, `rich>=13.0`, `rich-click>=1.7`. It is true of the engine — `parser`/`checker`/`suggest`/`markdown` import only `ast`, `importlib`, `inspect`, `difflib`, `dataclasses`, `typing`, `re` and total 173 SLOC, with all three dependencies confined to `cli.py` and `output.py` (126 SLOC of presentation a gate rung never calls). So the property #123 actually needed survives, with a number attached, while the sentence that carried it does not. "~330 lines" was never measured by anyone: it is ghostcall's own README figure, and `src/` is 381 physical / 303 non-blank-non-comment lines. That is the exact shape ADR-0004 exists to catch — an inherited claim traceable to the subject's own prose.

One finding that is load-bearing beyond the description: `checker.check()` resolves a call by `importlib.import_module` on the **target's** import roots, executing the target's module-level code. Under ADR-0005 that can never run host-side at any size, which converts ADR-0010's sandbox placement from a preference into a constraint.

The source is **vendored** at `records/evidence/ghostcall-2026-08-02/` (11 files, 18 KB, MIT LICENSE alongside, sha256 per file in `MANIFEST.json`) rather than cited by permalink alone. It is a zero-star personal repository last pushed 2026-04-19 and the claim is a line count: if it disappears, a permalink stops being recomputable. `tests/test_claims.py` — which #118 landed hours earlier — verifies the manifest byte-for-byte, so the counts above recompute rather than being taken on report.

**The corpus.** `records/corpora/reach-2026-08-02/` pins 3 frames, 77 changes, 14,272 added source lines: mcgyvr's complete history (20 of 29 first-parent merges add source, 11,563 lines), `pallets/click` at `00e592ce` (30 commits, 1,032 lines), `immerjs/immer` at `a3be9df7` (27 commits, 1,677 lines). `tools/reach/enumerate.py --check` re-fetches both external frames **at their pinned shas** and diffs against the checked-in copy — verified reproducing from clean clones. It reads git metadata only and executes nothing from a target, so it needs no sandbox; the counts that do are not in it. `tests/test_reach_corpus.py` guards the offline half: totals equal the frames, every commit is a full sha, nothing counted twice, no path outside its frame's glob, no frame over its declared limit.

**`immerjs/immer` replaced `date-fns`**, which had become a mise/pnpm/tsgo monorepo. The substitution earns its place on more than cost: immer declares its own coverage command (`scripts.coverage`) and click declares `[tool.coverage.run]`, so Count 1 measures each external frame with the instrument that repository already declared — ADR-0006's reasoning applied to coverage. mcgyvr declares none, so there the instrument is imposed; the README says so rather than letting the frames look alike.

The README states the limits before anyone quotes a number: the external frames are **windowed, not sampled** (depth-120 clones — immer yields 27 not 30 because the window ran out, not because three were filtered); n=3 repositories is a floor set by "one per launch language", not a sample size; and **mcgyvr is 81% of the line count**, so any pooled figure is mostly a fact about this repository and the per-frame split has to travel with it. It also says plainly that "changes the acceptance rung already accepted" has no literal referent yet — the rung has never run on any of these — so what the frames really hold is changes that passed a real declared human-gated check, which is the proxy the measurement needs and not the population #125 names.

`pyproject.toml` gained `records/evidence` to ruff's `extend-exclude`, for the reason `tools/baseline` is already there: a formatter run over vendored bytes does not tidy them, it invalidates the citation. No `baseline.repo.json` change, so no judgment record is owed.

## Left open

next: #125 — Counts 1, 2 and 3, which is the rest of this issue and wants a container.

Count 1 needs each frame's declared suite run under coverage in a sandbox and intersected with `added_lines`; Count 2 (share of corpus repos and commits declaring no runnable check at all) is nearly free now the frames are pinned; Count 3 needs a candidate resolver run over known-good code, and ghostcall is now a verified candidate rather than an inherited name. Nothing has been written into #123 yet — ADR-0010 asks this lane to hand it a **size**, and a size needs Count 1.

Two things this lane did not fix. **CLM-0006 carries `_verification_note` and `_measurement_note` and so does not validate against `tools/baseline/schema/record.claim.schema.json`** (`additionalProperties: false`), exactly as lane/118 flagged for CLM-0004 and CLM-0005 — every record in the register has this shape, the gate passes because CLAIM-01/02/04 check fields rather than running the schema, and the fix is still a pinned-tree question that is not this lane's to pick. I followed the precedent rather than diverging from it mid-register. Separately, **CLM-0006 checked ghostcall's description, not its yield**: whether it is the right resolver is Count 3, and nothing here should be read as choosing it.
