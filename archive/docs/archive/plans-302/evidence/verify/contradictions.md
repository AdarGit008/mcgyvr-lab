# contradictions

## verdicts
### Q: Is #262 OPEN or CLOSED, and does #295's body carry a 'Blocked by: #262' header?
VERDICT: CORRECTED (the reader who said open is wrong): #262 is CLOSED, closed 2026-08-17T19:15:26Z. #295 is OPEN and its body's first line is '**Blocked by:** #262 ...', so the blocker header is now stale.
EVIDENCE: gh issue view 262 --json state,closedAt -> {"state":"CLOSED","closedAt":"2026-08-17T19:15:26Z"}; gh issue view 295 --json state,body -> state OPEN, body begins '**Blocked by:** #262 — the bar an author authors against...'

### Q: Does test_estimate_reserve_is_derived exist and tie ESTIMATE_RESERVE=0.32 to records/measurements/tokens-2026-08-03/units.jsonl?
VERDICT: CONFIRMED: the test exists, reads that exact file, computes the worst-vocabulary p05 over four vocabs, ceils to 2 decimals, and asserts equality with the imported constant. Re-derivation reproduces 0.32.
EVIDENCE: tests/test_four_lenses.py:429 (TOKEN_UNITS = REPO/'records'/'measurements'/'tokens-2026-08-03'/'units.jsonl') and :444-471 (asserts pytest.approx(derived) == ESTIMATE_RESERVE); src/mcgyvr/gate/preflight.py:57 ESTIMATE_RESERVE = 0.32; independent recomputation over the committed units.jsonl (2,387 rows): worst p05 = -0.31076 -> ceil(|x|*100)/100 = 0.32.

### Q: 23,902 (#282 session record) vs 23,767 (golden.json): what did #282's sweep glob, and where did the 135 replies go?
VERDICT: CORRECTED (both numbers are right; they count different populations): the sweep counts results.jsonl ROWS (23,902); golden.json counts stored reply FILES (23,767). Delta = jsts-bundle-2026-08-04's 160 rows (80 + 80 replication-srv2) that have no stored reply text and so cannot enter a reply corpus, minus 25 retry reply files (t*-c*-2.txt) in python-bundle-2026-08-07/replies/ that exist beyond its 80 results.jsonl rows. 23,902 - 160 + 25 = 23,767. No replies are missing.
EVIDENCE: Re-ran the exact script published in docs/hybrid-orchestration-prior-art-2026-08-16.md:123-137 (glob records/measurements/**/results.jsonl): total 23,902 across 136 files / 31 top-level dirs, and the 5-row cross-tab reproduces exactly (23483/386/16/10/7). Per-run diff vs golden.json entries: jsts-bundle-2026-08-04 sweep=80 gold=0, jsts-bundle-2026-08-04/replication-srv2 sweep=80 gold=0, python-bundle-2026-08-07 sweep=80 gold=105; find over jsts-bundle-2026-08-04 -name '*.txt' -> 0 files; ls python-bundle-2026-08-07/replies/ -> 80 '-1.txt' + 25 '-2.txt' = 105. golden.json totals.replies=23767 (records/corpora/worker-replies/golden.json:5); builder is tools/replies/pin.py (rglob results.jsonl, entries keyed on reply files).

### Q: report.py:23 '6 of the 139' vs identity inventory '6 of 136' — which is right and what does each count?
VERDICT: CONFIRMED for report.py: 139 run.json manifests exist under records/measurements at 4e110156 and exactly 6 carry `round`/`product_sha256` (both fields, same 6). The phrase '6 of 136' appears NOWHERE in the repo (did not find; grep -rn '6 of 136' over the whole tree, zero hits) — 136 is the population identity.inventory() keeps after skipping 3 hand-authored records whose `protocol` is a dict, and the same 6 are inside it, so both denominators describe the same fact over different populations.
EVIDENCE: tools/bench/report.py:20-25 docstring; find records/measurements -name run.json | wc -l -> 139; python count over all 139: round=6, product_sha256=6, both=6; tools/bench/identity.py:1088-1112 inventory() skips manifests where protocol is not a str -> keeps 136, skipping finetune-pilot-2026-08-07, mbpp-plus-1.5b-2026-08-11, mbpp-plus-3b-2026-08-10 (protocol is dict).

### Q: Acceptance-ceiling record: README + score.py say slowest reference 0.305s; summary.json/units.jsonl say 0.1952s — does the committed artifact support the prose?
VERDICT: REFUTED (the artifact does not support the prose): the committed data says 0.1952s — summary.json references.all.max = 0.1952 (n=514) and the max `seconds` over units.jsonl's 514 rows is 0.1952. The 0.305s figure appears only in prose (README.md:35,40; ADR-0035:78,123; tools/bench/score.py:66) and in no committed data file, and the discrepancy is present from the record's first commit (8ec819ba, squashed as 27693ac7) — the branch-commit summary.json already said 0.1952 while its README already said 0.305. The README's '98x and 393x' ratios derive from 0.305 (30/0.305=98.4, 120/0.305=393.4); from the committed 0.1952 they would be ~154x and ~615x. Note the ADR-0035 decision itself rests on the 28.718s slowest PASSING candidate, which the artifact DOES support (summary.json candidates.passing.max=28.718, slowest_pass p242-cycle-shape-report).
EVIDENCE: records/measurements/acceptance-ceiling-2026-08-17/summary.json references.all.max=0.1952; python max over units.jsonl seconds -> 0.1952 (514 rows, population 'reference'); README.md:35 '**0.305 s**' and :40; git show 8ec819ba:...summary.json -> refs max 0.1952 while git show 8ec819ba:...README.md still says 0.305 at lines 35,40; git log --all -- records/measurements/acceptance-ceiling-2026-08-17/ shows only 8ec819ba/27693ac7.

### Q: test_bench_identity.py:281 docstring '96 manifests never had a condition' vs a recount of 99
VERDICT: CORRECTED (both counts are real, over different populations): 99 of all 139 run.json files lack `condition`, but the identity module's own population — the 136 manifests inventory() keeps after excluding the 3 hand-authored records with dict `protocol` — has exactly 96 without `condition`. The docstring's figure is correct for the module it documents; the recount of 99 includes the 3 records the module excludes by shape (all 3 also lack condition: 96 + 3 = 99). The docstring's phrase 'manifests on disk' is the loose part.
EVIDENCE: tests/test_bench_identity.py:279-289; python replication of identity.py:1088-1112's filter over records/measurements/**/run.json: total=139, kept=136 (skipped: finetune-pilot-2026-08-07, mbpp-plus-1.5b-2026-08-11, mbpp-plus-3b-2026-08-10 — protocol is dict), kept without condition=96; naive count without the filter: 99.

### Q: Did #243's 'mid-trunk re-derivation pass' ever run?
VERDICT: REFUTED (it never ran as specified): #243 is OPEN with all five acceptance boxes unchecked; records/sessions/lane/243/ does not exist (did not find — ls errored, and no lane/243 anywhere under records/sessions); no commit performs its work (git log --all --grep '#243' finds only three commits that mention it in passing: two noting its exit pass unblocked, one calling a records-discipline change '#243's rule arriving early'). The closest thing is #251's four-lens audit, which the issue's single comment (2026-08-14) explicitly calls 'a partial down payment on the mid-trunk pass' — partial, not the pass. Both passes have been runnable since #113 closed 2026-08-13.
EVIDENCE: gh issue view 243 --json state -> OPEN, closedAt null; body's acceptance checklist all '- [ ]'; gh api repos/AdarGit008/mcgyvr/issues/243/comments -> 1 comment (2026-08-14T04:54:05Z): '#251's audit is a partial down payment on the mid-trunk pass'; docs/four-lens-audit-2026-08-14.md exists; git log --all --grep '#243' -> 90cb1df6, 427bc551, d83d5c1c, none doing the pass.

### Q: ADR-0002 vs the live ruleset: does the ADR promise human review that the 0-approvals setting contradicts?
VERDICT: REFUTED (no contradiction): ADR-0002 explicitly decides FOR zero approvals — 'required_approving_review_count: 0. Zero is not a weakening; it is the only honest number', reasoning that GitHub bars self-approval so any value above zero deadlocks a solo repo, and names the gate as CI + strict up-to-date + resolved conversations, 'not multi-party authorization, which this repository does not have and should not pretend to.' The live forge ruleset (id 20186295, 'main — merge protection', enforcement active) matches .github/rulesets/main.json field for field, including required_approving_review_count: 0, required_review_thread_resolution: true, strict_required_status_checks_policy: true on baseline+test pinned to integration 15368, non_fast_forward, deletion, empty bypass_actors.
EVIDENCE: docs/decisions/0002-merge-protection-on-the-default-branch.md Decision section (the required_approving_review_count: 0 bullet); .github/rulesets/main.json; live read: gh api repos/AdarGit008/mcgyvr/rulesets -> id 20186295 active; gh api repos/AdarGit008/mcgyvr/rules/branches/main -> pull_request parameters required_approving_review_count: 0, matching the committed file exactly.

## new_findings
- #295 carries a stale blocker header: its body opens with 'Blocked by: #262' but #262 closed 2026-08-17T19:15:26Z while #295 stays open unamended — the exact failure shape #243's own comment names ('a blocker that has cleared and still reads as blocking').
- The acceptance-ceiling record shipped self-contradictory from birth: prose figure 0.305s (README.md:35,40; docs/decisions/0035:78,123; tools/bench/score.py:66) vs committed data 0.1952s (summary.json + units.jsonl max over 514 reference rows), both landed in the same commit (8ec819ba -> 27693ac7). 0.305 appears in no committed data file anywhere. The derived ratios 98x/393x are computed from the unsupported figure (correct values from committed data: ~154x/~615x). The ADR-0035 decision itself is unaffected — it rests on the 28.718s slowest passing candidate, which summary.json supports.
- Terminology collision on 'refusal': golden.json totals say refusals: 414, while ADR-0031/#282 say 'no refusal has ever occurred in 23,902 replies'. Not a contradiction — pin.py:205 labels any parse ReplyError under WHOLE_FILE re-parse as 'refusal' (parser refusal), whereas ADR-0031 means model refusals; but the shared word invites exactly the misreading a phase-1 reader made.
- The 23,902/23,767 pair reconciles exactly: 160 jsts-bundle-2026-08-04 rows (both arms) whose raw reply text was never stored on disk (0 .txt files), and 25 retry reply files (-2.txt) in python-bundle-2026-08-07/replies/ beyond its 80 results.jsonl rows. jsts-bundle is the only run directory in the tree whose replies cannot be re-verified byte-for-byte because only rows survive.
- The 139/136/99/96 counts all describe one tree: 139 run.json files; 3 hand-authored (dict protocol: finetune-pilot-2026-08-07, mbpp-plus-1.5b-2026-08-11, mbpp-plus-3b-2026-08-10) are excluded by identity.inventory()'s shape filter, leaving 136; 96 of the 136 lack condition (99 of all 139). The phrase '6 of 136' exists nowhere in the repo.
- #243 is fully unblocked (both passes, since #113 closed 2026-08-13) and unstarted — the trunk's exit-gate re-derivation has never run; #251's four-lens audit is on record as only a partial down payment.

## plan_input
- Amend #295's header: strike or annotate 'Blocked by: #262' — #262 closed 2026-08-17T19:15:26Z; #295 is now unblocked on that edge.
- Fix the acceptance-ceiling figure drift in one of two ways: correct 0.305s -> 0.1952s (and 98x/393x -> ~154x/~615x) in records/measurements/acceptance-ceiling-2026-08-17/README.md:35,40, docs/decisions/0035:78,123, and tools/bench/score.py:66, or re-run `uv run --no-sync python tools/bench/ceiling.py --out records/measurements/acceptance-ceiling-2026-08-17` and land prose and data together. The ADR-0035 decision (28.718s slowest pass under a 30s ceiling) is supported by summary.json and needs no change.
- Schedule #243's mid-trunk pass explicitly — it is OPEN, unblocked since 2026-08-13, has no lane, and this review just found a fifth instance of its motivating defect (the 0.305 figure standing in three documents against its own committed data).
- Consider renaming golden.json's totals key 'refusals' (parser ReplyError count, pin.py:205/245) or adding a one-line gloss, so it cannot be read against ADR-0031's 'no refusal has ever occurred' (model refusals).
- When citing manifest counts, state the population: 139 run.json on disk; 136 machine-written (identity.inventory()'s filter); 6 carry round/product_sha256; 96 of 136 (99 of 139) lack condition. docs/identity-surface-2026-08-16.md:11-15 already warns that a manifest count is only meaningful with its population and commit stated.
- jsts-bundle-2026-08-04 (both arms, 160 rows) stores no raw reply text, so it is permanently outside any reply-corpus re-verification; if byte-level reply provenance is ever made a requirement, this run needs an explicit exemption record.
