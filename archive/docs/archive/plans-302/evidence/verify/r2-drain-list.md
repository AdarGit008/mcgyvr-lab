# r2-drain-list

## verdicts
### Q: What does `python3 tools/bench/product.py --check` report against r1's pin?
VERDICT: CONFIRMED — refusal, exit 1. r1-commissioning pins ed508e612ff8… and the tree is efadd3625d3e…. 17 files moved: data/task-catalog.json, eslint.config.mjs, package-lock.json, prettier.config.mjs, pyproject.toml, src/mcgyvr/gate/__init__.py, src/mcgyvr/gate/adapter.py, src/mcgyvr/gate/adapters/javascript.py, src/mcgyvr/gate/adapters/python.py, src/mcgyvr/gate/runner.py, src/mcgyvr/prompts/javascript.md, src/mcgyvr/prompts/python.md, tools/bench/product.py, tools/bench/score.py, tools/breadth/measure.py, tools/bundle/measure.py, uv.lock. Surface is now 64 files vs the 56 r1 recorded — exactly the 8 joins ADR-0032's consequences predicted.
EVIDENCE: python3 tools/bench/product.py --check (exit 1, Changed: list above); python3 -c 'surface_files()' → 64 files, digest efadd3625d3e4fa5146fb0a18a778ae5243b1b24756081feda76ca5d8d8e16de; rounds.json r1 files map has 56 entries

### Q: Which merged issue moved each file — the complete --adopted batch?
VERDICT: CONFIRMED — exactly five merges moved the pin; every other merge since r1 (checked all 15 first-parent commits 205c7f00..HEAD) touched no surface file. (1) #291/PR#293 (10dc1167, ADR-0032): widened SURFACE — pyproject.toml, eslint.config.mjs, uv.lock, package-lock.json, data/task-catalog.json joined; directory glob went *.py→every-file, pulling in src/mcgyvr/prompts/{python,javascript}.md (contents unchanged since #167/#144); product.py edited. (2) #261/PR#292 (aed0edec): crashed-linter-is-a-refusal — gate/__init__.py, adapter.py, adapters/javascript.py, adapters/python.py, runner.py, score.py, breadth/measure.py. (3) #285/PR#294 (5d892ee7, ADR-0033): bar/prompt/weights hashed where resolved — breadth/measure.py. (4) #262/PR#298 (27693ac7, ADR-0035): bar recorded as content, one acceptance ceiling — created prettier.config.mjs and added it to SURFACE; product.py, score.py, breadth/measure.py, bundle/measure.py. (5) #275 dependabot (feadb92f): ruff 0.16.1→0.16.2 — pyproject.toml, uv.lock. Note #265/PR#284 (97173b2a) is NOT in the batch: it touched only identity.py/report.py/tests (read-time, off-surface) and changed no field a run records.
EVIDENCE: git log --oneline --first-parent --since=2026-08-14 -- <each moved file>; git show --stat for 4e110156, eb02e5a3, 5421a875, 79489029, ca99fe5a, 97173b2a, 87727dd8, d67dab86, b7209c13, e28bb46e grepped against the surface list → zero hits

### Q: What is the exact command an operator runs to open r2?
VERDICT: CONFIRMED (built from product.py _open_cli:363-418, main:448-457, and rounds.json doctrine). After every pre-r2 change is merged and the tree is clean at the tip: uv run --no-sync python tools/bench/product.py --open r2-<name> --opened <UTC YYYY-MM-DD> --issue 231 --why "<what this boundary is for>" --adopted "#291 the pin covers the bar's configuration and every file under a surface directory (ADR-0032)" --adopted "#261 a crashed linter is a refusal, not an empty finding list" --adopted "#285 bar_sha256/prompt_sha256/weights recorded where they are resolved (ADR-0033)" --adopted "#262 the bar recorded as content; prettier.config.mjs authored and pinned; one acceptance ceiling (ADR-0035)" --adopted "#275 ruff 0.16.1->0.16.2 (pyproject.toml, uv.lock)" — plus one --adopted line per pre-r2 item that lands (#286, #287, accept.py-into-digest, the config.Tier docstring). --open refuses without --adopted, --why AND --opened; it prints the four doctrine clauses and the moved list, appends to rounds.json (append-only — a duplicate id is refused), computes the digest at open time from the tree as it stands, and records the per-file map. Then re-run --check: expect exit 0, '64+ files in the surface, and the tree matches'.
EVIDENCE: tools/bench/product.py:363-418 (_open_cli), :383-393 (--adopted refusal), :451-456 (--why/--opened refusal), :373-376 (append-only refusal); rounds.json doctrine.clauses 1-4; the uv run --no-sync invocation is product.py's own recommended form (require_pinned error text, :283-285)

### Q: #286 (observed block): land before r2?
VERDICT: CONFIRMED — yes. It is written by both runners (issue scope: 'Written by both runners, computed inside the module'), and both tools/breadth/measure.py and tools/bundle/measure.py are in SURFACE, so implementing it moves the pin; deferring it forces an r3 the day it lands. It adds nothing to GROUPS/KEY (out of scope by construction), so the identity semantics are unchanged — the pin moves purely because runner files change. Also #231's reproducibility work 'cannot interpret a re-run comparison without concurrency', so r2 runs made without it are permanently weaker records. Blast radius: 2 surface files; the new observed module itself is off-surface if placed under tools/bench/.
EVIDENCE: gh issue view 286 (scope: 'Written by both runners'; out of scope: promoting into GROUPS/KEY); product.py SURFACE:117-160 lists both measure.py files

### Q: #287 (drift-check wiring): does fixing it touch measure.py, i.e. move the pin?
VERDICT: CONFIRMED — yes, both measure.py files. The defect is still live at tools/breadth/measure.py:1086 and tools/bundle/measure.py:674 (the issue's cited :949/:665 have drifted but the code is verbatim what it quotes: drift computed over the local identity dict, not identity.drift). Both files are in SURFACE, so the fix moves the pin — land before r2 or force r3. The `language`/`conditions_sha256` decision may additionally grow identity.GROUPS (identity.py is off-surface, but doctrine clause 2 makes any recorded GROUPS field an adopted change, so that half also belongs at the boundary). The issue's own sequencing agrees: 'it should land first — cheapest of the four'.
EVIDENCE: grep -n drift tools/breadth/measure.py tools/bundle/measure.py → breadth :1086, bundle :674, both `sorted(k for k, v in identity.items() if previous.get(k) != v)`; rounds.json doctrine clause 2

### Q: #288 (tag directories): land before r2?
VERDICT: CORRECTED — defer, unless the physical move is actually chosen, in which case it must be pre-r2. records/ is off-surface, BUT the machine-read references to records/measurements live partly in surface files: tools/breadth/measure.py, tools/bundle/measure.py, tools/bench/score.py, and src/mcgyvr/prompts/python.md:15 (a prompt file, in the digest since #291). A physical move that rewrites those paths moves the pin. The issue itself argues options 2/3 and says the move 'blocks nothing' and is 'last of the four by value'; option 3 (no move, generated index) has zero surface impact and forces no r3. Decision recommendation: pick option 3 now (or explicitly defer the decision), and only if a move is chosen does it join the pre-r2 batch.
EVIDENCE: grep -l 'records/measurements' over surface files → score.py, bundle/measure.py, breadth/measure.py, src/mcgyvr/prompts/python.md:15; gh issue view 288 ('Why nothing is blocked on this'; options 1-3)

### Q: The ADR-0035 renumber: which file, and does it touch the surface?
VERDICT: CONFIRMED as a real collision, zero blast radius. Two Accepted ADRs both claim 0035, both dated 2026-08-17: #262's 'bar recorded as content' (merged first, 27693ac7) and #301's 'bare word bar is banned' (merged last, 4e110156). Every in-tree code citation of ADR-0035 — product.py:138, score.py:55,:108 (surface files), admit.py:83, ceiling.py:7,:166 — means #262's ADR. So renumber #301's file to 0036: a docs/-only rename, no surface file changes, no pin movement, forces no r3, timing-independent — but do it before r2 so the round record's citations are unambiguous. Renumbering in the other direction would require editing two surface files and is the wrong move.
EVIDENCE: ls docs/decisions → both 0035-*.md; grep -rn ADR-0035 across src/, tools/ → all six citations resolve to #262's acceptance-ceiling/prettier meaning; merge order from git log --first-parent

### Q: accept.py-into-digest (identity-surface §3.1): land before r2?
VERDICT: CONFIRMED — yes, and it needs an owner first (it has none: ADR-0033's fan-out does not name it, no open issue covers it). Still live: tools/breadth/measure.py:434 tier_digests and tools/bundle/measure.py:605 both hash only bundle.dumps(task.contract) — the acceptance COMMAND string, never the accept.py file. The fix touches both runners (surface → pin moves) AND changes every recorded tasks_sha256 value (a recorded field — doctrine clause 2 twice over). Deferring forces r3, and worse: #295's active corpus authoring means graders WILL be edited during r2, mutable with no record — the silent-re-baseline class the round exists to refuse. reference.py needs the different disposition §3.1 states (never staged, cannot affect a verdict).
EVIDENCE: sed tier_digests tools/breadth/measure.py:434-440; grep tools/bundle/measure.py:605; docs/identity-surface-2026-08-16.md §3.1 ('DIGEST MOVED: False'); ADR-0033 fan-out table has no accept.py row

### Q: gate_rescore.py / lintless.py into SURFACE: land before r2?
VERDICT: CORRECTED — not required to honor the drained boundary, but if the project ever wants them pinned it must be pre-r2, because widening SURFACE edits product.py (itself in the surface) and moves the pin. They never dispatch (both re-score candidates already on disk at zero model cost, never rewriting original rows), and gate_rescore imports checker_digests from regrade so a re-score names its own checker versions — weaker than a pin but not nothing. ADR-0032's fan-out left them 'unowned, file if r2 wants it'. Blast radius if included: product.py + 2 files join the digest; also invites the boundary question for regrade.py, which shares the mechanism. Recommendation: decide explicitly at the r2 batch (one sentence in the round's --why either way) rather than drift.
EVIDENCE: ADR-0032 Fan-out row 1; heads of tools/bench/gate_rescore.py ('changes the scorer… checker_digests imported from regrade') and lintless.py ('re-parses and re-scores… writes nothing into the run directories'); product.py is in its own SURFACE:129

### Q: BOUND_MATCH cells (ADR-0027 D9): land before r2?
VERDICT: REFUTED as a pre-r2 requirement — read-path only, zero pin blast radius, forces no r3. BOUND_MATCH lives at tools/bench/identity.py:265 (4 fields), enforced in report.py:221; reproducibility.json's matching prose already states five fields against the tuple's four; none of identity.py/report.py/reproducibility.json is in SURFACE, and product.py:66-68 deliberately excludes read-time tools. ONE caveat: identity.py:279-288 documents that BOUND_MATCH gaining cells breaks the tested invariant set(BOUND_MATCH) <= set(KEY), and one of the two resolutions — 'cells becomes a recorded field of the run' — would touch the runners and then WOULD belong in the pre-r2 batch. #231 owns closing it; decide the invariant's fate before r2 even if the code lands after.
EVIDENCE: tools/bench/identity.py:265 BOUND_MATCH, :288 BOUND_MATCH_PENDING=('cells',), :279-288 invariant note; report.py:221; reproducibility.json 'matching' field; product.py:66-68 read-time exclusion

## new_findings
- A pre-r2 item nobody's list carries: tools/bench/identity.py:259-264 explicitly DEFERS a cross-reference docstring on src/mcgyvr/config.py's Tier class because src/mcgyvr is in SURFACE — 'It lands with the identity range #276's sequencing already schedules before r2 opens.' If r2 opens without it, that one-docstring change forces or waits for r3.
- product.py's stated reason for excluding tools/bench/tasks/ from SURFACE ('the task set is already pinned per run by tasks_sha256', product.py:59-61) is false for the per-task graders: identity-surface §3.1 demonstrates tasks_sha256 does not cover accept.py, so the graders are pinned by NEITHER mechanism. The accept.py fix is therefore not just an improvement — it repairs the premise of a surface exclusion.
- src/mcgyvr/prompts/python.md:15 cites a records/measurements path and is now inside the product digest (since #291's every-file glob) — so #288's physical move, if it rewrites that citation, moves the pin through a PROMPT file, coupling a records-layout decision to the round boundary in a way nobody has stated.
- #287's cited line numbers have drifted since it was written (breadth drift check now at measure.py:1086, was :949; bundle at :674, was :665) — the defect is intact, but a driver grepping the issue's line numbers verbatim finds nothing.
- The five pin-moving merges are already 5 of the 6+ --adopted lines, and one of them is a dependabot commit (#275, ruff 0.16.1→0.16.2) — the first instance of ADR-0032's admitted cost 'a dependabot bump now closes a round' actually occurring; the r2 record should name it as such so the precedent is legible.
- identity.py:265's comment block confirms the surface freeze is already distorting work: at least one correct edit (the Tier docstring) is being withheld solely because r1 is still formally open while --check already refuses — the tree has been un-dispatchable since 10dc1167 (#291, 2026-08-17), so there is no measurement reason left to delay the pre-r2 batch.

## plan_input
- r2 opening procedure, in order: (1) land the pre-r2 batch on main — #287 (both drift checks onto identity.drift; decide language/conditions_sha256 into GROUPS or out of the guard), #286 (observed block beside run.json, both runners), accept.py-into-tasks_sha256 (needs a new owning issue first; touches breadth/measure.py:434 and bundle/measure.py:605), the deferred config.Tier docstring (identity.py:259-264), and the #301 ADR renumber to 0036 (docs-only, for citation hygiene); (2) decide and record the two optional widenings — gate_rescore.py/lintless.py into SURFACE (must be now or never-until-r3), and #288 option 3 (no physical move; generated index) — one sentence each; (3) on the clean merged tip run python3 tools/bench/product.py --check (expect the refusal listing everything that moved), then open with: uv run --no-sync python tools/bench/product.py --open r2-<name> --opened <date> --issue 231 --why "..." plus one --adopted line per change (five are already fixed: #291 ADR-0032 surface widening; #261 crashed-linter refusal; #285 ADR-0033 four digests; #262 ADR-0035 prettier config + acceptance ceiling; #275 ruff 0.16.1→0.16.2 — plus one per pre-r2 landing); (4) re-run --check, expect exit 0 and 'tree matches'.
- The r3-forcing test sorts the seven candidates cleanly: #286, #287, and accept.py-into-digest all touch tools/{breadth,bundle}/measure.py (in SURFACE) so deferring any of them forces r3 — land before r2. The ADR-0035 renumber (docs-only) and BOUND_MATCH cells/D9 (identity.py/report.py/reproducibility.json, all off-surface by design, product.py:66-68) move nothing — they can land any time, EXCEPT that D9's resolution must be decided first because one of its two options (cells as a recorded run field) would touch the runners. #288 forces r3 only if the physical move is chosen — recommend option 3, zero surface impact.
- Blast radii on product.SURFACE, measured: #286 = 2 surface files (both runners; new module off-surface if under tools/bench/). #287 = 2 surface files + possible GROUPS growth (clause-2 identity change even though identity.py is off-surface). accept.py fix = 2 surface files + every recorded tasks_sha256 value changes. #288 physical move = ≥4 surface files hold records/measurements references (score.py, both measure.py, src/mcgyvr/prompts/python.md:15). gate_rescore/lintless = product.py + 2 joining files. ADR renumber = 0 (all six in-code ADR-0035 citations mean #262's ADR; renumber #301's file, never #262's — the reverse direction would edit product.py and score.py). D9 read-path = 0.
- The tree has been un-dispatchable since 2026-08-17 (10dc1167 made --check refuse by construction, per ADR-0032's own consequences), so nothing measurable is lost by batching aggressively now — the only cost of adding one more pre-r2 item is calendar time, while the cost of omitting one is a full r3 re-baseline. The doctrine's own drain rule (rounds.json clause 1) says to take them all.
- Two bookkeeping facts the r2 record should state to stay honest: the surface grew 56→64 files between r1 and r2 (the 8 joins: 5 config/lock/catalog files from #291, 2 prompt .md files from the glob widening, prettier.config.mjs from #262), and 7 of the 17 'moved' files never changed content — they joined the digest (data/task-catalog.json, eslint.config.mjs, package-lock.json, both prompts unchanged since #144/#167) or joined at creation (prettier.config.mjs); only the gate files, score.py, product.py, both measure.py, pyproject.toml and uv.lock actually changed bytes.
- #265 needs no --adopted line: verified against git show --stat 97173b2a — it touched only read-time files (identity.py, report.py, tests) and changed no recorded field, so under doctrine clause 2 it is not an adopted change. Naming it anyway would misstate what the boundary carries.
